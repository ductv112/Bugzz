---
phase: 04-filter-catalog-picker-face-filter-mode
plan: 02
type: execute
wave: 0
depends_on: [01]
files_modified:
  - app/src/test/java/com/bugzz/filter/camera/render/CrawlBehaviorTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/SwarmBehaviorTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/FallBehaviorTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/BehaviorStateMapTest.kt
  - app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogExpandedTest.kt
  - app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt
autonomous: true
requirements: []
threat_refs: [T-04-01, T-04-02, T-04-05]

must_haves:
  truths:
    - "6 new test files exist; each has ≥3 @Test methods covering Phase 4 behavior"
    - "FilterEngineTest.kt extended with multiFace_* and softCap_* @Test methods"
    - "CameraViewModelTest.kt extended with onSelectFilter + initialBind + rapidSelectFilter @Test methods"
    - "All new and extended Phase 4 tests @Ignore'd with TODO-Plan-04-0X annotation pointing at the SUT-landing plan"
    - "`./gradlew :app:testDebugUnitTest` exits 0 — existing 10 Phase 3 GREEN tests still GREEN; new tests compile and are @Ignore'd (or RED with clear failure text)"
  artifacts:
    - path: "app/src/test/java/com/bugzz/filter/camera/render/CrawlBehaviorTest.kt"
      provides: "RED tests for D-08 CRAWL progress advance/wrap + crawlPosition() linear interpolation"
      min_lines: 60
    - path: "app/src/test/java/com/bugzz/filter/camera/render/SwarmBehaviorTest.kt"
      provides: "RED tests for D-09 instance init/drift/respawn"
      min_lines: 60
    - path: "app/src/test/java/com/bugzz/filter/camera/render/FallBehaviorTest.kt"
      provides: "RED tests for D-10 spawn timing/gravity/despawn"
      min_lines: 60
    - path: "app/src/test/java/com/bugzz/filter/camera/render/BehaviorStateMapTest.kt"
      provides: "RED tests for D-13 perFaceState lifecycle (setFilter clear / onFaceLost remove / getOrPut creates)"
      min_lines: 50
    - path: "app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogExpandedTest.kt"
      provides: "RED tests for CAT-01/CAT-02 — 15 entries, all fields populated, assetDir in {sprite_spider, sprite_bugA, sprite_bugB, sprite_bugC}"
      min_lines: 40
    - path: "app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt"
      provides: "RED tests for CAT-05 + T-04-01 — DataStore round-trip + corruption fallback"
      min_lines: 70
    - path: "app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt"
      provides: "EXTENDED with multiFace_primaryGetsContourAnchor_secondaryGetsBboxCenter + softCap_halvesSwarmInstancesWhenExceeded tests (MOD-02, D-14)"
      contains: "multiFace_primary"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt"
      provides: "EXTENDED with onSelectFilter_callsEngineAndWritesDataStore + initialBind_readsLastUsedFromDataStore + rapidSelectFilter_noCameraRebind tests (CAT-04, CAT-05)"
      contains: "onSelectFilter_callsEngineAndWritesDataStore"
  key_links:
    - from: "Wave 0 test files"
      to: "Wave 1 (BehaviorState), Wave 2 (FilterEngine multiFace + catalog), Wave 3 (DataStore + picker wire)"
      via: "@Ignore annotation removal when SUT lands (RED → GREEN transition)"
      pattern: "@Ignore\\(\"Plan 04-0[3-7]"
---

<objective>
Nyquist gate — land all Phase 4 unit test scaffolds BEFORE any production-code waves. Every CAT-01..05, MOD-01, MOD-02, D-08..D-14, D-25 behavior gets at least one RED `@Test`. Two existing Phase 3 test files (`FilterEngineTest.kt`, `CameraViewModelTest.kt`) are EXTENDED, not replaced.

Purpose: Phase 2 + 3 proved this pattern — Wave 0 commits the test shapes first, executors in later waves un-`@Ignore` as each SUT lands. Prevents API drift during implementation, forces contract-first thinking, and keeps feedback latency <90s per commit (per 04-VALIDATION §Sampling Rate).

Output: 6 new `*Test.kt` files + 2 extended test files. Zero production-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-CONTEXT.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-RESEARCH.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md
@.planning/phases/03-first-filter-end-to-end-photo-capture/03-01-PLAN.md
@.planning/phases/03-first-filter-end-to-end-photo-capture/03-01-SUMMARY.md
@app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt
@app/src/test/java/com/bugzz/filter/camera/render/BugBehaviorTest.kt
@app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt
@app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogTest.kt
@app/src/main/java/com/bugzz/filter/camera/render/BugBehavior.kt
@app/src/main/java/com/bugzz/filter/camera/render/BugState.kt
@app/src/main/java/com/bugzz/filter/camera/render/FilterEngine.kt
@app/src/main/java/com/bugzz/filter/camera/filter/FilterDefinition.kt
@app/src/main/java/com/bugzz/filter/camera/filter/FilterCatalog.kt
@app/src/main/java/com/bugzz/filter/camera/detector/SmoothedFace.kt
@app/src/main/java/com/bugzz/filter/camera/detector/FaceLandmarkMapper.kt
</context>

<interfaces>
<!-- Types the tests will reference. Some land in Plan 04-03/04-04/04-05 — this plan must compile-stub them or use @Ignore to keep compile green. -->

<!-- Existing (Phase 3) — unchanged: -->
- `com.bugzz.filter.camera.detector.SmoothedFace` — already has `faces: List<SmoothedFace>` compatible shape, `boundingBox: Rect`, `contours: Map<Int, List<PointF>>`, `trackingId: Int`
- `com.bugzz.filter.camera.detector.FaceLandmarkMapper.Anchor` — enum {NOSE_TIP, FOREHEAD, LEFT_CHEEK, RIGHT_CHEEK, CHIN, LEFT_EYE, RIGHT_EYE}
- `com.bugzz.filter.camera.filter.FilterDefinition(id, displayName, anchorType, behavior, frameCount, frameDurationMs, scaleFactor, assetDir, mirrorable)`
- `com.bugzz.filter.camera.filter.FilterCatalog.all: List<FilterDefinition>` + `byId(id)`
- `com.bugzz.filter.camera.render.BugBehavior` — sealed interface with Static/Crawl/Swarm/Fall variants
- `com.bugzz.filter.camera.render.BugState` data class — Phase 3 shape; Plan 04-03 renames → BehaviorState sealed interface

<!-- New types landing in Plan 04-03 (Wave 1) — tests reference by fully-qualified name and @Ignore till landed: -->
- `com.bugzz.filter.camera.render.BehaviorState` — sealed interface (replaces BugState)
  - `BehaviorState.Static(pos: PointF)`
  - `BehaviorState.Crawl(var progress: Float, val direction: CrawlDirection)`
  - `BehaviorState.Swarm(val instances: MutableList<BugInstance>)`
  - `BehaviorState.Fall(val instances: MutableList<FallingBug>, var nextSpawnNanos: Long)`
- `com.bugzz.filter.camera.render.CrawlDirection` enum {CW, CCW}
- `com.bugzz.filter.camera.render.BugInstance(position, velocity, frameIndex)`
- `com.bugzz.filter.camera.render.FallingBug(position, velocity, spawnNanos)`
- `com.bugzz.filter.camera.render.BugBehavior.crawlPosition(contour: List<PointF>, progress: Float): PointF` (companion helper)
- `com.bugzz.filter.camera.render.BugBehavior.swarmTick(state, anchor, bbox, config, dtMs)` (or members on Swarm object)
- `com.bugzz.filter.camera.render.BugBehavior.fallTick(state, previewWidth, previewHeight, config, tsNanos, dtMs)`
- `com.bugzz.filter.camera.filter.SwarmConfig(instanceCount: Int, driftSpeedRange: ClosedRange<Float>)`
- `com.bugzz.filter.camera.filter.CrawlConfig(direction: CrawlDirection, speedFactor: Float)`
- `com.bugzz.filter.camera.filter.FallConfig(spawnIntervalMsRange: IntRange, gravity: Float, maxInstances: Int)`
- `com.bugzz.filter.camera.filter.BehaviorConfig` sealed interface grouping the three configs (optional field on FilterDefinition)

<!-- New types landing in Plan 04-04 (Wave 2) — referenced by tests, @Ignore'd till landed: -->
- `com.bugzz.filter.camera.render.FilterEngine` — extended API:
  - `companion object { const val MAX_DRAWS_PER_FRAME = 20 }`
  - `val perFaceState: ConcurrentHashMap<Int, BehaviorState>` (internal-visible for tests OR expose count helper)
  - `fun onFaceLost(trackingId: Int)` — removes entry
  - `fun onDraw(canvas, frame, faces: List<SmoothedFace>)` — signature CHANGE from Phase 3's `face: SmoothedFace?`
  - FilterCatalog.all has 15 entries (new ids per D-02 amended roster)

<!-- New types landing in Plan 04-05 (Wave 3) — referenced by tests, @Ignore'd till landed: -->
- `com.bugzz.filter.camera.data.FilterPrefsRepository` — @Singleton @Inject
  - `val lastUsedFilterId: Flow<String>`
  - `suspend fun setLastUsedFilter(id: String)`
  - `companion object { const val DEFAULT_FILTER_ID = "spider_nose_static" }` (first entry in D-02 roster)
- Extension: `val Context.dataStore: DataStore<Preferences>` (module-level delegate)
- `CameraViewModel` — extended:
  - ctor param `FilterPrefsRepository` injected
  - `fun onSelectFilter(id: String)` — writes DataStore + sets engine + updates uiState
  - `bind()` reads DataStore initial
- `CameraUiState` — extended: `filters: List<FilterSummary>`, `selectedFilterId: String`
- `com.bugzz.filter.camera.ui.camera.FilterSummary(id: String, displayName: String, assetDir: String)` — DTO for picker

<!-- SMOOTHEDFACE constructor (from existing tests) — keep pattern consistent with Phase 3 tests: -->
```kotlin
// See FilterEngineTest.kt lines 48-80 for existing SmoothedFace mock pattern.
private fun buildFace(
    trackingId: Int = 1,
    boundingBox: Rect = Rect(100, 100, 300, 400),
    contours: Map<Int, List<PointF>> = emptyMap()
): SmoothedFace = SmoothedFace(trackingId, boundingBox, contours, emptyMap())
```

<!-- @Ignore convention — identical to Phase 3 Wave 0 pattern: -->
```kotlin
@Test
@Ignore("TODO Plan 04-03 — un-Ignore when CRAWL impl lands")
fun crawl_progressAdvancesPerDtMs() { ... }
```

<!-- runTest + StandardTestDispatcher pattern — reuse from existing CameraViewModelTest.kt lines 40-95 -->
```kotlin
@OptIn(kotlinx.coroutines.ExperimentalCoroutinesApi::class)
class FooTest {
    private val testDispatcher = StandardTestDispatcher()
    @Before fun setUp() { Dispatchers.setMain(testDispatcher) }
    @After fun tearDown() { Dispatchers.resetMain() }
}
```

<!-- Turbine pattern for Flow testing: -->
```kotlin
repository.lastUsedFilterId.test {
    assertEquals("default", awaitItem())   // or the stored value
    cancelAndIgnoreRemainingEvents()
}
```

<!-- InMemoryDataStore test factory — datastore-preferences-core: -->
```kotlin
import androidx.datastore.core.DataStoreFactory
import androidx.datastore.preferences.core.PreferenceDataStoreFactory
import androidx.datastore.preferences.core.Preferences
import java.io.File

val tempDir: File = TemporaryFolder().apply { create() }.newFolder()
val testDataStore = PreferenceDataStoreFactory.create(
    corruptionHandler = null,
    produceFile = { File(tempDir, "test.preferences_pb") }
)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land 4 behavior + state tests under render/ — all @Ignore'd RED</name>
  <files>app/src/test/java/com/bugzz/filter/camera/render/CrawlBehaviorTest.kt, app/src/test/java/com/bugzz/filter/camera/render/SwarmBehaviorTest.kt, app/src/test/java/com/bugzz/filter/camera/render/FallBehaviorTest.kt, app/src/test/java/com/bugzz/filter/camera/render/BehaviorStateMapTest.kt</files>
  <read_first>
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CONTEXT.md D-08, D-09, D-10, D-12, D-13
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-RESEARCH.md §Pattern 1, §Pattern 2 (CRAWL), §Pattern 3 (SWARM), §Pattern 4 (FALL)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/03-first-filter-end-to-end-photo-capture/03-01-PLAN.md Task 2 §buildFace helper pattern
    - app/src/test/java/com/bugzz/filter/camera/render/BugBehaviorTest.kt (existing pattern to mirror)
    - app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt lines 40-60 (SmoothedFace mock pattern)
  </read_first>
  <behavior>
    - CrawlBehaviorTest: `crawl_progressAdvancesPerDtMs` (100ms dt → progress increases proportional to dtMs * 0.5 * faceBoxWidth / previewWidth); `crawl_progressWrapsAt1_0` (progress starts at 0.95, after dt → progress < 1.0 per modulo); `crawlPosition_interpolatesLinearlyBetweenVertices` (contour = 4 points at axis-aligned corners, progress 0.125 should land at midpoint of first edge); `crawlPosition_handlesEmptyContourGracefully` (returns boundingBox center, does not throw)
    - SwarmBehaviorTest: `swarm_initializesInstancesOnFirstTick` (empty state.instances → after tick, instances.size == config.instanceCount); `swarm_instanceDriftsTowardAnchor` (instance at bbox edge, anchor at center, after 1s tick → distance(anchor, position) decreased); `swarm_respawnsAtBboxEdgeWhenCloseToAnchor` (position at 0.1*bbox.width from anchor → after tick, position on bbox edge)
    - FallBehaviorTest: `fall_spawnsBugAtTopWhenIntervalElapsed` (nextSpawnNanos <= tsNanos → size increases by 1; bug.position.y == 0 initially); `fall_bugGravityAdvancesPositionY` (after dtMs, bug.position.y increases by gravity * dtSec); `fall_despawnsBugAtBottomBoundary` (bug at y > previewHeight → removed after tick); `fall_respectsMaxInstancesCap` (config.maxInstances=3, 10 ticks worth of spawns → state.instances.size <= 3)
    - BehaviorStateMapTest: `setFilter_clearsPerFaceStateMap` (put 2 entries, setFilter(other) → map.isEmpty()); `onFaceLost_removesThatIdOnly` (put id=1,id=2, onFaceLost(1) → only id=2 remains); `getOrPut_createsFreshStateForNewTrackingId` (map empty, access id=5 → entry created with filter's behavior type, default field values)
  </behavior>
  <action>
  Create 4 new test files. Every `@Test` annotated `@Ignore("TODO Plan 04-03 Task X — un-Ignore when [SUT] lands")`. Files compile-clean against current codebase — if a test references a type that doesn't yet exist (BehaviorState, CrawlConfig, etc.), comment out the @Test body with a TODO and leave the method empty — this keeps the sourceset compiling. Prefer the `@Ignore` pattern over commenting when possible.

  **CrawlBehaviorTest.kt** — test D-08 CRAWL progress math:

  ```kotlin
  package com.bugzz.filter.camera.render

  import android.graphics.PointF
  import android.graphics.Rect
  import com.bugzz.filter.camera.detector.SmoothedFace
  import org.junit.Assert.assertEquals
  import org.junit.Assert.assertTrue
  import org.junit.Ignore
  import org.junit.Test
  import org.junit.runner.RunWith
  import org.robolectric.RobolectricTestRunner
  import org.robolectric.annotation.Config

  /**
   * Nyquist Wave 0 tests for [BugBehavior.Crawl] impl (D-08 / REN-02).
   *
   * Plan 04-03 replaces the Phase 3 TODO stub with the real implementation and un-Ignores these tests.
   * Robolectric needed for PointF real implementation.
   */
  @RunWith(RobolectricTestRunner::class)
  @Config(sdk = [34])
  class CrawlBehaviorTest {

      @Test
      @Ignore("TODO Plan 04-03 Task 2 — un-Ignore when CRAWL impl lands")
      fun crawl_progressAdvancesPerDtMs() {
          // D-08: progress += dtSeconds * 0.5f * faceBoxWidth / previewWidth
          // With dtMs=1000 (1sec), faceBoxWidth=200, previewWidth=400 → progressDelta = 0.25
          // State progress 0f → after tick, progress ≈ 0.25f
          val state = BehaviorState.Crawl(progress = 0f, direction = CrawlDirection.CW)
          val face = buildFace(contours = mapOf(com.google.mlkit.vision.face.FaceContour.FACE to sampleContour()))
          val anchor = PointF(200f, 250f)
          // BugBehavior.Crawl.tick(state, face, anchor, dtMs=1000L, previewWidth=400f)
          // TODO Plan 04-03: adapt signature once interface is finalized
          // After tick: assertEquals(0.25f, state.progress, 0.01f)
      }

      @Test
      @Ignore("TODO Plan 04-03 Task 2 — un-Ignore when CRAWL impl lands")
      fun crawl_progressWrapsAt1_0() {
          val state = BehaviorState.Crawl(progress = 0.95f, direction = CrawlDirection.CW)
          // After tick with dtMs that adds ≥ 0.05 → progress wraps to ≥ 0.0 and < 0.95
          // TODO Plan 04-03: verify progress < 0.95f AND progress >= 0f after wrap
      }

      @Test
      @Ignore("TODO Plan 04-03 Task 2 — un-Ignore when crawlPosition() companion helper lands")
      fun crawlPosition_interpolatesLinearlyBetweenVertices() {
          // 4-vertex axis-aligned square contour; progress 0.125 = halfway along first edge
          val contour = listOf(
              PointF(0f, 0f), PointF(100f, 0f),
              PointF(100f, 100f), PointF(0f, 100f)
          )
          // val result = BugBehavior.crawlPosition(contour, 0.125f)
          // scaled = 0.125 * 4 = 0.5 → i=0, t=0.5 → midpoint of (0,0)→(100,0) = (50,0)
          // TODO Plan 04-03: assertEquals(50f, result.x, 0.01f); assertEquals(0f, result.y, 0.01f)
      }

      @Test
      @Ignore("TODO Plan 04-03 Task 2 — un-Ignore when crawlPosition fallback lands")
      fun crawlPosition_handlesEmptyContourGracefully() {
          // Empty contour — tick should not throw; should return bbox-center fallback in drawFace,
          // but crawlPosition itself must not throw IOOBE.
          // TODO Plan 04-03: verify no exception on empty input
      }

      private fun sampleContour(): List<PointF> = listOf(
          PointF(100f, 100f), PointF(300f, 100f),
          PointF(300f, 400f), PointF(100f, 400f)
      )

      private fun buildFace(
          trackingId: Int = 1,
          boundingBox: Rect = Rect(100, 100, 300, 400),
          contours: Map<Int, List<PointF>> = emptyMap(),
      ): SmoothedFace = SmoothedFace(trackingId, boundingBox, contours, emptyMap())
  }
  ```

  **SwarmBehaviorTest.kt** — mirror the CrawlBehaviorTest structure; test D-09 swarm instance init + drift + respawn. Use Robolectric. Every @Test `@Ignore("TODO Plan 04-03 Task 3 — un-Ignore when SWARM impl lands")`. Tests reference `BehaviorState.Swarm`, `BugInstance`, `SwarmConfig(instanceCount=6, driftSpeedRange=0.3f..0.8f)` — which may not compile until Plan 04-03 lands these. If compile fails, inline a minimal stub at top of test file:

  ```kotlin
  // Compile-stub until Plan 04-03 replaces real type. DELETE when real type lands.
  // Placed outside of the @Ignore'd bodies so the sourceset compiles.
  // (If real type lands first, delete this stub; otherwise it shadows the real one — Plan 04-03
  //  must verify no stale stubs remain before un-Ignore.)
  ```

  Prefer inline import + @Ignore over stubs — stubs risk drift. Only use stubs if test file cannot compile otherwise.

  **FallBehaviorTest.kt** — test D-10 gravity + spawn + despawn. Structure:
  ```kotlin
  @Test
  @Ignore("TODO Plan 04-03 Task 4 — un-Ignore when FALL impl lands")
  fun fall_spawnsBugAtTopWhenIntervalElapsed() {
      val state = BehaviorState.Fall(instances = mutableListOf(), nextSpawnNanos = 0L)
      val config = FallConfig(spawnIntervalMsRange = 200..400, gravity = 0.5f, maxInstances = 8)
      // BugBehavior.fallTick(state, previewWidth=400f, previewHeight=800f, config, tsNanos=1_000_000L, dtMs=16L)
      // assertEquals(1, state.instances.size)
      // assertEquals(0f, state.instances[0].position.y, 0.01f)
      // assertTrue(state.instances[0].position.x in 0f..400f)
  }
  ```

  **BehaviorStateMapTest.kt** — test D-13 ConcurrentHashMap lifecycle. This test does NOT need Robolectric if it only tests the map lifecycle via a synthetic harness. However, if it goes through `FilterEngine.setFilter()` → perFaceState.clear(), Robolectric is needed for PointF et al. Use Robolectric for consistency with other render tests:
  ```kotlin
  @Test
  @Ignore("TODO Plan 04-04 Task 1 — un-Ignore when FilterEngine.perFaceState lands")
  fun setFilter_clearsPerFaceStateMap() {
      // val engine = FilterEngine(mockAssetLoader)
      // engine.setFilter(filterA) — populates perFaceState via onDraw
      // engine.setFilter(filterB) — should clear map
      // assertEquals(0, engine.perFaceState.size)
      // TODO Plan 04-04: expose perFaceState as internal or add sizeForTest()
  }
  ```

  All 4 new files must be self-contained (no new test helpers in src/main). `buildFace()` helper duplicated per file (Phase 3 pattern — 4-line function) OR extract shared helper to a new `TestFixtures.kt` under `app/src/test/java/com/bugzz/filter/camera/render/` — planner-authored decision: **duplicate** (3-6 lines of code; avoids a shared-test-code dependency chain across plans).

  The @Ignore string pattern is mandatory: `@Ignore("TODO Plan 04-0X Task Y — un-Ignore when <description>")`. Plan 04-03 removes these @Ignores when impl lands. The grep `grep "@Ignore.*TODO Plan 04-03" app/src/test/` will enumerate all Wave 0 work remaining.
  </action>
  <verify>
    <automated>cd "D:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="C:/Program Files/Android/Android Studio/jbr" ./gradlew :app:testDebugUnitTest --tests "*CrawlBehaviorTest*" --tests "*SwarmBehaviorTest*" --tests "*FallBehaviorTest*" --tests "*BehaviorStateMapTest*"</automated>
  </verify>
  <done>
    - 4 new test files exist, each ≥3 @Test methods
    - Every new @Test has `@Ignore("TODO Plan 04-03` or `TODO Plan 04-04` annotation
    - `./gradlew :app:testDebugUnitTest` exits 0 (existing 10 GREEN tests untouched; new @Ignore'd tests skipped)
    - `grep -c "@Ignore.*TODO Plan 04-0[34]" app/src/test/java/com/bugzz/filter/camera/render/*.kt` returns ≥10 (all new tests annotated)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land FilterCatalogExpandedTest + FilterPrefsRepositoryTest + 2 extensions (FilterEngineTest, CameraViewModelTest)</name>
  <files>app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogExpandedTest.kt, app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt, app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt, app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt</files>
  <read_first>
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CONTEXT.md D-02 (15-entry roster), D-25, D-26
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-RESEARCH.md §Pattern 5 (DataStore), §Pattern 10 (FilterSummary), §Validation Architecture
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md Per-Task table rows 03/04/07
    - app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogTest.kt (existing 2-entry test — keep this file! do not replace)
    - app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt (append — preserve all existing tests)
    - app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt (append — preserve all existing Phase 3 tests: onShutterTapped_success/failure/onCycleFilter — 03-REVIEW-FIX commits must not be reverted)
  </read_first>
  <behavior>
    - FilterCatalogExpandedTest: `catalog_has_15_entries` (FilterCatalog.all.size == 15); `allEntries_haveUniqueIds` (ids.toSet().size == 15); `allEntries_haveNonEmptyDisplayName`; `allEntries_haveValidAnchorType` (in enum); `allEntries_haveFrameCountGreaterThan0`; `allEntries_haveScaleFactorInZeroToOne` ((0f, 1f] exclusive-inclusive); `allEntries_assetDirInAllowedSet` (set contains exactly sprite_spider/sprite_bugA/sprite_bugB/sprite_bugC prefixed by "sprites/"); `allBehaviors_represented` (STATIC count ≥3, CRAWL ≥3, SWARM ≥3, FALL ≥3 per D-02 coverage)
    - FilterPrefsRepositoryTest: `writeThenRead_returnsSameId` (write "bugA_fall", read → "bugA_fall"); `read_beforeWrite_returnsDefault` (fresh store → DEFAULT_FILTER_ID); `corruptedRead_emitsEmptyPreferences_fallbackToDefault` (simulate IOException in flow — T-04-01); `writeAgain_overwrites_notAppends`
    - FilterEngineTest extension: `multiFace_primaryGetsContourAnchor_secondaryGetsBboxCenter` (inject List<SmoothedFace> size 2 with areas 40k/10k; verify primary uses FaceLandmarkMapper anchor, secondary uses bbox-center-at-40%-height); `softCap_halvesSwarmInstancesWhenExceeded` (config.instanceCount=30 on 2 faces → total draws = 60 > MAX=20 → drawn count ≤ 20); `onFaceLost_removesStateEntry`
    - CameraViewModelTest extension: `onSelectFilter_callsEngineAndWritesDataStore` (vm.onSelectFilter("bugC_fall") → verify filterEngine.setFilter AND filterPrefsRepository.setLastUsedFilter both called, uiState.selectedFilterId == "bugC_fall"); `initialBind_readsLastUsedFromDataStore` (DataStore returns "bugB_crawl" → bind() → uiState.selectedFilterId == "bugB_crawl" + filterEngine.setFilter called with bugB_crawl def); `rapidSelectFilter_noCameraRebind` (10 onSelectFilter calls in 100ms → controller.bind NEVER called after initial)
  </behavior>
  <action>
  Create 2 new test files + EXTEND 2 existing test files with new @Test methods (do NOT rewrite existing Phase 3 tests — those are GREEN and cover 03-REVIEW-FIX commits like `isCapturing` guard, `bindJob.cancel()`, `FilterLoadError` emission, flash order, `frameCount > 0` require).

  **FilterCatalogExpandedTest.kt** — pure JVM, no Android types:
  ```kotlin
  package com.bugzz.filter.camera.filter

  import com.bugzz.filter.camera.detector.FaceLandmarkMapper.Anchor
  import com.bugzz.filter.camera.render.BugBehavior
  import org.junit.Assert.assertEquals
  import org.junit.Assert.assertTrue
  import org.junit.Ignore
  import org.junit.Test

  /**
   * Nyquist Wave 0 tests for Phase 4 expanded [FilterCatalog] (CAT-01 / CAT-02 / D-02 amended roster).
   *
   * Plan 04-04 Task 2 replaces FilterCatalog's 2 entries with 15 and un-Ignores these tests.
   * The existing FilterCatalogTest.kt (Phase 3 2-entry tests) is SUPERSEDED — Plan 04-04 either
   * deletes that file or flips its @Test to @Ignore + migrates to this expanded test. Planner
   * discretion: recommend DELETE once Phase 4 catalog lands (Phase 3 ant_on_nose_v1 / spider_on_forehead_v1
   * no longer in FilterCatalog.all; their sprite dirs are retained for AssetLoaderTest fixtures only).
   */
  class FilterCatalogExpandedTest {

      @Test
      @Ignore("TODO Plan 04-04 Task 2 — un-Ignore when FilterCatalog expands to 15 entries")
      fun catalog_has_15_entries() {
          assertEquals("FilterCatalog must contain exactly 15 entries per D-02 amended roster",
              15, FilterCatalog.all.size)
      }

      @Test
      @Ignore("TODO Plan 04-04 Task 2")
      fun allEntries_haveUniqueIds() {
          val ids = FilterCatalog.all.map { it.id }
          assertEquals("All filter ids must be unique", ids.size, ids.toSet().size)
      }

      @Test
      @Ignore("TODO Plan 04-04 Task 2")
      fun allEntries_haveNonEmptyDisplayName() {
          FilterCatalog.all.forEach {
              assertTrue("Filter ${it.id} must have non-empty displayName", it.displayName.isNotBlank())
          }
      }

      @Test
      @Ignore("TODO Plan 04-04 Task 2")
      fun allEntries_haveFrameCountGreaterThan0() {
          FilterCatalog.all.forEach {
              assertTrue("Filter ${it.id} must have frameCount > 0", it.frameCount > 0)
          }
      }

      @Test
      @Ignore("TODO Plan 04-04 Task 2")
      fun allEntries_haveScaleFactorInExpectedRange() {
          FilterCatalog.all.forEach {
              assertTrue("Filter ${it.id} scaleFactor must be in (0,1]", it.scaleFactor > 0f && it.scaleFactor <= 1f)
          }
      }

      @Test
      @Ignore("TODO Plan 04-04 Task 2")
      fun allEntries_assetDirInAllowedSet() {
          val allowed = setOf(
              "sprites/sprite_spider",
              "sprites/sprite_bugA",
              "sprites/sprite_bugB",
              "sprites/sprite_bugC",
          )
          FilterCatalog.all.forEach {
              assertTrue("Filter ${it.id} assetDir '${it.assetDir}' must be in $allowed",
                  it.assetDir in allowed)
          }
      }

      @Test
      @Ignore("TODO Plan 04-04 Task 2")
      fun allFourBehaviors_representedWithAtLeast3Entries() {
          val byBehavior = FilterCatalog.all.groupingBy { it.behavior::class.java.simpleName }.eachCount()
          assertTrue("STATIC count must be ≥3 per D-02", (byBehavior["Static"] ?: 0) >= 3)
          assertTrue("CRAWL count must be ≥3 per D-02",  (byBehavior["Crawl"]  ?: 0) >= 3)
          assertTrue("SWARM count must be ≥3 per D-02",  (byBehavior["Swarm"]  ?: 0) >= 3)
          assertTrue("FALL count must be ≥3 per D-02",   (byBehavior["Fall"]   ?: 0) >= 3)
      }
  }
  ```

  **FilterPrefsRepositoryTest.kt** — new test file at `app/src/test/java/com/bugzz/filter/camera/data/`. Pure JVM + Turbine + InMemoryDataStore factory:
  ```kotlin
  package com.bugzz.filter.camera.data

  import android.content.Context
  import androidx.datastore.preferences.core.PreferenceDataStoreFactory
  import app.cash.turbine.test
  import kotlinx.coroutines.Dispatchers
  import kotlinx.coroutines.flow.first
  import kotlinx.coroutines.test.StandardTestDispatcher
  import kotlinx.coroutines.test.resetMain
  import kotlinx.coroutines.test.runTest
  import kotlinx.coroutines.test.setMain
  import org.junit.After
  import org.junit.Assert.assertEquals
  import org.junit.Before
  import org.junit.Ignore
  import org.junit.Rule
  import org.junit.Test
  import org.junit.rules.TemporaryFolder
  import org.mockito.kotlin.mock
  import java.io.File

  /**
   * Nyquist Wave 0 tests for [FilterPrefsRepository] (CAT-05 / T-04-01 / D-25).
   *
   * Plan 04-05 Task 1 lands FilterPrefsRepository + DataStore wiring; un-Ignores these tests.
   * Uses InMemoryDataStore via PreferenceDataStoreFactory — no Robolectric needed.
   */
  @OptIn(kotlinx.coroutines.ExperimentalCoroutinesApi::class)
  class FilterPrefsRepositoryTest {

      @get:Rule val tempFolder = TemporaryFolder()

      private val testDispatcher = StandardTestDispatcher()

      @Before fun setUp() { Dispatchers.setMain(testDispatcher) }
      @After  fun tearDown() { Dispatchers.resetMain() }

      @Test
      @Ignore("TODO Plan 04-05 Task 1 — un-Ignore when FilterPrefsRepository lands")
      fun writeThenRead_returnsSameId() = runTest(testDispatcher) {
          // val tempFile = File(tempFolder.root, "test.preferences_pb")
          // val repo = FilterPrefsRepository.forTest(tempFile)
          // repo.setLastUsedFilter("bugA_fall")
          // assertEquals("bugA_fall", repo.lastUsedFilterId.first())
          // TODO Plan 04-05: verify round-trip
      }

      @Test
      @Ignore("TODO Plan 04-05 Task 1")
      fun read_beforeWrite_returnsDefault() = runTest(testDispatcher) {
          // val repo = FilterPrefsRepository.forTest(File(tempFolder.root, "empty.preferences_pb"))
          // assertEquals(FilterPrefsRepository.DEFAULT_FILTER_ID, repo.lastUsedFilterId.first())
      }

      @Test
      @Ignore("TODO Plan 04-05 Task 1 — T-04-01 corruption fallback")
      fun corruptedDataStore_emitsDefault() = runTest(testDispatcher) {
          // Simulate IOException source in flow .catch{} — repo.lastUsedFilterId emits DEFAULT_FILTER_ID
          // TODO Plan 04-05: either real corrupted file OR inject mock DataStore with throwing flow
      }

      @Test
      @Ignore("TODO Plan 04-05 Task 1")
      fun writeAgain_overwritesOldValue() = runTest(testDispatcher) {
          // repo.setLastUsedFilter("first"); repo.setLastUsedFilter("second")
          // assertEquals("second", repo.lastUsedFilterId.first())
      }
  }
  ```

  Note: the `FilterPrefsRepository.forTest(file: File): FilterPrefsRepository` test-construction seam is a Claude's Discretion decision for Plan 04-05. Alternatively the test can construct its own DataStore and inject via @VisibleForTesting constructor. Plan 04-05 picks one pattern and documents in its SUMMARY.

  **FilterEngineTest.kt extension** — APPEND to existing file. Preserve lines 1-259 exactly; add new tests at end (before class closing brace):
  ```kotlin
  // NEW — Phase 4 Wave 0 tests (added by Plan 04-02 Task 2).
  @Test
  @Ignore("TODO Plan 04-04 Task 1 — un-Ignore when FilterEngine.onDraw takes List<SmoothedFace>")
  fun multiFace_primaryGetsContourAnchor_secondaryGetsBboxCenter() {
      // Build 2 SmoothedFace objects, area 200x200 vs 100x100 — primary is larger
      // val primary = buildFace(1, Rect(0, 0, 200, 200), mapOf(...contours...))
      // val secondary = buildFace(2, Rect(300, 0, 400, 100), emptyMap())
      // engine.setFilter(staticFilter)  // NOSE_TIP anchor
      // engine.onDraw(mockCanvas, mockFrame, listOf(primary, secondary))
      // Verify: drawBitmap called twice with different x,y — primary at mapper(NOSE_TIP from contours),
      //                                                      secondary at (bbox.left + w*0.5, bbox.top + h*0.4) = (350, 40)
  }

  @Test
  @Ignore("TODO Plan 04-04 Task 1 — un-Ignore when softCap MAX_DRAWS_PER_FRAME=20 enforced")
  fun softCap_halvesSwarmInstancesWhenExceeded() {
      // Swarm filter with instanceCount=30 on 2 faces → 60 total > 20 cap → drawBitmap called ≤20 times
      // TODO Plan 04-04: verify verify(mockCanvas, times(at most 20)).drawBitmap(any(), any(), any(), any())
  }

  @Test
  @Ignore("TODO Plan 04-04 Task 1 — un-Ignore when FilterEngine.onFaceLost + perFaceState exposed")
  fun onFaceLost_removesStateEntry() {
      // Populate 2 entries via onDraw, call onFaceLost(1), verify only id=2 remains
      // (requires internal-access or @VisibleForTesting helper to inspect perFaceState size)
  }
  ```

  **CameraViewModelTest.kt extension** — APPEND new test methods at end of class (preserve all existing Phase 3 tests, especially the `isCapturing`/`bindJob`/`FilterLoadError`/flash-order tests from 03-REVIEW-FIX commits):
  ```kotlin
  // NEW — Phase 4 Wave 0 tests (added by Plan 04-02 Task 2).
  // These tests reference FilterPrefsRepository (lands Plan 04-05 Task 1) + onSelectFilter method
  // (lands Plan 04-05 Task 3). All @Ignore'd until then.

  @Test
  @Ignore("TODO Plan 04-05 Task 3 — un-Ignore when vm.onSelectFilter lands")
  fun onSelectFilter_callsEngineAndWritesDataStore() = runTest(testDispatcher) {
      // val mockPrefsRepo: FilterPrefsRepository = mock()
      // val vm = CameraViewModel(mockController, mockFilterEngine, mockAssetLoader, testExecutor, mockPrefsRepo)
      // vm.onSelectFilter("bugC_fall")
      // advanceUntilIdle()
      // verify(mockFilterEngine).setFilter(argThat { id == "bugC_fall" })
      // verify(mockPrefsRepo).setLastUsedFilter("bugC_fall")
      // assertEquals("bugC_fall", vm.uiState.value.selectedFilterId)
  }

  @Test
  @Ignore("TODO Plan 04-05 Task 3 — un-Ignore when vm.bind reads DataStore")
  fun initialBind_readsLastUsedFromDataStore() = runTest(testDispatcher) {
      // mockPrefsRepo.lastUsedFilterId stub returns flowOf("bugB_crawl")
      // vm.bind(mockOwner); advanceUntilIdle()
      // verify(mockFilterEngine).setFilter(argThat { id == "bugB_crawl" })
      // assertEquals("bugB_crawl", vm.uiState.value.selectedFilterId)
  }

  @Test
  @Ignore("TODO Plan 04-05 Task 3 + 04-06 Task 2 — un-Ignore when rapid-tap flow stable")
  fun rapidSelectFilter_noCameraRebind() = runTest(testDispatcher) {
      // vm.bind(mockOwner); advanceUntilIdle()  — initial bind fires
      // Reset bind-count tracking
      // repeat(10) { vm.onSelectFilter("spider_nose_static") }  // rapid taps
      // advanceUntilIdle()
      // verify(mockController, never()).bind(any(), any(), any())  — rebind NEVER re-fires after initial
  }
  ```

  Also retain existing `onCycleFilter_togglesFilterEngineActive` test as-is — Plan 04-06 Task 1 removes the Cycle button from CameraScreen but the `onCycleFilter()` method is retained as `@Deprecated` per 04-UI-SPEC §Implementation Notes #3. Existing test stays green.
  </action>
  <verify>
    <automated>cd "D:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="C:/Program Files/Android/Android Studio/jbr" ./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>
    - 2 new test files exist: FilterCatalogExpandedTest.kt (7 @Test methods all @Ignore'd), FilterPrefsRepositoryTest.kt (4 @Test methods all @Ignore'd)
    - FilterEngineTest.kt extended with 3 new @Test methods (all @Ignore'd TODO Plan 04-04)
    - CameraViewModelTest.kt extended with 3 new @Test methods (all @Ignore'd TODO Plan 04-05)
    - `./gradlew :app:testDebugUnitTest` exits 0 — all 10+ Phase 3 tests + all new Wave 0 tests pass (Wave 0 pass by being @Ignore'd)
    - `grep -c "@Ignore.*TODO Plan 04-0[4-6]" app/src/test/java/com/bugzz/filter/camera/` returns ≥14 (all new + extension tests)
    - 03-REVIEW-FIX Phase 3 test assertions (isCapturing re-entrance, bindJob.cancel, FilterLoadError, flash deferred to success) ARE NOT modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unit-test sourceset → CI + local feedback loop | Tests drive contracts for production code waves. RED-before-GREEN Nyquist gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Resource integrity | DataStore file corruption → default filter | mitigate | `FilterPrefsRepositoryTest.corruptedDataStore_emitsDefault` pins the `.catch { emit(emptyPreferences()) }` fallback contract before impl lands. |
| T-04-02 | Tampering / Input validation | Manifest.json BehaviorConfig parse | mitigate | `FilterCatalogExpandedTest.allEntries_assetDirInAllowedSet` pins the 4-sprite-group constraint — prevents accidental catalog entries pointing to non-existent dirs. |
| T-04-05 | Race condition | Rapid filter-swap | mitigate | `rapidSelectFilter_noCameraRebind` pins the no-rebind invariant before Plan 04-05/04-06 wire the flow. |
</threat_model>

<verification>
- Every REN-02 behavior variant (CRAWL/SWARM/FALL) has at least one RED @Test
- CAT-01, CAT-02, CAT-05 each have at least one RED @Test
- MOD-02 multi-face scenario has at least one RED @Test in FilterEngineTest
- T-04-01 DataStore corruption fallback is pinned in a @Test
- T-04-05 rapid-swap no-rebind invariant is pinned in a @Test
- `./gradlew :app:testDebugUnitTest` exits 0 with all new tests @Ignore'd (not RED-failing) — Wave 1+ un-Ignores them as SUT lands
</verification>

<success_criteria>
- 6 new test files land + 2 existing files extended (total 8 changed test files)
- All new @Test methods annotated `@Ignore("TODO Plan 04-0X Task Y — ...")` with correct plan pointer
- Phase 3 green-test count unchanged (10 → 10)
- `:app:testDebugUnitTest` exits 0
- No stale `BugState` imports in test files that Plan 04-03 would break — prefer `BehaviorState` in new test bodies with @Ignore guard
</success_criteria>

<output>
After completion, create `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-02-SUMMARY.md`. Document:
1. Any test that had to use a compile-stub instead of @Ignore + mark as a Plan 04-03/04/05 forward-dependency
2. Count of new @Ignore'd tests per target Plan (04-03: N, 04-04: M, 04-05: K)
3. Whether any existing Phase 3 tests were touched (should be ZERO modifications; ONLY appends to two files)
</output>
