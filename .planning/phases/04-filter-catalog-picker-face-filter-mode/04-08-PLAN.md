---
phase: 04-filter-catalog-picker-face-filter-mode
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md
  - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md
  - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md
autonomous: false
requirements: []
threat_refs: [T-04-01, T-04-02, T-04-03, T-04-04, T-04-05, T-04-06]

must_haves:
  truths:
    - "Clean debug APK builds from scratch: `./gradlew :app:clean :app:assembleDebug :app:testDebugUnitTest` exits 0"
    - "04-HANDOFF.md Xiaomi 13T runbook covers all 5 ROADMAP Phase 4 success criteria + per-behavior visual checks"
    - "04-VALIDATION.md `nyquist_compliant` frontmatter flipped false → true"
    - "User device sign-off checkpoint pauses execution until user confirms pass"
  artifacts:
    - path: ".planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md"
      provides: "Step-by-step Xiaomi 13T device runbook — catalog scroll + picker rapid-tap + multi-face + DataStore persist across restart + Home nav flow + per-behavior visuals (STATIC/CRAWL/SWARM/FALL)"
      min_lines: 200
    - path: ".planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md"
      provides: "Device-verification sign-off record from user — pass/fail per step + any gap_closure plan pointers"
      min_lines: 20
  key_links:
    - from: "04-HANDOFF.md step outcomes"
      to: "04-VERIFICATION.md (post-checkpoint next plan, if any) OR Phase 5 entry"
      via: "Either full PASS → phase complete, or gap-closure plan(s) 04-gaps-0N"
      pattern: "ROADMAP Phase 4 success criteria"
---

<objective>
Wave 4 phase close — produce a clean debug APK from a scratch build, author the Xiaomi 13T device runbook (04-HANDOFF.md) exercising all 5 ROADMAP Phase 4 success criteria, then pause for user device sign-off. After pass, flip 04-VALIDATION.md `nyquist_compliant: true`.

This plan is **autonomous: false** — Task 3 is a `checkpoint:human-verify` requiring the user to run the APK on Xiaomi 13T per 04-HANDOFF.md. Per user memory (`Phase cadence`) — stop-test per phase is mandatory.

Purpose: Feature-complete Phase 4 work needs physical-device validation of the five observable truths (15 filters visible + tap-swap + rapid-tap + multi-face + DataStore persist). No substitutions for device verification — synthetic multi-face unit tests prove data-flow but not ML Kit real-world behavior.

Output: Fresh debug APK; 04-HANDOFF.md runbook; 04-CHECKPOINT.md sign-off record; VALIDATION nyquist flip; git commits for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-CONTEXT.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-RESEARCH.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-UI-SPEC.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-01-SUMMARY.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-03-SUMMARY.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-04-SUMMARY.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-05-SUMMARY.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-06-SUMMARY.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-07-SUMMARY.md
@.planning/phases/03-first-filter-end-to-end-photo-capture/03-HANDOFF.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Phase 4 ROADMAP success criteria (authoritative — 04-HANDOFF.md Steps map 1:1 to these): -->
1. FilterCatalog bundles 15-25 filters spanning STATIC/CRAWL/SWARM/FALL; each has id, displayName, thumbnail, sprite atlas, behavior, landmark anchor
2. All four behaviors render correctly on-face at ≥24fps subjective
3. Horizontal LazyRow picker renders thumbnails, highlights selected, scrolls smoothly, survives mid-session rapid-tap (10 swaps in 5s) without visible stutter or CameraX rebind
4. Home screen "Face Filter" launches camera with last-used filter restored from DataStore; "Insect Filter" is present (non-functional until Phase 5)
5. Multi-face scene (2 faces in frame) does not crash; primary face receives full filter; contour-dependent behaviors fall back gracefully to boundingBox center on secondary faces

<!-- Xiaomi 13T specifics (from STATE.md + 03-HANDOFF.md environment): -->
- Device: Xiaomi 13T (HyperOS / MIUI)
- APK path: `app/build/outputs/apk/debug/app-debug.apk`
- ADB: `C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe`
- JBR: `C:/Program Files/Android/Android Studio/jbr`
- Google Photos preinstalled
- USB debugging + Install via USB + USB debugging (Security) all must be ON
- No battery-saver / thermal-throttle mode

<!-- 03-HANDOFF.md runbook format to mirror (headings + `Prerequisites` + `Hard vs Soft gates` + `Known expected findings` + Steps with `Expected:` + `If you see:` failure branches + OEM Quirks section at end). -->

<!-- Runbook step list — 13 total, covering all 5 criteria + per-behavior visual checks + regression check (Phase 3 photo capture still works): -->

Step 1 — Install APK + verify launch (nav: Splash → Home)
Step 2 — Home screen UI check (04-UI-SPEC §1 compliance; Face Filter filled; Insect Filter disabled; settings gear; My Collection)
Step 3 — Settings gear + Insect Filter disabled visual verification (Toast on settings tap; Insect Filter button looks disabled; tapping does nothing)
Step 4 — Face Filter nav → CameraScreen opens with picker strip visible + restored filter selected (first launch → default "spider_nose_static"; subsequent → last-used)
Step 5 — Picker LazyRow scroll smoothness (swipe left/right; 15 thumbnails visible; no jank)
Step 6 — STATIC behavior visual verification (select `spider_nose_static` → spider on nose; move head → bug follows)
Step 7 — CRAWL behavior visual verification (select `bugB_crawl` OR `spider_jawline_crawl` → bug traverses jawline continuously; reasonable speed)
Step 8 — SWARM behavior visual verification (select `spider_swarm` → 6 spiders drift toward nose; respawn at edge when close)
Step 9 — FALL behavior visual verification (select `bugC_fall` → bugs rain from top, fall under gravity, despawn at bottom; max ~8 simultaneous)
Step 10 — Rapid-tap stress test (tap 10 different filters in <5 seconds; no freeze, no black flash, no "Camera in use" error, each swap takes effect within ~1 preview frame)
Step 11 — Multi-face test (hold up printed photo of a face OR ask a friend to be in frame → primary face full anchor, secondary face bbox-center fallback, no crash, fps remains smooth)
Step 12 — DataStore persist test (select `bugC_fall` → exit app via back or Home → relaunch → picker auto-scrolls to bugC_fall + filter renders immediately)
Step 13 — Phase 3 regression spot-check (tap shutter during filter active → JPEG saved to Google Photos with bug baked in; no CameraX rebind during any of the above)

<!-- Hard vs soft gates: -->
Hard gates (blocking — must PASS to close Phase 4):
- Step 1, 4 (nav), 6-9 (all 4 behaviors), 10 (rapid-tap no rebind), 12 (DataStore persist)

Soft gates (document — not blocking):
- Step 11 (multi-face — ML Kit behavior varies by device/scene; acceptable if second face doesn't render bug cleanly on a printed photo, as long as no crash)
- Step 5 (scroll smoothness — acceptable on Xiaomi 13T; degraded on older devices is Phase 7 territory)
- Step 13 (Phase 3 regression — should be trivially PASS since Phase 4 didn't touch capture code)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Clean debug build + unit-test sweep</name>
  <files></files>
  <read_first>
    - app/build.gradle.kts (ensure no debug-only ⅶ Debug-only work broken during Phase 4 edits)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-01-SUMMARY.md..04-07-SUMMARY.md (any deviations / Rule 3 auto-fixes to account for)
  </read_first>
  <action>
  Run a scratch-clean build + full unit test sweep + lint to confirm Phase 4 is ship-clean before handing to user device test:

  ```bash
  cd "D:/ClaudeProject/appmobile/Bugzz"
  JAVA_HOME="C:/Program Files/Android/Android Studio/jbr" \
    ./gradlew :app:clean :app:assembleDebug :app:testDebugUnitTest :app:lintDebug
  ```

  Expected outcomes:
  - `:app:clean` — removes `app/build/`
  - `:app:assembleDebug` — produces `app/build/outputs/apk/debug/app-debug.apk` (~80 MB, similar to Phase 3 79.1 MB; may bloat ~2-4 MB from added sprites). Record APK size in 04-08-SUMMARY.
  - `:app:testDebugUnitTest` — all Phase 3 + Phase 4 GREEN tests pass. Approximate test count after Phase 4: 10 Phase 3 + 13 new Phase 4 Wave 0 → ~23 tests GREEN. Execute in ~60s per 04-VALIDATION.
  - `:app:lintDebug` — emits `app/build/reports/lint-results-debug.html`. Acceptable: zero new **Errors** introduced by Phase 4; warnings OK. If any Error categories introduced, triage before handoff (likely Unused import or Accessibility — auto-fix + re-run).

  If the build fails:
  - KSP / Hilt codegen error → check FilterPrefsRepository's `@Inject`/`internal constructor` split pattern (04-05 Task 1)
  - Coil unresolved → check libs.coil.compose in app/build.gradle.kts + libs.versions.toml catalog entry
  - DataStore unresolved → same — libs.androidx.datastore.preferences must be wired
  - navigation-compose `toRoute` unresolved → import alternative `androidx.navigation.compose.toRoute` if `androidx.navigation.toRoute` fails
  - Icons.Default.Settings unresolved → material-icons-core transitive chain; confirm via `./gradlew :app:dependencies | grep material-icons`

  Any Rule 3 auto-fix applied must be documented in 04-08-SUMMARY "Deviations" section.

  Confirm binary present:
  ```bash
  ls -la app/build/outputs/apk/debug/app-debug.apk
  ```

  Commit message template (run only if user hasn't committed Phase 4 along the way):
  ```
  build(04-08-01): Phase 4 clean debug build — 23+ unit tests GREEN, APK {size} MB

  :app:clean :app:assembleDebug :app:testDebugUnitTest :app:lintDebug all exit 0.
  Ready for Xiaomi 13T device verification per 04-HANDOFF.md.
  ```
  </action>
  <verify>
    <automated>cd "D:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="C:/Program Files/Android/Android Studio/jbr" ./gradlew :app:clean :app:assembleDebug :app:testDebugUnitTest :app:lintDebug &amp;&amp; test -f app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <done>
    - `app/build/outputs/apk/debug/app-debug.apk` exists after `:app:clean + :app:assembleDebug`
    - `:app:testDebugUnitTest` exits 0 — full suite passes
    - `:app:lintDebug` exits 0 — no new lint errors
    - APK size documented (for Phase 7 PRF-04 tracking)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 04-HANDOFF.md Xiaomi 13T runbook</name>
  <files>.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md</files>
  <read_first>
    - .planning/phases/03-first-filter-end-to-end-photo-capture/03-HANDOFF.md (full — mirror structure and tone)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CONTEXT.md §Specifics (Phase 4 acceptance gate 4 items)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md §Manual-Only Verifications (7 device checks)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-UI-SPEC.md §Component Specs §1 + §2 (visual ground-truth for runbook checks)
    - .planning/ROADMAP.md §Phase 4 Success Criteria (5 items)
  </read_first>
  <action>
  Create `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md` with the following structure. Use 03-HANDOFF.md as the template (same headings + same tone). Substitute Phase 4 specifics.

  Required sections:

  ```markdown
  # Phase 4 Handoff — Filter Catalog + Picker + Face Filter Mode Device Runbook

  **Device:** Xiaomi 13T (HyperOS / MIUI)
  **APK:** `app/build/outputs/apk/debug/app-debug.apk` (Plan 04-08 Task 1 produced)
  **Reference APK:** `reference/com.insect.filters.funny.prank.bug.filter.face.camera.apk`
  **Reference APK SHA-256:** 616c6990331ea59f36f135221f15aff2cdb5c290dfde0cd6c91f898fa26e7859 (unchanged from Phase 3)

  ---

  ## Prerequisites (user-side)

  [Mirror 03-HANDOFF.md Prerequisites 1-7 verbatim.]

  ---

  ## What you are verifying

  | Criterion | Description | Hard gate? |
  |-----------|-------------|------------|
  | **CAT-01** | FilterCatalog bundles 15 filters covering STATIC/CRAWL/SWARM/FALL | Yes (unit-test verified; device sanity spot-check in Step 5) |
  | **CAT-03** | LazyRow picker renders + scrolls smoothly; rapid-tap 10 swaps in 5s without rebind | Yes (Step 10) |
  | **CAT-04** | Tap filter → swap < 1 frame visible; no freeze | Yes (Step 10) |
  | **CAT-05** | Last-used filter persists across app restart | Yes (Step 12) |
  | **MOD-01** | Home has Face Filter + Insect Filter buttons + settings gear + collection | Yes (Steps 2-4) |
  | **MOD-02 #5** | Multi-face scene (2 faces) — no crash, primary gets full anchor, secondary bbox-center | Soft (Step 11) |
  | **REN-02 (4 behaviors)** | STATIC + CRAWL + SWARM + FALL all render correctly | Yes (Steps 6-9) |
  | **Regression** | Phase 3 shutter + JPEG capture still works with Phase 4 pipeline | Yes (Step 13) |

  Hard gates: failure blocks Phase 4 sign-off — create `04-gaps-0N-PLAN.md` before advancing to Phase 5.
  Soft gates: document finding; Phase 4 may still close if user agrees to a follow-up plan.

  ---

  ## Known expected findings (NOT bugs — flag only if different)

  - Black letterbox bars on top/bottom of camera preview — expected (Phase 2 D-10 FIT_CENTER).
  - Debug bounding-box overlay visible behind the bug sprite — expected (Phase 2 DebugOverlayRenderer retained D-27; BuildConfig.DEBUG only).
  - Insect Filter button visually disabled — expected (Phase 4 D-19).
  - Settings gear shows Toast only — expected (Phase 6 wires real settings).
  - My Collection opens a stub — expected (Phase 6 wires real collection).
  - Secondary face in multi-face scene shows no contour dots — Phase 2 PITFALLS #13 limitation; secondary face gets bbox-center bug only per Phase 4 D-22.
  - MIUI/HyperOS OEM quirks — document any unexpected behavior under §OEM Quirks at the end; do NOT attempt to fix; Phase 7 handles cross-OEM matrix.

  ---

  ## Step 1 — Install and launch

  ```bash
  "C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" devices
  "C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" install -r app/build/outputs/apk/debug/app-debug.apk
  "C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" shell am start -n com.bugzz.filter.camera/.MainActivity
  ```

  Expected: Splash shows → taps "Continue" → Home appears.

  If you see: crash on launch → check `adb logcat | grep -E "FATAL|AndroidRuntime"` for a stack trace and paste into the 04-CHECKPOINT.md findings. Common culprits: Hilt binding-missing error for FilterPrefsRepository (regression from 04-05), serialization error on CameraRoute (regression from 04-07).

  ## Step 2 — Home screen UI check (MOD-01)

  Expected layout per 04-UI-SPEC §Component Specs §1:
  - [ ] Settings gear icon top-right (48dp tap target)
  - [ ] "Face Filter" button in middle, 200×80dp, filled (Material3 primary fill — looks enabled)
  - [ ] "Insect Filter" button below Face Filter, 200×80dp, outlined + visibly disabled (greyed text + faint border)
  - [ ] "My Collection" button near bottom, 160×56dp, outlined
  - [ ] Portrait-locked — rotation does not change layout

  If you see: any button missing or wrong size → document in 04-CHECKPOINT.md § Step 2 findings.

  ## Step 3 — Settings gear + Insect Filter verification (MOD-01 soft)

  - Tap the settings gear — expect Toast "Settings coming soon"
  - Tap "Insect Filter" button — expect NO response (button is disabled, Material3 ripple absent or muted)

  ## Step 4 — Face Filter nav + picker strip + restored filter (MOD-01 hard, CAT-05)

  Tap "Face Filter" from Home.

  Expected:
  - [ ] CameraScreen opens
  - [ ] Picker strip visible at bottom center (100dp tall, dark overlay, 15 thumbnails)
  - [ ] On FIRST launch: picker centered on "Spider Nose" filter (DEFAULT_FILTER_ID `spider_nose_static`)
  - [ ] Bug sprite visible on user's nose
  - [ ] 72dp white shutter circle at BottomCenter
  - [ ] Flip camera "Flip" text button at TopEnd
  - [ ] DEBUG build only: "TEST 5s" button at BottomStart
  - [ ] No "Cycle" button (removed in Plan 04-06)

  ## Step 5 — Picker scroll smoothness (CAT-03 hard-ish)

  Swipe the picker strip left and right multiple times. Expected:
  - [ ] 15 thumbnails total (count by scrolling fully left-to-right)
  - [ ] Each thumbnail shows bug sprite image (Coil loads)
  - [ ] Thumbnail label visible underneath (e.g. "Spider Nose", "Bug A Rain")
  - [ ] Scroll is fluid — no visible stutter, no tearing, no missing images
  - [ ] Currently-selected thumbnail has white border and slightly larger size

  ## Step 6 — STATIC behavior (REN-02)

  Tap "Spider Nose" filter in the picker.

  Expected:
  - [ ] Spider sprite snaps to nose tip within 1 frame
  - [ ] Move head left/right/up/down → sprite follows nose
  - [ ] Sprite animation (flipbook) plays smoothly

  ## Step 7 — CRAWL behavior (REN-02)

  Tap "Bug B Crawl" OR "Spider Crawl" filter in the picker.

  Expected:
  - [ ] Bug traverses along the face contour (jawline/edge)
  - [ ] Motion is continuous — bug loops around the face perimeter
  - [ ] Direction consistent (CW or CCW per filter config)
  - [ ] Speed reasonable — visible motion, not frozen/too-fast
  - [ ] No teleporting or glitching

  ## Step 8 — SWARM behavior (REN-02)

  Tap "Spider Swarm" filter.

  Expected:
  - [ ] 6 spiders visible, drifting toward nose
  - [ ] When spider gets close to nose, it respawns at edge of face bbox
  - [ ] Continuous motion — swarm stays active indefinitely
  - [ ] No single spider stays stationary

  ## Step 9 — FALL behavior (REN-02)

  Tap "Bug C Rain" filter.

  Expected:
  - [ ] Bugs spawn at top of screen randomly
  - [ ] Each bug falls at constant speed under gravity
  - [ ] Bug disappears at the bottom of the preview
  - [ ] New bugs spawn every 200-400ms
  - [ ] Max ~8 simultaneous bugs visible

  ## Step 10 — Rapid-tap stress test (CAT-03 hard, CAT-04 hard)

  Within 5 seconds, tap 10 DIFFERENT filters as fast as you can.

  Expected:
  - [ ] Each tap highlights the tapped thumbnail immediately (optimistic UI)
  - [ ] Preview swaps to the new filter within ~1 frame (~33ms) of tap
  - [ ] No preview freeze during any tap
  - [ ] No black flash (would indicate CameraX rebind)
  - [ ] No "Camera in use" error or crash
  - [ ] After the 10 taps, the last-tapped filter is the one rendering

  If you see: freeze or rebind → capture `adb logcat | grep -E "CameraX|CameraController"` for the last 30 seconds.

  ## Step 11 — Multi-face test (MOD-02 soft)

  Option A: Find a friend; both hold faces in front of front camera simultaneously.
  Option B: Print a face photo at ~2x2cm; hold it alongside your own face.

  Expected:
  - [ ] No crash when 2 faces are detected
  - [ ] Primary face (larger bbox) gets full anchor-resolved bug
  - [ ] Secondary face gets bug at bbox center (mid-forehead position)
  - [ ] FPS remains smooth (subjective)
  - [ ] Soft cap 20 in effect: for SWARM/FALL on 2 faces, bug count stays reasonable

  This is a SOFT gate — ML Kit behavior on printed photos varies; a "no crash" + "primary face gets bug" is sufficient even if secondary is occasional.

  ## Step 12 — DataStore persistence (CAT-05 hard)

  1. Select "Bug C Rain" (`bugC_fall`) in the picker.
  2. Observe the filter rendering — bugs falling from top.
  3. Exit the app: press Home button.
  4. Force-stop the app: long-press app icon → App info → Force stop. (Or from Recent apps, swipe the app away.)
  5. Re-launch the app.
  6. Navigate to Face Filter.

  Expected:
  - [ ] On re-launch, picker auto-scrolls to "Bug C Rain" being selected (white border + larger)
  - [ ] Camera immediately shows FALL bugs without user intervention
  - [ ] No freeze or delay on resumed-filter load

  If you see: default filter selected (Spider Nose) instead of restored one → DataStore write/read regression; check `adb shell run-as com.bugzz.filter.camera ls files/datastore/` for `bugzz_prefs.preferences_pb`.

  ## Step 13 — Phase 3 regression check

  With any filter active:
  1. Tap the 72dp shutter button.
  2. Observe brief capture flash.
  3. Open Google Photos.

  Expected:
  - [ ] Capture flash visible (white fade)
  - [ ] Toast "Saved to gallery"
  - [ ] JPEG appears in Google Photos within 1 second
  - [ ] Bug sprite baked into the JPEG
  - [ ] No rebind (camera continues live after capture)

  ---

  ## OEM Quirks (Xiaomi 13T / HyperOS / MIUI)

  [Leave empty initially; user fills during run if they observe something unexpected.]

  - 
  - 

  ---

  ## Final sign-off

  All hard gates PASS: close Phase 4 via 04-CHECKPOINT.md sign-off, then proceed to Plan 04-08 Task 4 (VALIDATION flip).

  Any hard gate FAIL: do NOT close Phase 4. File `04-gaps-0N-PLAN.md` per Phase 2 gap-closure precedent; describe the failing step + probable root cause. Re-run 04-HANDOFF.md after gap-closure plans land.

  Soft gates partial fail: document in 04-CHECKPOINT.md, agree with user on Phase 5 follow-up or defer to Phase 6/7.
  ```

  After writing 04-HANDOFF.md, print it to the user with:
  ```bash
  cat .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md
  ```
  So they can step through it.
  </action>
  <verify>
    <automated>test -f ".planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md" &amp;&amp; grep -q "## Step 1" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md &amp;&amp; grep -q "## Step 13" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md &amp;&amp; grep -q "DataStore" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md &amp;&amp; grep -q "rapid-tap" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md &amp;&amp; grep -q "multi-face\|Multi-face" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md &amp;&amp; wc -l .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md | awk '{ if ($1 >= 200) print "OK"; else exit 1 }'</automated>
  </verify>
  <done>
    - `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md` ≥200 lines
    - Contains Steps 1-13 covering all 5 ROADMAP success criteria + 4 behavior visuals + regression spot-check
    - Hard/soft gate designation explicit
    - Uses Xiaomi 13T specifics (JBR path, adb path, reference APK hash)
    - OEM Quirks section left blank for user
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User performs 04-HANDOFF.md on Xiaomi 13T device; records sign-off</name>
  <files>.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md, .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md</files>
  <action>
    Human-verify checkpoint. Claude MUST pause here and not proceed to Task 4 until the user reports the runbook result. Claude should:
    1. Output the full absolute path to 04-HANDOFF.md in chat.
    2. Summarize the 13 runbook steps (1 sentence each) in chat.
    3. Wait for user PASS/FAIL reply per the resume-signal.
    4. If all hard gates PASS (and soft-gate failures are acceptable): proceed to Task 4.
    5. If any hard gate FAILS: STOP; do NOT run Task 4; announce that /gsd-plan-phase 04 --gaps is needed to create 04-gaps-0N-PLAN.md.
  </action>
  <verify>Runbook PASS/FAIL reported by user in chat; hard-gate failures escalate to gap-closure plan.</verify>
  <done>User replied "Phase 4 closed — proceed" with all hard gates PASS, OR user replied "Phase 4 blocked — step N failed" and gap-closure plan path is taken.</done>
  <what-built>
    All Phase 4 Plans 04-01 through 04-07 merged. Unit test suite GREEN (~23 tests). Clean debug APK at `app/build/outputs/apk/debug/app-debug.apk` ready to install. Runbook at `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md` covers all 5 Phase 4 ROADMAP success criteria + 4 behavior visuals + regression.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HANDOFF.md`.
    2. Execute Steps 1-13 on Xiaomi 13T per user memory `Phase cadence` (stop-test per phase).
    3. For each step, check the expected bullets. Document any failure or unexpected behavior.
    4. After finishing all 13 steps, create `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md` with this structure:

    ```markdown
    # Phase 4 Device Checkpoint — Xiaomi 13T sign-off

    **Date:** <YYYY-MM-DD>
    **Device:** Xiaomi 13T (HyperOS build: ___)
    **APK SHA:** <shasum of app-debug.apk>

    ## Results

    | Step | Criterion | Outcome | Notes |
    |------|-----------|---------|-------|
    | 1 | Install + launch | PASS / FAIL | |
    | 2 | Home UI | PASS / FAIL | |
    | 3 | Settings + Insect disabled | PASS / FAIL | |
    | 4 | Camera nav + picker visible | PASS / FAIL | |
    | 5 | Picker scroll smoothness | PASS / FAIL | |
    | 6 | STATIC | PASS / FAIL | |
    | 7 | CRAWL | PASS / FAIL | |
    | 8 | SWARM | PASS / FAIL | |
    | 9 | FALL | PASS / FAIL | |
    | 10 | Rapid-tap 10×5s | PASS / FAIL | |
    | 11 | Multi-face | PASS / FAIL / SOFT | |
    | 12 | DataStore persist | PASS / FAIL | |
    | 13 | Phase 3 regression | PASS / FAIL | |

    ## Hard gate summary

    All hard gates PASS: <Yes/No>

    If No — list failing hard gates and proposed gap-closure plan(s):
    - 

    ## Soft gate notes

    - 

    ## OEM Quirks observed

    - 

    ## Decision

    - [ ] Phase 4 CLOSED — proceed to Plan 04-08 Task 4 (VALIDATION flip)
    - [ ] Phase 4 BLOCKED — file gap-closure plans before advancing
    ```

    5. Run all 13 steps patiently. Do NOT short-circuit — user memory emphasizes stop-test per phase on the physical device.

    6. If any HARD gate fails: do NOT invoke Task 4. Escalate to Phase 4 gap-closure: "Phase 4 blocked — I need to file `04-gaps-0N-PLAN.md` for step N". Wait for user approval of the gap plan before proceeding.

    7. If all HARD gates PASS + user signs off: proceed to Task 4.
  </how-to-verify>
  <resume-signal>
    Reply "Phase 4 closed — proceed" to continue to Task 4 (VALIDATION flip).
    Reply "Phase 4 blocked — step N failed" to pause and file gap-closure plans.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Flip 04-VALIDATION nyquist_compliant → true; update STATE + ROADMAP</name>
  <files>.planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md (current `nyquist_compliant: false` in frontmatter)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md (user's Task 3 sign-off)
    - .planning/STATE.md (Phase 4 status + Phase Map update)
    - .planning/ROADMAP.md (Phase 4 row + Progress table)
    - .planning/REQUIREMENTS.md (Phase 4 req IDs to mark complete: CAT-01..05, MOD-01, MOD-02)
  </read_first>
  <action>
  **Precondition:** Task 3 resume-signal = "Phase 4 closed — proceed". If blocked, Task 4 does NOT run.

  **Step 1 — Flip `nyquist_compliant` in 04-VALIDATION.md frontmatter**:

  ```yaml
  # Before
  nyquist_compliant: false
  # After
  nyquist_compliant: true
  wave_0_complete: true
  ```

  Also update the sign-off section at the bottom:
  ```markdown
  ## Validation Sign-Off

  - [x] All tasks have `<automated>` verify or Wave 0 dependencies
  - [x] Sampling continuity: no 3 consecutive tasks without automated verify
  - [x] Wave 0 covers all MISSING test references (7 new + 2 extensions)
  - [x] `io.coil-kt:coil-compose:2.7.0` and `androidx.datastore:datastore-preferences:1.1.3` added to catalog
  - [x] Turbine 1.2.0 added to testImplementation
  - [x] No watch-mode flags
  - [x] Feedback latency < 90s (measured: ~60s on Plan 04-08 Task 1 run)
  - [x] `nyquist_compliant: true` — flipped {YYYY-MM-DD} post Xiaomi 13T device sign-off

  **Approval:** APPROVED {YYYY-MM-DD}
  ```

  **Step 2 — Update STATE.md**:
  - Change "Current Focus" → "Phase 5 — Video Recording + Audio + Insect Filter Mode"
  - Update "Phase:" field → 5
  - Update "Previous plan:" with the one-sentence Phase 4 outcome (e.g. "04-08 complete — Phase 4 closed on Xiaomi 13T; 15 filters visible, all 4 behaviors render, DataStore persist verified, multi-face graceful.")
  - Update "Phase Map" — mark Phase 4 `[ complete ]`
  - Append to "Accumulated Context → Key Decisions During Execution" any Phase 4 lessons learned (e.g. the FilterPrefsRepository constructor-split pattern, Coil AsyncImage asset URI confirmed, any Rule 3 auto-fixes discovered)

  **Step 3 — Update ROADMAP.md**:
  - Mark Phase 4 row `[x]` in the Phases list
  - Update Progress table Phase 4 row: "Plans Complete: 8/8 | Status: Complete | Completed: YYYY-MM-DD"
  - Use `gsd-tools.cjs` if available:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap mark-phase-complete 04 2>/dev/null || true
    ```

  **Step 4 — Update REQUIREMENTS.md traceability table**:
  - Mark CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, MOD-01, MOD-02 as `Complete` in the Traceability table (bottom of file).
  - Use `gsd-tools.cjs` if available:
    ```bash
    for req in CAT-01 CAT-02 CAT-03 CAT-04 CAT-05 MOD-01 MOD-02; do
      node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete "$req" 2>/dev/null || true
    done
    ```
  - Also mark the per-requirement checkboxes in the §Filter Catalog / §Dual Mode sections of REQUIREMENTS.md by hand if gsd-tools command doesn't exist.

  **Step 5 — Final commit**:

  ```bash
  cd "D:/ClaudeProject/appmobile/Bugzz"
  git add .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md \
         .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md \
         .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
  git commit -m "$(cat <<'EOF'
  docs(04-08): Phase 4 complete — Xiaomi 13T device sign-off, nyquist_compliant flip

  All 5 ROADMAP Phase 4 success criteria verified on Xiaomi 13T per 04-HANDOFF.md:
  - 15 filters visible in picker, all 4 behaviors (STATIC/CRAWL/SWARM/FALL) render
  - Picker rapid-tap (10×5s) no rebind
  - DataStore last-used-filter persistence across app restart confirmed
  - Multi-face scene graceful (primary anchor + secondary bbox-center fallback)
  - Home screen MOD-01 layout matches 04-UI-SPEC §1

  Requirements complete: CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, MOD-01, MOD-02
  VALIDATION nyquist_compliant: true
  STATE advanced to Phase 5
  EOF
  )"
  ```

  **Step 6 — Post to the user**:
  Print Phase 4 completion summary:
  - 7 requirements closed
  - 23+ unit tests GREEN
  - Clean debug APK ~80-83 MB (report actual)
  - Next: `/gsd-research-phase 5` or `/gsd-discuss-phase 5`
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-VALIDATION.md &amp;&amp; grep -q "Phase 4" .planning/phases/04-filter-catalog-picker-face-filter-mode/04-CHECKPOINT.md &amp;&amp; (grep -q "Phase 4.*Complete\|\[x\].*Phase 4\|4\..*8/8" .planning/ROADMAP.md || true)</automated>
  </verify>
  <done>
    - 04-VALIDATION.md `nyquist_compliant: true` + sign-off Approved
    - 04-CHECKPOINT.md records Xiaomi 13T sign-off with per-step PASS/FAIL
    - STATE.md advanced to Phase 5
    - ROADMAP.md Phase 4 row marked complete
    - REQUIREMENTS.md CAT-01..05 + MOD-01 + MOD-02 all marked complete
    - Final commit lands on master
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → APK → physical device | Entire Phase 4 feature surface exercised end-to-end on hardware. |

## STRIDE Threat Register

All Phase 4 threats (T-04-01 to T-04-06) are exercised end-to-end during Task 3 device runbook:

| Threat ID | Category | Exercised in Step | Expected outcome |
|-----------|----------|-------------------|------------------|
| T-04-01 | Tampering / DataStore corruption | Step 12 | Persist round-trip works; corruption fallback tested in unit test only |
| T-04-02 | Tampering / manifest input validation | Steps 5-9 | 4 sprite groups loaded correctly; AsyncImage error-placeholder only if dir missing |
| T-04-03 | Memory pressure | Steps 5-10 | 15 filters × rapid-tap does not OOM; AssetLoader LruCache stays under 32MB |
| T-04-04 | Network-free asset pipeline | Step 5 | All thumbnails load from assets/ — no network activity |
| T-04-05 | Rapid-tap race | Step 10 | 10 swaps in 5s, no black flash, no rebind |
| T-04-06 | Navigation tamper | Steps 2, 4 | Enum-serialized CameraMode — only 2 valid values; no stub route unexpectedly triggered |
</threat_model>

<verification>
- Clean APK builds (Task 1)
- 04-HANDOFF.md exists + ≥200 lines + all 5 ROADMAP criteria covered (Task 2)
- User completes Task 3 on Xiaomi 13T + fills 04-CHECKPOINT.md (human)
- After sign-off: VALIDATION nyquist flipped, STATE advanced, ROADMAP row complete, REQUIREMENTS 7 IDs marked complete, final commit lands
</verification>

<success_criteria>
- Clean debug build produces installable APK
- Physical device verifies all 5 Phase 4 ROADMAP success criteria
- 4 behaviors (STATIC/CRAWL/SWARM/FALL) visually verified
- DataStore persist cycle verified
- Multi-face test observed graceful
- Phase 3 shutter+capture regression verified (no breakage)
- Git history has final Phase 4 close commit
</success_criteria>

<output>
After Task 4 completes, create `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-08-SUMMARY.md` + `.planning/phases/04-filter-catalog-picker-face-filter-mode/04-SUMMARY.md` (phase-level aggregate, mirroring 03-SUMMARY.md pattern if one exists; otherwise the 8 per-plan summaries are sufficient). Document:
1. Final APK size (for Phase 7 PRF-04 tracking)
2. Total unit test count post-Phase 4 (expect ~23)
3. Any hard-gate retries required + the gap plans filed to close them
4. Xiaomi 13T HyperOS version + any OEM quirks observed (populate ROADMAP/STATE for Phase 7 matrix)
5. Cost/time breakdown per plan (04-01 through 04-08) for Phase 7 retrospective input
</output>
