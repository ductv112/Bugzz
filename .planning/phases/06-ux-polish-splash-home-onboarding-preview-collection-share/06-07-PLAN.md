---
phase: 06
plan: 07
type: execute
wave: 5
status: draft
created: 2026-05-05
depends_on: [06-06]
files_modified:
  - app/src/main/java/com/bugzz/filter/camera/ui/settings/SettingsScreen.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/screens/StubScreens.kt
autonomous: true
requirements: [UX-09]
threat_refs: [T-06-06]
must_haves:
  truths:
    - "SettingsScreen renders TopAppBar 'Settings' + back arrow + 4 ListItem rows (Version, Privacy Policy, Rate the App, About)"
    - "Version row shows BuildConfig.VERSION_NAME (read-only, disabled semantics)"
    - "Privacy Policy + Rate rows show Toast stubs (D-17, D-18 — no real URL/Play Store)"
    - "About row read-only with trailing 'Bugzz — Bug filter prank camera'"
    - "BugzzApp.kt: composable<SettingsRoute> { SettingsScreen(onBack = popBackStack) } added"
    - "BugzzApp.kt: HomeRoute composable's onSettings lambda swapped from Plan 06 placeholder to navigate(SettingsRoute)"
    - "StubScreens.kt deleted entirely (no remaining references after Plans 03/04/05)"
    - "All Phase 5 + Wave 0-5a tests still GREEN"
    - "9 D-32 grep-asserts all pass"
  artifacts:
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/settings/SettingsScreen.kt"
      provides: "Stateless Settings stub composable with 4 rows + Toast stubs (UX-09, D-17/D-18, UI-SPEC §9)"
      min_lines: 80
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt"
      provides: "Final NavHost — SettingsRoute composable wired + HomeScreen onSettings finalized + StubScreens import lines removed"
  key_links:
    - from: "BugzzApp HomeRoute composable"
      to: "navController.navigate(SettingsRoute)"
      via: "onSettings lambda"
      pattern: "navigate\\(SettingsRoute"
    - from: "SettingsScreen back arrow IconButton"
      to: "navController.popBackStack()"
      via: "onBack lambda"
      pattern: "onBack"
---

<objective>
Wave 5 part B — Final UX-09 ship + nav graph close-out + dead code removal.

Three concerns combined because they are tightly coupled (cannot ship one without the others without leaving build broken or features non-functional):

1. Create `ui/settings/SettingsScreen.kt` — stateless composable with TopAppBar + 4 ListItem rows per UI-SPEC §9 / D-17 / D-18 — Version + Privacy Policy + Rate the App + About. Privacy + Rate use Toast stubs (no real URL / Play Store availability per personal-use scope).

2. BugzzApp.kt: add `composable<SettingsRoute> { ... }` block + finalize HomeScreen `onSettings` lambda from Plan 06 placeholder to real `navigate(SettingsRoute)`.

3. Delete `ui/screens/StubScreens.kt` entirely. After Plans 03/04/05/06:
   - SplashScreen stub: replaced by `ui/splash/SplashScreen` (Plan 03)
   - PreviewScreen stub: replaced by `ui/preview/PreviewScreen` (Plan 04)
   - CollectionScreen stub: replaced by `ui/collection/CollectionScreen` (Plan 05)
   - StubContent helper: orphaned
   - The unused stub `CameraScreen` in StubScreens.kt was never imported by BugzzApp (production CameraScreen lives in `ui/camera/`)
   No imports reference `com.bugzz.filter.camera.ui.screens.*` after Plan 06 — verified by `Grep "import com.bugzz.filter.camera.ui.screens"` returning nothing.

Output: Phase 6 production code complete. All 5 production screens shipped + nav graph wired + zero stubs left in repo. Ready for Plan 08 device handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-CONTEXT.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-RESEARCH.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-UI-SPEC.md
@app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
@app/src/main/java/com/bugzz/filter/camera/ui/screens/StubScreens.kt

<interfaces>
<!-- SettingsScreen — stateless (UI-SPEC §9, D-17, D-18) -->
@Composable
fun SettingsScreen(
    onBack: () -> Unit,
)
// No ViewModel. No state. Pure presentational + Toast/Intent on click.
// Uses Material3 SmallTopAppBar (or TopAppBar) + ListItem rows + HorizontalDivider.

<!-- 4 rows (UI-SPEC §9 row table — exact strings + behavior): -->
// Row 1 — Version: headline "Version" + trailing BuildConfig.VERSION_NAME — read-only (no clickable)
// Row 2 — Privacy Policy: headline "Privacy Policy" — clickable → Toast "Coming in next release"
// Row 3 — Rate the App: headline "Rate the App" — clickable → Toast "Coming when published to Play Store"
// Row 4 — About: headline "About" + trailing "Bugzz — Bug filter prank camera" — read-only

<!-- BugzzApp HomeRoute — change Plan 06's placeholder lambda to real nav -->
composable<HomeRoute> {
    HomeScreen(
        onFaceFilter = { ... },
        onInsectFilter = { ... },
        onMyCollection = { navController.navigate(CollectionRoute) },
        onSettings = { navController.navigate(SettingsRoute) },   // ← Plan 07 finalize
    )
}

<!-- BugzzApp SettingsRoute composable — NEW -->
composable<SettingsRoute> {
    SettingsScreen(onBack = { navController.popBackStack() })
}

<!-- StubScreens.kt deletion check -->
// Pre-condition (verify before delete): grep "import com.bugzz.filter.camera.ui.screens" → 0 matches expected
// Pre-condition (verify before delete): grep "ui.screens." → 0 matches in any source file (production or test)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsScreen + wire BugzzApp SettingsRoute composable + finalize HomeScreen onSettings</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/settings/SettingsScreen.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
  </files>
  <action>
    1. **Create `app/src/main/java/com/bugzz/filter/camera/ui/settings/SettingsScreen.kt`** (~110 lines per UI-SPEC §9):
       - Package `com.bugzz.filter.camera.ui.settings`
       - Imports: `BuildConfig` (`com.bugzz.filter.camera.BuildConfig`), Material3 Scaffold/TopAppBar/ListItem/HorizontalDivider/IconButton/Icon, ArrowBack icon, Toast/LocalContext, Modifier.clickable + .semantics
       - `@OptIn(ExperimentalMaterial3Api::class)` if needed for SmallTopAppBar (M3 typically requires this annotation)
       - Composable signature: `SettingsScreen(onBack: () -> Unit)`
       - Body matches RESEARCH Code Examples §SettingsScreen + UI-SPEC §9:
         ```kotlin
         @OptIn(ExperimentalMaterial3Api::class)
         @Composable
         fun SettingsScreen(onBack: () -> Unit) {
             val context = LocalContext.current
             Scaffold(
                 topBar = {
                     TopAppBar(
                         title = { Text("Settings") },
                         navigationIcon = {
                             IconButton(onClick = onBack) {
                                 Icon(Icons.Default.ArrowBack, contentDescription = "Back")
                             }
                         },
                     )
                 },
             ) { padding ->
                 Column(modifier = Modifier.fillMaxSize().padding(padding)) {
                     ListItem(
                         headlineContent = { Text("Version", style = MaterialTheme.typography.bodyMedium) },
                         trailingContent = { Text(BuildConfig.VERSION_NAME, style = MaterialTheme.typography.bodyMedium) },
                         modifier = Modifier.semantics {
                             contentDescription = "Version: ${BuildConfig.VERSION_NAME}"
                         },
                     )
                     HorizontalDivider()
                     ListItem(
                         headlineContent = { Text("Privacy Policy", style = MaterialTheme.typography.bodyMedium) },
                         modifier = Modifier
                             .clickable {
                                 Toast.makeText(context, "Coming in next release", Toast.LENGTH_SHORT).show()
                             }
                             .semantics { role = Role.Button; contentDescription = "Privacy Policy" },
                     )
                     HorizontalDivider()
                     ListItem(
                         headlineContent = { Text("Rate the App", style = MaterialTheme.typography.bodyMedium) },
                         modifier = Modifier
                             .clickable {
                                 Toast.makeText(context, "Coming when published to Play Store", Toast.LENGTH_SHORT).show()
                             }
                             .semantics { role = Role.Button; contentDescription = "Rate the App" },
                     )
                     HorizontalDivider()
                     ListItem(
                         headlineContent = { Text("About", style = MaterialTheme.typography.bodyMedium) },
                         supportingContent = { Text("Bugzz — Bug filter prank camera", style = MaterialTheme.typography.bodyMedium) },
                         modifier = Modifier.semantics {
                             contentDescription = "About: Bugzz — Bug filter prank camera"
                         },
                     )
                     HorizontalDivider()
                 }
             }
         }
         ```
       - Per D-18: NO real ViewModel, NO real prefs, NO debug toggle. Phase 6 stub only.
       - Per UI-SPEC §9 typography: bodyMedium (14sp Normal) for headline + trailing — preserve 4-size inventory (no new sizes)
       - Per UI-SPEC §9 dividers: `HorizontalDivider()` Material3 default — no color override (1dp colorScheme.outlineVariant)
       - KDoc references UX-09, D-17, D-18, UI-SPEC §9 — explicitly lists "Phase 6 stub; v2 milestone adds real Privacy URL + Play Store deep-link"

    2. **Edit `app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt`** — finalize nav graph:
       - Add import: `import com.bugzz.filter.camera.ui.settings.SettingsScreen`
       - Add import: `import com.bugzz.filter.camera.ui.nav.SettingsRoute`
       - HomeRoute composable: replace Plan 06's placeholder `onSettings = { /* Plan 06-07 will replace ... */ }` with `onSettings = { navController.navigate(SettingsRoute) }`
       - Add NEW composable block AFTER existing routes (e.g., right after CollectionRoute composable):
         ```kotlin
         composable<SettingsRoute> {
             SettingsScreen(onBack = { navController.popBackStack() })
         }
         ```
       - Verify final composable count in NavHost = 7: Splash, Onboarding, Home, Camera (FaceFilter+InsectFilter dispatch), Preview, Collection, Settings (CameraRoute is one composable that branches internally)

    3. Build + run:
       - `./gradlew :app:assembleDebug :app:testDebugUnitTest -x lintDebug` — must be GREEN.
       - SettingsScreen device-renderable; Settings → tap any clickable row → Toast appears (manual device verification deferred to Plan 08).
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest :app:assembleDebug -x lintDebug</automated>
  </verify>
  <done>
    SettingsScreen compiles. BugzzApp wires SettingsRoute composable + finalizes HomeScreen onSettings to navigate(SettingsRoute). APK assembles. All tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete StubScreens.kt entirely (orphaned after Plans 03/04/05)</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/screens/StubScreens.kt
  </files>
  <action>
    Pre-deletion safety verification:
    1. Run `Grep` for `import com.bugzz.filter.camera.ui.screens` across `app/src` — must return 0 matches.
    2. Run `Grep` for `\.ui\.screens\.` (with `\\.` escaping) across `app/src` — must return 0 matches.
    3. Run `Grep` for `screens\.SplashScreen|screens\.PreviewScreen|screens\.CollectionScreen|screens\.CameraScreen|screens\.StubContent` — must return 0 matches.

    If ALL 3 grep queries return 0 matches, proceed to deletion. If ANY returns matches, HALT — Plan 06 wiring is incomplete.

    Deletion:
    1. `Remove-Item app/src/main/java/com/bugzz/filter/camera/ui/screens/StubScreens.kt` (PowerShell) OR `git rm app/src/main/java/com/bugzz/filter/camera/ui/screens/StubScreens.kt` (preferred — git tracks deletion)
    2. The empty `ui/screens/` directory: optionally remove. If git keeps the directory entry, leave it — empty package directories are not problematic.
    3. Run `./gradlew :app:assembleDebug :app:testDebugUnitTest -x lintDebug` — full GREEN sweep.

    Phase 5 invariants check (D-32 — non-negotiable):
    Run all 9 grep-asserts. If ANY misses, the deletion accidentally took down a fix invariant — restore the file from git and investigate. Expected counts (production sources untouched):
    - `isCapturing` ≥ 1
    - `bindJob.cancel()` ≥ 1
    - `OneShotEvent.FilterLoadError` ≥ 2
    - `captureFlash` inside onSuccess pattern ≥ 1
    - `require(frameCount > 0)` ≥ 1
    - `assetLoader.preload(def.assetDir)` ≥ 1
    - `isRecording` guard ≥ 1
    - `cameraMode = CameraMode.InsectFilter` ≥ 1
    - `setPreviewSize` (Phase 5 gap-02) ≥ 1
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest :app:assembleDebug -x lintDebug</automated>
  </verify>
  <done>
    StubScreens.kt deleted. APK assembles. All tests GREEN. 9 D-32 grep-asserts all pass. No imports reference `ui.screens` package.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 5 final compliance gate + 06-VALIDATION.md status update</name>
  <files>
    .planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-VALIDATION.md
  </files>
  <action>
    Final pre-Wave-6 gate:
    1. Full unit suite: `./gradlew :app:testDebugUnitTest -x lintDebug` — must be GREEN. Document the count of tests RUN, IGNORED, and the count of NEW tests added across Phase 6 (Wave 0..5).
    2. Clean assemble: `./gradlew :app:clean :app:assembleDebug -x lintDebug` — must produce APK. Document APK file size delta vs Phase 5 baseline (expect +~750KB Lottie + a few KB code).
    3. UI-SPEC compliance audit (visual contract — production code):
       - 4 typography sizes only: grep `fontSize\s*=\s*[0-9]+\.sp` — values must be subset of {10, 14, 16, 24}
       - 2 font weights only: grep `fontWeight\s*=\s*FontWeight\.` — values must be subset of {Normal, Medium} (no SemiBold, no Bold)
       - On-grid spacing: grep `padding\(.*?\.dp` and `Spacer.*\.dp` — values must be multiples of 4 OR explicit allow-listed sizes (200dp Lottie/Splash, 120dp Lottie/EmptyState, 80dp action bar, 56dp button, 48dp IconButton, 12dp dot, 8dp dot, 4dp 1dp)
       - Accent palette unchanged: grep `Color\(0xFF` — count of distinct hex colors in production sources must match Phase 5 baseline + permitted additions (`0xFFE53935` already exists, `0xFF9E9E9E` Onboarding inactive dot, `0xFF1E1E1E` Preview action bar, `0xFF2A2A2A` Coil placeholder, `0xFF121212` background — all from UI-SPEC §Color).
       - Verbatim copy strings audit: grep these strings, each must appear ≥1 time in the listed file:
         - "Bugzz" — SplashScreen.kt
         - "Welcome to Bugzz", "Pick a filter", "Capture and share" — OnboardingScreen.kt
         - "Skip", "Get Started", "Next" — OnboardingScreen.kt
         - "Done", "Share", "Delete", "Retake" — PreviewScreen.kt
         - "Share via" — ShareIntentBuilder.kt
         - "Delete this artifact?", "This can't be undone.", "Cancel" — DeleteConfirmDialog.kt
         - "My Collection" — CollectionScreen.kt
         - "No bugs captured yet", "Open Camera" — CollectionScreen.kt (or EmptyStateColumn caller)
         - "Settings", "Version", "Privacy Policy", "Rate the App", "About", "Bugzz — Bug filter prank camera", "Coming in next release", "Coming when published to Play Store" — SettingsScreen.kt
       - All Phase 5 carry strings (per 06-UI-SPEC §Copywriting "Inherited"): grep these in production sources, must be present ≥1 time:
         - "Saved to gallery" — should now appear ZERO times (Plan 04 replaced with nav). Note: this string may appear in test fixtures; restrict the grep to `app/src/main` only.
         - "Recording saved" — same — should be ZERO times in app/src/main.
         - "Take photo" — CameraScreen.kt or InsectFilterScreen.kt (shutter contentDescription, Phase 3 carry)
         - "Flip" — CameraScreen.kt + InsectFilterScreen.kt
       - 4. Verify all 13 Phase 6 requirements have at least one production source contributing:
         - UX-01: SplashScreen.kt + SplashViewModel.kt
         - UX-02: OnboardingScreen.kt + OnboardingViewModel.kt
         - UX-03: HomeScreen.kt onSettings + BugzzApp HomeRoute composable
         - UX-04: PreviewScreen.kt + PreviewViewModel.kt + VideoPreview.kt
         - UX-05: CollectionRepository.kt + CollectionScreen.kt
         - UX-06: BugzzApp CollectionRoute onItemTap → PreviewRoute(uri)
         - UX-07: EmptyStateColumn.kt called by CollectionScreen
         - UX-08: DeleteConfirmDialog.kt called by PreviewScreen
         - UX-09: SettingsScreen.kt + BugzzApp SettingsRoute composable
         - SHR-01..04: ShareIntentBuilder.kt + PreviewScreen Share onClick
       5. Update 06-VALIDATION.md task status table:
         - Plan 06 (Wave 5a) row Status = "✅ DeleteConfirmDialog + Share + HomeScreen onSettings GREEN"
         - Plan 07 (Wave 5b) row Status = "✅ SettingsScreen + nav graph + StubScreens delete GREEN"
       6. DO NOT yet flip nyquist_compliant — Plan 08 owns that flip after device PASS.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest -x lintDebug</automated>
  </verify>
  <done>
    Phase 6 production code shipped. All 13 requirements have grep-evidence in production sources. UI-SPEC compliance audit GREEN. APK assembles clean. Test suite GREEN. D-32 invariants intact. 06-VALIDATION.md task statuses updated. Plan 08 (clean build + handoff + device + post-PASS close-out) ready to start.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ external app via Toast / future Intent.ACTION_VIEW | SettingsScreen Privacy Policy + Rate rows: Toast only (no real Intent.ACTION_VIEW launched per D-17/D-18 stub-only scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | Privacy Policy URL launches malicious content | accept (Phase 6 stub-only) | Phase 6 ships Toast only — no Intent.ACTION_VIEW with URI is launched. When v2 milestone adds a real Privacy URL, must use HTTPS-only Intent.ACTION_VIEW (system browser handles URL safely; no WebView) — defer mitigation to v2. |
</threat_model>

<rollback>
If Task 1 fails (SettingsScreen + BugzzApp wire):
1. `git restore` SettingsScreen.kt + BugzzApp.kt
2. Re-run Wave 5a baseline `./gradlew :app:testDebugUnitTest`. Settings gear → Plan 06's placeholder lambda still works (does nothing).

If Task 2 fails (StubScreens delete):
1. `git checkout HEAD -- app/src/main/java/com/bugzz/filter/camera/ui/screens/StubScreens.kt` (un-delete)
2. Re-run baseline. App still works (stub file untouched even if orphaned).

If Task 3 reveals UI-SPEC compliance violation:
1. Identify the violating source file (typography, color, copy, or spacing)
2. Fix inline OR open a 06-gaps-NN-PLAN.md per inline gap fix protocol from Phase 5 precedent (05-gaps-01 / 05-gaps-02). Document in SUMMARY.
</rollback>

<verification>
- SettingsScreen file at correct path; compiles
- BugzzApp HomeRoute onSettings finalized; SettingsRoute composable added
- StubScreens.kt deleted; no imports reference ui.screens package
- 4 typography sizes only (10/14/16/24); 2 font weights only (Normal/Medium)
- On-grid spacing audit passes
- 13 verbatim Phase 6 copy strings present
- Phase 5 carry strings ("Take photo", "Flip", etc.) preserved
- "Saved to gallery" + "Recording saved" both removed from app/src/main (Plan 04 nav replacement verified)
- 9 D-32 grep-asserts all pass
- APK assembles
- Full test suite GREEN
</verification>

<success_criteria>
- [ ] SettingsScreen + BugzzApp.kt SettingsRoute composable shipped
- [ ] HomeScreen onSettings lambda wired to navigate(SettingsRoute) (Plan 06 placeholder replaced)
- [ ] StubScreens.kt deleted (no orphan source files)
- [ ] All 13 Phase 6 requirements have grep-evidence in production sources
- [ ] UI-SPEC compliance audit GREEN (typography + color + copy + spacing)
- [ ] D-32 invariants intact
- [ ] 06-VALIDATION.md task statuses updated for Plans 06 + 07
- [ ] Plan 08 (clean build + device handoff) ready
</success_criteria>

<output>
Create `.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-07-SUMMARY.md` documenting:
- Files created/modified/deleted (1 created, 1 modified, 1 deleted)
- 13-requirement grep-evidence map
- UI-SPEC compliance audit result
- D-32 invariant continuity
- APK size delta vs Phase 5 baseline
- Plan 08 readiness (device checkpoint pending)
</output>
