---
phase: 06
plan: 04
type: execute
wave: 3
status: draft
created: 2026-05-05
depends_on: [06-03]
files_modified:
  - app/src/main/java/com/bugzz/filter/camera/ui/nav/Routes.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewViewModel.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewScreen.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/preview/VideoPreview.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/camera/CameraScreen.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/insect/InsectFilterScreen.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/preview/PreviewViewModelTest.kt
autonomous: true
requirements: [UX-04]
threat_refs: [T-06-01, T-06-03]
must_haves:
  truths:
    - "PreviewRoute changed from data object to data class(val uri: String) in single atomic commit"
    - "Photo URIs render via Coil AsyncImage at full screen with ContentScale.Fit"
    - "Video URIs render via ExoPlayer + PlayerView (AndroidView) with auto-play loop"
    - "ExoPlayer release() called on DisposableEffect onDispose (T-06-03 mitigation)"
    - "Bottom action bar 80dp Surface shows Done + Share + Delete + Retake icons + 10sp labels"
    - "Done + Retake → popBackStack (file remains saved)"
    - "Delete shows inline AlertDialog → confirm → MediaStore.delete on IO → popBackStack"
    - "Share button is a TODO placeholder showing Toast (Plan 06 wires real share)"
    - "CameraScreen + InsectFilterScreen: OneShotEvent.PhotoSaved/VideoSaved navigate to PreviewRoute(uri.toString())"
    - "Toast 'Saved to gallery' / 'Recording saved' replaced by navigation (Toast removed for these branches)"
    - "All Phase 5 + Wave 0..2 tests still GREEN"
  artifacts:
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewScreen.kt"
      provides: "Production PreviewScreen — photo OR video branch + 4-action bottom bar + inline DeleteConfirmDialog"
      min_lines: 130
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewViewModel.kt"
      provides: "@HiltViewModel — MIME detection + deleteArtifact suspend on IO"
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/preview/VideoPreview.kt"
      provides: "ExoPlayer + PlayerView + DisposableEffect lifecycle helper composable (RESEARCH Pattern 3)"
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/nav/Routes.kt"
      provides: "PreviewRoute now data class(val uri: String)"
      contains: "data class PreviewRoute"
  key_links:
    - from: "CameraScreen LaunchedEffect"
      to: "navController.navigate(PreviewRoute(uri.toString()))"
      via: "OneShotEvent.PhotoSaved / VideoSaved branch"
      pattern: "navigate\\(PreviewRoute"
    - from: "InsectFilterScreen LaunchedEffect"
      to: "navController.navigate(PreviewRoute(uri.toString()))"
      via: "OneShotEvent.PhotoSaved / VideoSaved branch"
      pattern: "navigate\\(PreviewRoute"
    - from: "VideoPreview DisposableEffect onDispose"
      to: "exoPlayer.release()"
      via: "T-06-03 mitigation"
      pattern: "exoPlayer\\.release"
---

<objective>
Wave 3 — atomic PreviewRoute breaking change + PreviewScreen production wiring + Camera/InsectFilter post-capture nav. This is the highest-risk plan in Phase 6 because PreviewRoute changing from `data object` to `data class(val uri: String)` simultaneously breaks Routes.kt, BugzzApp.kt, CameraScreen.kt, and InsectFilterScreen.kt. RESEARCH Pitfall 6 mandates a SINGLE commit that covers all 4 files.

Why deferred from Plan 03: keeping the breaking change isolated makes it the only commit that can introduce a navigation graph regression. Plan 03 left Routes.kt with PreviewRoute still as `data object` so Plan 03 was reversible. Now Plan 04 owns the entire PreviewRoute change in one atomic step.

Why Share button is placeholder (not real share): Per VALIDATION row, ShareIntentBuilder + tests land in Plan 06 alongside DeleteConfirmDialog extraction. This plan ships PreviewScreen with all 4 buttons visible but Share triggers a Toast "Share coming next" — Plan 06 swaps the Toast for the real Intent.ACTION_SEND. This split keeps Plan 04 task count at 3 (no scope creep).

Why DeleteConfirmDialog inline (not shared component): Same VALIDATION reasoning — shared `ui/components/DeleteConfirmDialog.kt` lands Plan 06. This plan ships PreviewScreen with an INLINE Material3 AlertDialog (Phase 5 pattern). Plan 06 will refactor the inline dialog out to the shared component and re-use across Preview + future screens.

Output: After Plan 04, capturing a photo/video automatically navigates to Preview which displays the artifact and offers Done/Delete/Retake (working) + Share (Toast placeholder). UX-04 device-verifiable end-to-end except Share — finalized in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-CONTEXT.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-RESEARCH.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-UI-SPEC.md
@app/src/main/java/com/bugzz/filter/camera/ui/nav/Routes.kt
@app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
@app/src/main/java/com/bugzz/filter/camera/ui/camera/CameraScreen.kt
@app/src/main/java/com/bugzz/filter/camera/ui/camera/OneShotEvent.kt
@app/src/main/java/com/bugzz/filter/camera/ui/insect/InsectFilterScreen.kt

<interfaces>
<!-- Routes.kt PreviewRoute breaking change (RESEARCH Pitfall 6) -->
@Serializable data class PreviewRoute(val uri: String)
// All other routes UNCHANGED from Plan 03

<!-- PreviewViewModel signature -->
@HiltViewModel
class PreviewViewModel @Inject constructor(
    @ApplicationContext private val context: Context,
) : ViewModel() {
    suspend fun resolveMimeType(uri: Uri): String =
        withContext(Dispatchers.IO) { context.contentResolver.getType(uri) ?: "image/jpeg" }

    suspend fun deleteArtifact(uri: Uri): Boolean =
        withContext(Dispatchers.IO) {
            try {
                context.contentResolver.delete(uri, null, null) > 0
            } catch (e: Exception) {
                Timber.tag("Preview").e(e, "Delete failed for $uri")
                false
            }
        }
}

<!-- PreviewScreen signature -->
@Composable
fun PreviewScreen(
    uri: Uri,
    onDone: () -> Unit,
    onRetake: () -> Unit,
    onDeleted: () -> Unit,                 // called after successful MediaStore.delete
    onShareNotImplemented: () -> Unit,     // Plan 06 will replace with real share
    viewModel: PreviewViewModel = hiltViewModel(),
)

<!-- VideoPreview helper (RESEARCH Pattern 3) -->
@Composable
fun VideoPreview(uri: Uri, modifier: Modifier = Modifier) {
    val context = LocalContext.current
    val exoPlayer = remember(uri) {
        ExoPlayer.Builder(context).build().apply {
            setMediaItem(MediaItem.fromUri(uri))
            repeatMode = ExoPlayer.REPEAT_MODE_ALL
            prepare()
            playWhenReady = true
        }
    }
    val lifecycleOwner = LocalLifecycleOwner.current
    DisposableEffect(lifecycleOwner, exoPlayer) {
        val observer = LifecycleEventObserver { _, event -> when (event) {
            Lifecycle.Event.ON_PAUSE -> exoPlayer.pause()
            Lifecycle.Event.ON_RESUME -> exoPlayer.play()
            else -> Unit
        }}
        lifecycleOwner.lifecycle.addObserver(observer)
        onDispose {
            lifecycleOwner.lifecycle.removeObserver(observer)
            exoPlayer.release()                       // T-06-03 — non-negotiable
        }
    }
    AndroidView(
        factory = { ctx -> PlayerView(ctx).apply { player = exoPlayer; useController = false } },
        modifier = modifier,
    )
}

<!-- BugzzApp.kt PreviewRoute composable rewire (Plan 03 currently still has stub) -->
composable<PreviewRoute> { backStackEntry ->
    val route: PreviewRoute = backStackEntry.toRoute()
    val uri = Uri.parse(route.uri)
    PreviewScreen(
        uri = uri,
        onDone = { navController.popBackStack() },
        onRetake = { navController.popBackStack() },
        onDeleted = { navController.popBackStack() },
        onShareNotImplemented = { /* Plan 06 wires real share */ }
    )
}

<!-- Camera/InsectFilter post-capture nav (replaces Toast for PhotoSaved + VideoSaved branches) -->
// In CameraScreen.kt + InsectFilterScreen.kt LaunchedEffect for events.collect:
is OneShotEvent.PhotoSaved -> navController.navigate(PreviewRoute(event.uri.toString()))
is OneShotEvent.VideoSaved -> navController.navigate(PreviewRoute(event.uri.toString()))
// PhotoError + VideoError + FilterLoadError + CameraError Toasts UNCHANGED
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PreviewRoute breaking change + PreviewViewModel + un-Ignore PreviewViewModelTest</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/nav/Routes.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewViewModel.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/preview/PreviewViewModelTest.kt
  </files>
  <behavior>
    PreviewViewModelTest cases (un-Ignored, Robolectric):
    - resolveMimeType_imageMime_returnsImageJpeg: ContentResolver returns "image/jpeg" → resolveMimeType = "image/jpeg"
    - resolveMimeType_videoMime_returnsVideoMp4: ContentResolver returns "video/mp4" → resolveMimeType = "video/mp4"
    - resolveMimeType_nullMime_fallsBackToImageJpeg: ContentResolver returns null → "image/jpeg" (safe default)
    - deleteArtifact_success_returnsTrue: ContentResolver.delete returns 1 → deleteArtifact returns true
    - deleteArtifact_throws_returnsFalseNoCrash: ContentResolver.delete throws SecurityException → deleteArtifact returns false; no rethrow
  </behavior>
  <action>
    1. **Edit `app/src/main/java/com/bugzz/filter/camera/ui/nav/Routes.kt`** — single atomic change:
       - Replace `@Serializable data object PreviewRoute` with `@Serializable data class PreviewRoute(val uri: String)`
       - DO NOT touch other routes
       - Compile WILL break — BugzzApp.kt + CameraScreen.kt + InsectFilterScreen.kt all currently navigate `PreviewRoute` as data object. THIS TASK creates the new shape; Task 3 fixes the consumers in the same plan.
       - It is OK if `./gradlew :app:assembleDebug` FAILS at the end of Task 1 — Task 3 closes the gap. Task 1's verify command tests PreviewViewModel ONLY.

    2. **Create `app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewViewModel.kt`** (~50 lines):
       - Package `com.bugzz.filter.camera.ui.preview`
       - @HiltViewModel + @Inject constructor(@ApplicationContext context: Context)
       - Public `suspend fun resolveMimeType(uri: Uri): String` — withContext(Dispatchers.IO) { context.contentResolver.getType(uri) ?: "image/jpeg" }
       - Public `suspend fun deleteArtifact(uri: Uri): Boolean` — try/catch wrapping `context.contentResolver.delete(uri, null, null) > 0`; on exception log via Timber + return false
       - KDoc references UX-04, T-06-01 (Share will use mimeType later — Plan 06)

    3. **Un-Ignore `PreviewViewModelTest.kt`** — implement 5 test bodies:
       - @RunWith(RobolectricTestRunner::class) @Config(sdk = [34]) — needed for Context + ContentResolver types
       - Mockito-Kotlin: mock<Context>(), mock<ContentResolver>() — set up `whenever(context.contentResolver).thenReturn(resolver)`
       - For Uri argument: use `mock<Uri>()` (Phase 3 STATE #24 pattern)
       - resolveMimeType tests: `whenever(resolver.getType(any())).thenReturn("image/jpeg")` etc.
       - deleteArtifact tests: `whenever(resolver.delete(any(), anyOrNull(), anyOrNull())).thenReturn(1)` for success; `.thenThrow(SecurityException("denied"))` for failure path
       - Use runTest + StandardTestDispatcher — don't need Hilt module (test-construct VM directly with mocked Context)
       - Remove all 4 @Ignore("Plan 06-04") annotations (1 of original 4 is the MIME-isVideo test which was originally about state — fold into resolveMimeType tests if state class doesn't exist; PreviewViewModel keeps no isVideo state — composable does the branch directly)

    Run `./gradlew :app:testDebugUnitTest --tests "*PreviewViewModelTest*"` — expect 5 GREEN. APK build is EXPECTED TO FAIL until Task 3.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*PreviewViewModelTest*" -x lintDebug</automated>
  </verify>
  <done>
    Routes.kt has PreviewRoute as data class. PreviewViewModel compiles + 5 tests GREEN. APK build is intentionally broken pending Task 3 wiring (verify command does NOT include assembleDebug — only the test target).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PreviewScreen + VideoPreview composable (production)</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewScreen.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/preview/VideoPreview.kt
  </files>
  <action>
    1. **Create `app/src/main/java/com/bugzz/filter/camera/ui/preview/VideoPreview.kt`** (~50 lines per RESEARCH Pattern 3):
       - Package `com.bugzz.filter.camera.ui.preview`
       - @Composable VideoPreview(uri: Uri, modifier: Modifier = Modifier)
       - Body matches `<interfaces>` block exactly:
         - `val exoPlayer = remember(uri) { ExoPlayer.Builder(context).build().apply { setMediaItem(MediaItem.fromUri(uri)); repeatMode = ExoPlayer.REPEAT_MODE_ALL; prepare(); playWhenReady = true } }`
         - DisposableEffect(lifecycleOwner, exoPlayer) — observer pauses on ON_PAUSE, plays on ON_RESUME, releases on dispose (T-06-03)
         - AndroidView { PlayerView(ctx).apply { player = exoPlayer; useController = false } }
       - Imports: `androidx.media3.common.MediaItem`, `androidx.media3.exoplayer.ExoPlayer`, `androidx.media3.ui.PlayerView`, lifecycle observer types, Compose AndroidView
       - Accessibility: AndroidView wrapped in `Modifier.semantics { contentDescription = "Captured video" }` per UI-SPEC §5
       - KDoc references UX-04 video case + T-06-03 + RESEARCH Pattern 3

    2. **Create `app/src/main/java/com/bugzz/filter/camera/ui/preview/PreviewScreen.kt`** (~150 lines per UI-SPEC §5):
       - Package `com.bugzz.filter.camera.ui.preview`
       - Composable signature per `<interfaces>` block — onDone, onRetake, onDeleted, onShareNotImplemented callbacks
       - Internal state: `var mimeType by remember { mutableStateOf<String?>(null) }`; LaunchedEffect(uri) loads via viewModel.resolveMimeType(uri).
       - `var showDeleteDialog by remember { mutableStateOf(false) }` — drives inline AlertDialog visibility
       - `val scope = rememberCoroutineScope()` — for deleteArtifact suspend call from button onClick
       - Layout per UI-SPEC §5:
         - Box fillMaxSize background Color.Black
         - Media: branch on `mimeType?.startsWith("image") == true` → AsyncImage(model = uri, contentScale = ContentScale.Fit, contentDescription = "Captured photo", fillMaxSize) ELSE VideoPreview(uri, fillMaxSize)
         - Bottom action bar Surface align BottomCenter fillMaxWidth height 80.dp color Color(0xFF1E1E1E)
         - Inside Surface: Row Arrangement.SpaceEvenly verticalAlignment CenterVertically fillMaxSize
         - 4 PreviewAction(icon, label, onClick) sub-composables: Done(Check), Share(Share), Delete(Delete), Retake(Refresh)
         - PreviewAction internal composable — Column { IconButton size 48dp { Icon size 24dp tint White } + Spacer 4.dp + Text labelSmall White }
       - Action onClicks:
         - Done: onDone()
         - Share: onShareNotImplemented() — display a Toast "Share coming next" via passed lambda (Plan 06 wires real Intent)
         - Delete: showDeleteDialog = true
         - Retake: onRetake()
       - Inline DeleteConfirmDialog (not shared yet — Plan 06):
         ```kotlin
         if (showDeleteDialog) {
             AlertDialog(
                 onDismissRequest = { showDeleteDialog = false },
                 title = { Text("Delete this artifact?", style = TextStyle(fontSize = 16.sp, fontWeight = FontWeight.Medium)) },
                 text = { Text("This can't be undone.", style = MaterialTheme.typography.bodyMedium) },
                 confirmButton = { TextButton(onClick = { showDeleteDialog = false }) { Text("Cancel") } },
                 dismissButton = { TextButton(
                     onClick = {
                         showDeleteDialog = false
                         scope.launch {
                             val ok = viewModel.deleteArtifact(uri)
                             if (ok) onDeleted() else { /* leave dialog dismissed; user sees no change */ }
                         }
                     },
                     colors = ButtonDefaults.textButtonColors(contentColor = MaterialTheme.colorScheme.error)
                 ) { Text("Delete") } }
             )
         }
         ```
       - Mark inline AlertDialog with comment: `// Plan 06 will refactor to ui/components/DeleteConfirmDialog.kt shared composable.`
       - Accessibility per UI-SPEC §5: contentDescriptions Done/Share/Delete/Retake/photo/video
       - KDoc references UX-04, D-08, D-09, D-10, T-06-03 (delegated to VideoPreview), UI-SPEC §5
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug -x lintDebug 2>&1 | grep -E "error|FAILED" || echo "compile ok except known PreviewRoute consumers"</automated>
  </verify>
  <done>
    PreviewScreen + VideoPreview compile (consumers fail until Task 3 — that is expected). Files exist at correct package paths. PreviewScreen has 4 action buttons + inline AlertDialog + photo/video branch.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire BugzzApp PreviewRoute composable + Camera/InsectFilter post-capture navigation</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/camera/CameraScreen.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/insect/InsectFilterScreen.kt
  </files>
  <action>
    1. **Edit `app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt`**:
       - Add import: `import android.net.Uri` and `import androidx.navigation.toRoute`
       - Add import: `import com.bugzz.filter.camera.ui.preview.PreviewScreen` (production)
       - Remove import: `import com.bugzz.filter.camera.ui.screens.PreviewScreen` (stub — but DO NOT delete StubScreens.kt yet, Plan 07 owns deletion)
       - Replace existing `composable<PreviewRoute> { PreviewScreen(onBack = { navController.popBackStack() }) }` with:
         ```kotlin
         composable<PreviewRoute> { backStackEntry ->
             val context = LocalContext.current
             val route: PreviewRoute = backStackEntry.toRoute()
             val uri = Uri.parse(route.uri)
             PreviewScreen(
                 uri = uri,
                 onDone = { navController.popBackStack() },
                 onRetake = { navController.popBackStack() },
                 onDeleted = { navController.popBackStack() },
                 onShareNotImplemented = {
                     Toast.makeText(context, "Share coming next", Toast.LENGTH_SHORT).show()
                 }
             )
         }
         ```
       - Add imports: `androidx.compose.ui.platform.LocalContext`, `android.widget.Toast`
       - CameraRoute composable: replace `onOpenPreview = { navController.navigate(PreviewRoute) }` with passing the navController to CameraScreen so CameraScreen can navigate on PhotoSaved/VideoSaved events. Cleanest: change CameraScreen signature to accept `onCaptureSaved: (Uri) -> Unit`. Update BugzzApp:
         ```kotlin
         composable<CameraRoute> { backStackEntry ->
             val route: CameraRoute = backStackEntry.toRoute()
             when (route.mode) {
                 CameraMode.FaceFilter -> CameraScreen(
                     onCaptureSaved = { uri ->
                         navController.navigate(PreviewRoute(uri.toString()))
                     }
                 )
                 CameraMode.InsectFilter -> InsectFilterScreen(
                     onCaptureSaved = { uri ->
                         navController.navigate(PreviewRoute(uri.toString()))
                     }
                 )
             }
         }
         ```

    2. **Edit `app/src/main/java/com/bugzz/filter/camera/ui/camera/CameraScreen.kt`**:
       - Change signature from `fun CameraScreen()` (no args) — actually verify current signature (read file lines 1-40 for current sig):
         - If current sig has no args: ADD `onCaptureSaved: (Uri) -> Unit` parameter
         - Keep all other params unchanged
       - Inside the existing `LaunchedEffect(Unit) { vm.events.collect { event -> when (event) { ... } } }`:
         - For `is OneShotEvent.PhotoSaved -> Toast.makeText(context, "Saved to gallery", Toast.LENGTH_SHORT).show()` — REPLACE with: `is OneShotEvent.PhotoSaved -> { onCaptureSaved(event.uri) }`
         - For `is OneShotEvent.VideoSaved -> Toast.makeText(context, "Recording saved", Toast.LENGTH_SHORT).show()` — REPLACE with: `is OneShotEvent.VideoSaved -> { onCaptureSaved(event.uri) }`
         - DO NOT touch PhotoError/VideoError/FilterLoadError/CameraError/TestRecord* branches
       - Update KDoc to reference D-09 navigation (replaces Phase 3 D-13 Toast)

    3. **Edit `app/src/main/java/com/bugzz/filter/camera/ui/insect/InsectFilterScreen.kt`** (mirror Camera changes):
       - Change signature: ADD `onCaptureSaved: (Uri) -> Unit` parameter
       - In events.collect block:
         - `is OneShotEvent.PhotoSaved -> Toast.makeText(context, "Saved to gallery", Toast.LENGTH_SHORT).show()` → REPLACE with `is OneShotEvent.PhotoSaved -> { onCaptureSaved(event.uri) }`
         - `is OneShotEvent.VideoSaved -> Toast.makeText(context, "Recording saved", Toast.LENGTH_SHORT).show()` → REPLACE with `is OneShotEvent.VideoSaved -> { onCaptureSaved(event.uri) }`
         - Other branches UNCHANGED
       - Update KDoc to reference D-09

    4. **Build verification**:
       - `./gradlew :app:assembleDebug` — must SUCCEED. PreviewRoute breaking change is now fully closed.
       - `./gradlew :app:testDebugUnitTest` — must be GREEN (Phase 5 + Wave 0..3 tests).
       - 9 D-32 grep-asserts: re-run; all must pass. CRITICAL — InsectFilterScreen.kt edits must NOT disturb the gap-01 fix `cameraMode = CameraMode.InsectFilter` (Phase 5 37b7a17) — that lives in InsectFilterViewModel.bind, not Screen.kt, so unaffected. CameraScreen.kt edits must NOT disturb `captureFlash inside onSuccess` (Phase 3 4e94591) — also in CameraViewModel callback chain.
       - Toast for "Saved to gallery" / "Recording saved" must NO LONGER appear in CameraScreen.kt or InsectFilterScreen.kt (verify via grep: count of `Saved to gallery` should drop from 2 to 0; `Recording saved` from 2 to 0).
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest :app:assembleDebug -x lintDebug</automated>
  </verify>
  <done>
    Build succeeds. Suite GREEN. CameraScreen + InsectFilterScreen receive `onCaptureSaved` lambda from BugzzApp. Toasts removed for capture-saved branches. D-32 invariants preserved. Capture → navigate to PreviewRoute(uri.toString()) is wired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ MediaStore content URI | URI passed as nav arg, parsed via Uri.parse, fed to ContentResolver. Receiving end (Coil/ExoPlayer/contentResolver.delete) trusts the URI's namespace — but URI originates from Phase 3 ImageCapture / Phase 5 VideoCapture which only ever produce DCIM/Bugzz/* URIs. No external URI input. |
| App ↔ ExoPlayer/Media3 | New: ExoPlayer holds AudioFocus, MediaCodec, SurfaceTexture native resources. Lifecycle leak (T-06-03) is the only credible threat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Share intent leakage | accept (deferred) | Phase 6 ships clean URI-only share via FLAG_GRANT_READ_URI_PERMISSION in Plan 06; this plan ships Share as a Toast placeholder so no real sharing happens yet. |
| T-06-03 | Denial of Service (self) | ExoPlayer memory + resource leak | mitigate | VideoPreview composable: `DisposableEffect(lifecycleOwner, exoPlayer) { ...; onDispose { exoPlayer.release() } }` — release() is non-negotiable. Also pause()/play() on ON_PAUSE/ON_RESUME via LifecycleEventObserver. Pattern matches RESEARCH Pattern 3 + Pitfall 2 verbatim. |
</threat_model>

<rollback>
**This is the riskiest plan in Phase 6.** PreviewRoute breaking change spans 4 files in 1 commit window.

If Task 1 fails (PreviewViewModel tests):
1. `git restore` Routes.kt + PreviewViewModel.kt + PreviewViewModelTest.kt
2. Re-run Wave 2 baseline `./gradlew :app:testDebugUnitTest`

If Task 2 fails (Compose composable compile):
1. `git restore` Task 1 + Task 2 files (5 files total)
2. Re-run Wave 2 baseline

If Task 3 fails (BugzzApp/Camera/InsectFilter wiring):
1. `git restore` ALL 8 files this plan touched — single revert returns to Wave 2 baseline (Plan 03 GREEN)
2. Re-run Wave 2 baseline

Recovery is atomic — no half-merged state because the breaking change MUST be in one commit. Plan 04 SUMMARY must record git SHA1 of the merge commit so subsequent gap-fix plans can reference it.
</rollback>

<verification>
- 5 PreviewViewModelTest cases GREEN
- Suite GREEN (Phase 5 + Waves 0-3)
- APK assembles
- Routes.kt has `data class PreviewRoute(val uri: String)` exactly once
- BugzzApp wires PreviewRoute composable with toRoute<PreviewRoute>() + Uri.parse pattern
- CameraScreen.kt: count of `navigate(PreviewRoute` ≥ 1 (via onCaptureSaved closure)
- InsectFilterScreen.kt: count of `navigate(PreviewRoute` ≥ 1 (via onCaptureSaved closure)
- Toast `"Saved to gallery"` count = 0 in CameraScreen.kt + InsectFilterScreen.kt (replaced by nav)
- Toast `"Recording saved"` count = 0 in CameraScreen.kt + InsectFilterScreen.kt (replaced by nav)
- exoPlayer.release() count = 1 in VideoPreview.kt
- 9 D-32 grep-asserts all pass
</verification>

<success_criteria>
- [ ] Routes.kt PreviewRoute is data class(val uri: String)
- [ ] PreviewViewModel + PreviewScreen + VideoPreview compile with documented APIs
- [ ] BugzzApp PreviewRoute composable rewired with toRoute + Uri.parse
- [ ] Camera + InsectFilter screens post-capture navigation working (Toasts removed for capture-saved branches)
- [ ] 5 PreviewViewModelTest cases GREEN
- [ ] T-06-03 mitigation present (DisposableEffect onDispose release())
- [ ] Phase 5 + Waves 0-2 suite untouched (D-32 invariants intact)
- [ ] APK assembles
- [ ] StubScreens.kt UNCHANGED (deletion deferred to Plan 07)
</success_criteria>

<output>
Create `.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-04-SUMMARY.md` documenting:
- PreviewRoute breaking change SHA1
- Files edited (3 production + 1 test)
- 5 GREEN tests added
- Camera/InsectFilter capture→nav verified at commit
- T-06-03 mitigation grep-evidence
- Wave 4 (Plan 05 — Collection) readiness sign-off
</output>
