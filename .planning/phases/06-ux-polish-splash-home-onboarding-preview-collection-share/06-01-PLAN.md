---
phase: 06
plan: 01
type: execute
wave: 0
status: draft
created: 2026-05-05
depends_on: []
files_modified:
  - app/src/test/java/com/bugzz/filter/camera/ui/splash/SplashViewModelTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/onboarding/OnboardingViewModelTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/onboarding/OnboardingPagerStateTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/preview/PreviewViewModelTest.kt
  - app/src/test/java/com/bugzz/filter/camera/data/CollectionRepositoryTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/collection/CollectionViewModelTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/components/DeleteConfirmDialogTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/share/ShareIntentBuilderTest.kt
  - app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt
autonomous: true
requirements: [UX-01, UX-02, UX-04, UX-05, UX-07, UX-08, SHR-01, SHR-02, SHR-03]
threat_refs: [T-06-01, T-06-02, T-06-03, T-06-04, T-06-05]
must_haves:
  truths:
    - "8 new test files compile RED (every test method @Ignored or asserting MISSING)"
    - "FilterPrefsRepositoryTest extended with 3 onboarding cases (still @Ignored, pointing to Plan 02)"
    - "./gradlew :app:testDebugUnitTest passes (existing Phase 5 GREEN suite preserved; new tests do not break compile)"
  artifacts:
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/splash/SplashViewModelTest.kt"
      provides: "RED scaffolds for UX-01 — Splash routes to Onboarding (first launch) vs Home (subsequent)"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/onboarding/OnboardingViewModelTest.kt"
      provides: "RED scaffolds for UX-02 — completeOnboarding() writes onboarding_completed=true"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/onboarding/OnboardingPagerStateTest.kt"
      provides: "RED scaffolds for UX-02 — Skip/Next/GetStarted page-state logic"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/preview/PreviewViewModelTest.kt"
      provides: "RED scaffolds for UX-04 — photo vs video MIME branch + deleteArtifact on IO"
    - path: "app/src/test/java/com/bugzz/filter/camera/data/CollectionRepositoryTest.kt"
      provides: "RED scaffolds for UX-05 — MediaStore query DCIM/Bugzz filter + URI namespace per MIME"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/collection/CollectionViewModelTest.kt"
      provides: "RED scaffolds for UX-07 — empty state when MediaItem list empty"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/components/DeleteConfirmDialogTest.kt"
      provides: "RED scaffolds for UX-08 — Cancel vs Confirm callback contract"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/share/ShareIntentBuilderTest.kt"
      provides: "RED scaffolds for SHR-01..03 — buildShareIntent() shape + createChooser wrap"
    - path: "app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt"
      provides: "EXTEND with onboardingCompleted read/write/corruption-default tests for D-23"
  key_links:
    - from: "*Test.kt files"
      to: "production sources landing in Plans 02..07"
      via: "@Ignore + KDoc forward-pointer"
      pattern: "@Ignore.*Plan 06-0[2-7]"
---

<objective>
Wave 0 Nyquist test scaffolds. Land 8 NEW RED test files + extend 1 existing test file with onboarding cases. Every new test method @Ignored with KDoc pointing to the plan that will un-Ignore it. Existing Phase 5 unit suite remains GREEN. Per Phase 2/3/4/5 Nyquist precedent (STATE #1) — tests defined BEFORE production code.

Purpose: Every requirement UX-01..09 + SHR-01..04 has a test file landed at this wave so per-task verification commands in Plans 02..07 wire to existing test paths. EXTEND to FilterPrefsRepositoryTest avoids creating a second prefs test class (D-23 — single repo, single test class).

Output: 8 new test files + 1 extended test file; compile-clean; full suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-CONTEXT.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-RESEARCH.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-VALIDATION.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-01-PLAN.md
@app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt
@app/src/test/java/com/bugzz/filter/camera/ui/insect/InsectFilterViewModelTest.kt

<interfaces>
<!-- Test patterns inherited from Phase 4/5 scaffolds — mirror exactly. -->
- JUnit 4.13.2 + Robolectric 4.13 (when Android types needed) + Mockito 5.11 + Mockito-Kotlin 5.2.1 + Turbine 1.2.0 + kotlinx-coroutines-test 1.10.2
- Pure-JVM test (no Android types): no @RunWith annotation
- Robolectric test (Android types touched): @RunWith(RobolectricTestRunner::class) @Config(sdk = [34])
- @HiltViewModel test pattern: Dispatchers.setMain(StandardTestDispatcher()) in @Before; resetMain() in @After
- VM Channel collection pattern (Phase 3 STATE #25): launch { events.first() }.async, advanceUntilIdle(), assert
- Mockito Uri trick (Phase 3 STATE #24): mock<Uri>() to avoid NPE under plain JVM
- DataStore test seam (Phase 4 STATE #14): InMemoryDataStore via PreferenceDataStoreFactory + TemporaryFolder
- @Ignore("Plan 06-NN") + KDoc forward-pointer is the standard RED scaffold marker — un-ignored when production code lands
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 4 new test files for ViewModels (Splash + Onboarding x2 + Preview)</name>
  <files>
    app/src/test/java/com/bugzz/filter/camera/ui/splash/SplashViewModelTest.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/onboarding/OnboardingViewModelTest.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/onboarding/OnboardingPagerStateTest.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/preview/PreviewViewModelTest.kt
  </files>
  <behavior>
    - SplashViewModelTest: 3 @Ignored tests — (a) onboarding_completed=false → emits Onboarding nav target; (b) onboarding_completed=true → emits Home nav target; (c) DataStore IOException → emits Onboarding (safe default false per T-06-04). Each @Ignore("Plan 06-03") with KDoc pointing to SplashViewModel landing.
    - OnboardingViewModelTest: 2 @Ignored tests — (a) completeOnboarding() calls FilterPrefsRepository.setOnboardingCompleted(); (b) flow re-emits true after write. @Ignore("Plan 06-03").
    - OnboardingPagerStateTest: 3 @Ignored tests — (a) Skip on any page fires onComplete callback; (b) Next on page 0/1 advances pagerState; (c) Get Started on page 2 fires onComplete. Pure-JVM test using mocked PagerState (no Compose UI test). @Ignore("Plan 06-03").
    - PreviewViewModelTest: 4 @Ignored tests — (a) MIME starts with "image" → state.isVideo=false; (b) MIME = "video/mp4" → state.isVideo=true; (c) deleteArtifact() calls contentResolver.delete on IO dispatcher; (d) deleteArtifact returns false when delete throws (no crash). @Ignore("Plan 06-04").
  </behavior>
  <action>
    Create 4 new test files. Each file:
    - Package matches directory (e.g. `package com.bugzz.filter.camera.ui.splash`)
    - Imports only what compiles RED (use String/Boolean primitives where possible; mock<X>() for Android types)
    - @Test methods all annotated @Ignore("Plan 06-NN — un-ignore when {Class} lands")
    - Each test body asserts MISSING by calling a stub helper that returns Unit, OR uses `fail("MISSING — Plan 06-NN")` if no setup possible. Compile must succeed without referencing classes that do not yet exist (do not import SplashViewModel, OnboardingViewModel, PreviewViewModel — they don't exist yet). Reference them by string in @Ignore message only.
    - KDoc on class: "RED scaffold per 06-VALIDATION Wave 0. Un-ignored in Plan 06-NN."
    - Mirror exact pattern from `app/src/test/java/com/bugzz/filter/camera/ui/insect/StickerStateTest.kt` (Phase 5 Wave 0 reference — pure JVM @Ignore scaffolds).
    - DO NOT create production source files — only tests. Production lands in later plans.
    - Verify all 9 grep-asserts from D-32 still pass (run grep on the 9 patterns to confirm no regression — these are in production sources untouched).
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest -x lintDebug</automated>
  </verify>
  <done>
    4 new test files exist; suite GREEN; new tests show as IGNORED in test report (not RUN); no Phase 5 test regressions; no production source modified in this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 4 new test files for Repository, Components, Share + EXTEND FilterPrefsRepositoryTest</name>
  <files>
    app/src/test/java/com/bugzz/filter/camera/data/CollectionRepositoryTest.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/collection/CollectionViewModelTest.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/components/DeleteConfirmDialogTest.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/share/ShareIntentBuilderTest.kt,
    app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt
  </files>
  <behavior>
    - CollectionRepositoryTest: 4 @Ignored tests — (a) returns empty list when ContentResolver.query returns null; (b) maps cursor rows to MediaItem with correct mimeType; (c) image rows produce Images.Media URIs; video rows produce Video.Media URIs (T-06-02 namespace correctness); (d) excludes rows outside DCIM/Bugzz/ via RELATIVE_PATH selection. Robolectric runner (Context type touched). @Ignore("Plan 06-05").
    - CollectionViewModelTest: 2 @Ignored tests — (a) empty MediaItem list → uiState.isEmpty=true; (b) non-empty list → uiState.isEmpty=false + items match. Pure JVM (mock repository). @Ignore("Plan 06-05").
    - DeleteConfirmDialogTest: 2 @Ignored tests — (a) onConfirm lambda invocation count after Delete tap; (b) onDismiss lambda invocation count after Cancel tap. Pure JVM lambda test (no Compose UI test); test the callback wiring contract used by PreviewViewModel/CollectionViewModel. @Ignore("Plan 06-06").
    - ShareIntentBuilderTest: 4 @Ignored tests — (a) buildShareIntent.action == ACTION_CHOOSER (createChooser wrap, SHR-03); (b) inner intent.type == passed mimeType (SHR-02); (c) inner intent EXTRA_STREAM == passed Uri (SHR-01); (d) FLAG_GRANT_READ_URI_PERMISSION set on inner intent (T-06-01). Robolectric runner (Intent + Uri types). @Ignore("Plan 06-06").
    - FilterPrefsRepositoryTest EXTEND: add 3 @Ignored cases — (a) writeOnboardingCompleted_thenRead_returnsTrue; (b) read_beforeWrite_returnsFalse_default; (c) corruptedDataStore_emitsFalse_default (T-06-04 mirror). @Ignore("Plan 06-02"). Existing 4 tests untouched.
  </behavior>
  <action>
    Create 4 new test files + EXTEND existing FilterPrefsRepositoryTest:
    - Same @Ignore + KDoc forward-pointer pattern as Task 1.
    - CollectionRepositoryTest + ShareIntentBuilderTest get @RunWith(RobolectricTestRunner::class) @Config(sdk = [34]) because they touch Context/ContentResolver/Intent/Uri types (Phase 5 Robolectric pattern from VideoRecorderTest.kt).
    - CollectionViewModelTest + DeleteConfirmDialogTest are pure-JVM (no @RunWith).
    - For FilterPrefsRepositoryTest extension: add 3 new @Test methods at end of class, all @Ignore("Plan 06-02 — un-ignore when onboardingCompleted property + setOnboardingCompleted() land"). Mirror existing test pattern (newRepo() helper, runTest, advanceUntilIdle).
    - DO NOT modify the existing 4 GREEN tests in FilterPrefsRepositoryTest. Insert new tests below the corruption test.
    - DO NOT create production source files — only tests.
    - Verify D-32 grep-asserts unchanged (production code untouched).
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest -x lintDebug</automated>
  </verify>
  <done>
    4 new test files + 1 extended test file exist; suite GREEN; 8 new + 3 extension tests show IGNORED; existing 4 FilterPrefsRepositoryTest cases still GREEN; no production source modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Wave 0 sampling continuity + grep D-32 fix preservation</name>
  <files>
    .planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-VALIDATION.md
  </files>
  <action>
    Run final verification gate before declaring Wave 0 complete:
    1. Run full test suite: `./gradlew :app:testDebugUnitTest -x lintDebug` — must be GREEN.
    2. Count IGNORED tests: should equal 8 new + 3 FilterPrefsRepo extensions = 11+ ignored tests in the new files (does not include any pre-existing @Ignored from earlier phases).
    3. Grep-assert all 9 D-32 patterns in production sources (READ ONLY — no edits):
       - `isCapturing` (Phase 3 dafc21e) — in CameraViewModel.kt or ImageCaptureSession.kt
       - `bindJob.cancel()` (Phase 3 9abbd0b) — in CameraController.kt
       - `OneShotEvent.FilterLoadError` (Phase 3 6ff00e0) — in CameraViewModel.kt + InsectFilterViewModel.kt
       - `captureFlash` inside onSuccess (Phase 3 4e94591) — in CameraScreen.kt
       - `require(frameCount > 0)` (Phase 3 b7f74cf) — in FilterEngine.kt or AssetLoader.kt
       - `assetLoader.preload(def.assetDir)` (Phase 4 514410c) — in CameraViewModel.kt
       - `isRecording` guard (Phase 5 D-26) — in InsectFilterViewModel.kt or CameraViewModel.kt
       - `cameraMode = CameraMode.InsectFilter` (Phase 5 gap-01 37b7a17) — in InsectFilterViewModel.bind
       - `setPreviewSize` (Phase 5 gap-02 de27c4e) — in StickerRenderer.kt
       Each grep MUST return at least 1 match. If ANY misses: HALT, this is a regression.
    4. Update 06-VALIDATION.md task statuses table: mark Plan 01 row Status = "✅ Wave 0 RED scaffolds GREEN".
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest -x lintDebug</automated>
  </verify>
  <done>
    Suite GREEN. 11+ new tests IGNORED. All 9 D-32 grep-asserts pass. 06-VALIDATION.md Plan 01 row updated. Ready for Wave 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test classpath ↔ production classpath | Wave 0 ONLY adds test files; production untouched. No new trust boundary. |

## STRIDE Threat Register (Wave 0 — test infra only)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (Wave 0 has no production threats — test scaffolds only) | — | — | accept | Phase 6 production threats T-06-01..06 land in Plans 02..07; Wave 0 just lays the test files that verify those mitigations. |
</threat_model>

<rollback>
If any task fails:
1. `git restore app/src/test/java/com/bugzz/filter/camera/ui/splash/SplashViewModelTest.kt` (and other newly-created files — they are not yet in git)
2. `git checkout HEAD -- app/src/test/java/com/bugzz/filter/camera/data/FilterPrefsRepositoryTest.kt` (revert extension)
3. Re-run `./gradlew :app:testDebugUnitTest` to confirm Phase 5 baseline still GREEN
4. Investigate compile/import error before retry
</rollback>

<verification>
- All 8 new test files compile under JDK 17
- FilterPrefsRepositoryTest still has 4 GREEN tests + 3 new IGNORED tests
- `./gradlew :app:testDebugUnitTest -x lintDebug` exits 0
- 9 D-32 grep-asserts all return ≥1 match
</verification>

<success_criteria>
- [ ] 8 new test files created at exact paths in files_modified
- [ ] FilterPrefsRepositoryTest extended with 3 new @Ignored cases
- [ ] Full unit suite GREEN
- [ ] No production source files touched in this plan
- [ ] All D-32 grep-asserts pass
- [ ] 06-VALIDATION.md Plan 01 row marked complete
</success_criteria>

<output>
Create `.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-01-SUMMARY.md` documenting:
- Test files created with line counts
- IGNORED test count per file
- D-32 grep-assert results
- Wave 1 readiness sign-off
</output>
