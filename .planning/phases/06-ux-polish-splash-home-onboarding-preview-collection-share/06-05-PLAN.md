---
phase: 06
plan: 05
type: execute
wave: 4
status: draft
created: 2026-05-05
depends_on: [06-04]
files_modified:
  - app/src/main/java/com/bugzz/filter/camera/data/MediaItem.kt
  - app/src/main/java/com/bugzz/filter/camera/data/CollectionRepository.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/components/EmptyStateColumn.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionViewModel.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionScreen.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
  - app/src/test/java/com/bugzz/filter/camera/data/CollectionRepositoryTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/collection/CollectionViewModelTest.kt
autonomous: true
requirements: [UX-05, UX-06, UX-07]
threat_refs: [T-06-02]
must_haves:
  truths:
    - "MediaItem data class exists with uri, mimeType, displayName, dateModified fields (D-15)"
    - "CollectionRepository.loadMediaItems() returns Flow<List<MediaItem>> filtered to DCIM/Bugzz/ and image/jpeg + video/mp4 only (T-06-02 selectionArgs binding)"
    - "Image rows construct MediaStore.Images.Media URIs; video rows construct MediaStore.Video.Media URIs (RESEARCH Critical Note — namespace correctness)"
    - "Sorted by DATE_MODIFIED DESC (newest first)"
    - "Query runs on Dispatchers.IO via flowOn(Dispatchers.IO)"
    - "CollectionViewModel exposes uiState StateFlow with isEmpty + items + videoThumbnails(Map<Uri, Bitmap?>)"
    - "Video thumbnails extracted via MediaMetadataRetriever.getFrameAtTime(0) on IO; cached in ViewModel state (NOT in MediaItem — RESEARCH anti-pattern)"
    - "CollectionScreen LazyVerticalGrid 3-column (Adaptive 120dp) with 4dp spacing"
    - "Tap thumbnail → navigate(PreviewRoute(uri.toString())) — back from Preview returns to Collection"
    - "EmptyStateColumn with Lottie + 'No bugs captured yet' + 'Open Camera' button → navigate(HomeRoute)"
    - "BugzzApp CollectionRoute composable rewired to production CollectionScreen"
    - "All Phase 5 + Wave 0-3 tests still GREEN"
  artifacts:
    - path: "app/src/main/java/com/bugzz/filter/camera/data/MediaItem.kt"
      provides: "MediaItem(uri, mimeType, displayName, dateModified) data class (D-15)"
    - path: "app/src/main/java/com/bugzz/filter/camera/data/CollectionRepository.kt"
      provides: "@Singleton MediaStore Files query with RELATIVE_PATH filter + per-MIME URI namespace"
      min_lines: 80
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionScreen.kt"
      provides: "LazyVerticalGrid + CollectionThumbnail + back arrow + empty state branch (UI-SPEC §6)"
      min_lines: 120
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionViewModel.kt"
      provides: "@HiltViewModel — StateFlow<UiState> with thumbnails Map cache"
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/components/EmptyStateColumn.kt"
      provides: "Shared empty state (D-26, UI-SPEC §8)"
  key_links:
    - from: "CollectionRepository.loadMediaItems"
      to: "context.contentResolver.query(MediaStore.Files.getContentUri('external'), ..., 'DATE_MODIFIED DESC')"
      via: "selection 'RELATIVE_PATH LIKE ? AND (MIME_TYPE = ? OR MIME_TYPE = ?)'"
      pattern: "RELATIVE_PATH"
    - from: "CollectionScreen item tap"
      to: "navController.navigate(PreviewRoute(uri.toString()))"
      via: "onItemTap lambda"
      pattern: "navigate\\(PreviewRoute"
    - from: "EmptyStateColumn 'Open Camera' button"
      to: "navController.navigate(HomeRoute)"
      via: "onCta lambda"
      pattern: "navigate\\(HomeRoute"
---

<objective>
Wave 4 — Production Collection screen + MediaStore-backed repository + shared EmptyStateColumn. Replace Phase 1 CollectionScreen stub. Tap-to-Preview wired via Plan 04's `PreviewRoute(val uri: String)` shape. Empty state for first-launch / cleared collection.

Why this plan does NOT yet wire HomeScreen → Collection: Phase 4 already wired Home's My Collection button to navigate(CollectionRoute). The wiring exists at HomeScreen call-site in BugzzApp. This plan only swaps the destination composable from stub → production.

Why DeleteConfirmDialog still inline (deferred to Plan 06): Per VALIDATION + UX-08, the SHARED component lands in Plan 06. Collection grid items are tap-only — no per-item delete affordance on the grid. Delete happens via Preview → Delete (already inline-AlertDialog from Plan 04). Plan 06 extracts the inline dialog to shared component + reuses across both contexts.

Output: Capture → Preview → popBack → Open Collection from Home → see thumbnail grid with Phase 5 device captures → tap a thumbnail → Preview opens → Retake from there pops back to Collection (NOT Camera) per D-13 standard back-stack. Empty state shows on cleared device. UX-05/06/07 device-verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-CONTEXT.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-RESEARCH.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-UI-SPEC.md
@app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
@app/src/main/java/com/bugzz/filter/camera/data/FilterPrefsRepository.kt

<interfaces>
<!-- MediaItem data class (D-15) -->
data class MediaItem(
    val uri: Uri,
    val mimeType: String,
    val displayName: String,
    val dateModified: Long,
)

<!-- CollectionRepository (RESEARCH Pattern 4 — adapted with per-MIME URI namespace) -->
@Singleton
class CollectionRepository @Inject constructor(
    @ApplicationContext private val context: Context,
) {
    fun loadMediaItems(): Flow<List<MediaItem>> = flow {
        val projection = arrayOf(
            MediaStore.MediaColumns._ID,
            MediaStore.MediaColumns.DISPLAY_NAME,
            MediaStore.MediaColumns.MIME_TYPE,
            MediaStore.MediaColumns.DATE_MODIFIED,
        )
        val selection = "${MediaStore.MediaColumns.RELATIVE_PATH} LIKE ? AND " +
            "(${MediaStore.MediaColumns.MIME_TYPE} = ? OR ${MediaStore.MediaColumns.MIME_TYPE} = ?)"
        val selectionArgs = arrayOf("DCIM/Bugzz/%", "image/jpeg", "video/mp4")
        val sortOrder = "${MediaStore.MediaColumns.DATE_MODIFIED} DESC"
        val items = mutableListOf<MediaItem>()
        context.contentResolver.query(
            MediaStore.Files.getContentUri("external"),
            projection, selection, selectionArgs, sortOrder
        )?.use { cursor ->
            // ... iterate; type-safe URI per MIME ...
        }
        emit(items)
    }.flowOn(Dispatchers.IO)
}

<!-- CollectionViewModel UiState -->
data class CollectionUiState(
    val items: List<MediaItem> = emptyList(),
    val videoThumbnails: Map<Uri, Bitmap?> = emptyMap(),
    val isLoading: Boolean = true,
    val isEmpty: Boolean = false,
)

@HiltViewModel
class CollectionViewModel @Inject constructor(
    @ApplicationContext private val context: Context,
    private val repository: CollectionRepository,
) : ViewModel() {
    private val _uiState = MutableStateFlow(CollectionUiState())
    val uiState: StateFlow<CollectionUiState> = _uiState.asStateFlow()

    init {
        viewModelScope.launch {
            repository.loadMediaItems().collect { items ->
                _uiState.update {
                    it.copy(items = items, isLoading = false, isEmpty = items.isEmpty())
                }
                // Lazy-load video thumbnails per item (off the critical path)
                items.filter { it.mimeType.startsWith("video") }.forEach { item ->
                    launch { extractAndCacheThumbnail(item.uri) }
                }
            }
        }
    }
    private suspend fun extractAndCacheThumbnail(uri: Uri) = withContext(Dispatchers.IO) {
        val retriever = MediaMetadataRetriever()
        try {
            retriever.setDataSource(context, uri)
            val bmp = retriever.getFrameAtTime(0, MediaMetadataRetriever.OPTION_CLOSEST_SYNC)
            _uiState.update { it.copy(videoThumbnails = it.videoThumbnails + (uri to bmp)) }
        } catch (e: Exception) {
            Timber.tag("Collection").w(e, "Thumbnail extraction failed for $uri")
            _uiState.update { it.copy(videoThumbnails = it.videoThumbnails + (uri to null)) }
        } finally {
            retriever.release()
        }
    }
}

<!-- EmptyStateColumn signature (D-26 / UI-SPEC §8) -->
@Composable
fun EmptyStateColumn(
    heading: String,
    ctaLabel: String,
    onCta: () -> Unit,
    modifier: Modifier = Modifier,
    animationAsset: String = "lottie/home_lottie.json",
)

<!-- CollectionScreen signature -->
@Composable
fun CollectionScreen(
    onBack: () -> Unit,
    onItemTap: (MediaItem) -> Unit,           // navController.navigate(PreviewRoute(item.uri.toString()))
    onOpenCamera: () -> Unit,                 // navController.navigate(HomeRoute) — empty state CTA
    viewModel: CollectionViewModel = hiltViewModel(),
)

<!-- BugzzApp CollectionRoute rewire -->
composable<CollectionRoute> {
    CollectionScreen(
        onBack = { navController.popBackStack() },
        onItemTap = { item -> navController.navigate(PreviewRoute(item.uri.toString())) },
        onOpenCamera = { navController.navigate(HomeRoute) {
            popUpTo(CollectionRoute) { inclusive = true }
        } }
    )
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MediaItem + CollectionRepository + un-Ignore CollectionRepositoryTest</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/data/MediaItem.kt,
    app/src/main/java/com/bugzz/filter/camera/data/CollectionRepository.kt,
    app/src/test/java/com/bugzz/filter/camera/data/CollectionRepositoryTest.kt
  </files>
  <behavior>
    CollectionRepositoryTest cases (un-Ignored, Robolectric):
    - emptyCursor_emitsEmptyList: ContentResolver.query returns empty cursor → loadMediaItems() emits empty list
    - imageRow_constructsImagesMediaUri: cursor row with mime=image/jpeg → produced URI starts with "content://media/external/images/media/" (RESEARCH Critical Note — sharing apps reject Files namespace)
    - videoRow_constructsVideoMediaUri: cursor row with mime=video/mp4 → produced URI starts with "content://media/external/video/media/"
    - selectionArgsBindRelativePath: passing context to query — verify selectionArgs[0] starts with "DCIM/Bugzz/" (T-06-02 parameter binding prevents path traversal)
  </behavior>
  <action>
    1. **Create `app/src/main/java/com/bugzz/filter/camera/data/MediaItem.kt`** (~15 lines, D-15):
       - Package `com.bugzz.filter.camera.data`
       - `data class MediaItem(val uri: Uri, val mimeType: String, val displayName: String, val dateModified: Long)`
       - KDoc references D-15 + UX-05

    2. **Create `app/src/main/java/com/bugzz/filter/camera/data/CollectionRepository.kt`** (~110 lines):
       - Package `com.bugzz.filter.camera.data`
       - @Singleton + @Inject constructor(@ApplicationContext context: Context)
       - Public `fun loadMediaItems(): Flow<List<MediaItem>>` — body matches `<interfaces>` block:
         - Projection: _ID, DISPLAY_NAME, MIME_TYPE, DATE_MODIFIED (4 columns)
         - Selection: `RELATIVE_PATH LIKE ? AND (MIME_TYPE = ? OR MIME_TYPE = ?)`
         - selectionArgs: `["DCIM/Bugzz/%", "image/jpeg", "video/mp4"]`
         - sortOrder: `"$DATE_MODIFIED DESC"`
         - Iterate cursor with `cursor.use { ... while (cursor.moveToNext()) { ... } }`
         - **CRITICAL** per RESEARCH Pattern 4 + Critical Note — construct URI by MIME:
           ```kotlin
           val contentUri = if (mime.startsWith("image")) {
               ContentUris.withAppendedId(MediaStore.Images.Media.EXTERNAL_CONTENT_URI, id)
           } else {
               ContentUris.withAppendedId(MediaStore.Video.Media.EXTERNAL_CONTENT_URI, id)
           }
           ```
         - flowOn(Dispatchers.IO) at end of flow chain
       - KDoc references UX-05, D-12, T-06-02 + RESEARCH Pattern 4 + Critical Note

    3. **Un-Ignore `CollectionRepositoryTest.kt`** — implement 4 cases:
       - @RunWith(RobolectricTestRunner::class) @Config(sdk = [34])
       - Mock Context + ContentResolver + Cursor
       - For empty case: `whenever(resolver.query(any(), any(), any(), any(), any())).thenReturn(emptyCursor)`
       - For image case: build cursor row with id=1, mime="image/jpeg" — assert produced URI string contains `"images/media/1"`
       - For video case: same with id=2, mime="video/mp4" — assert URI contains `"video/media/2"`
       - For selectionArgs case: use `argumentCaptor<Array<String>>()` to capture the selectionArgs passed to resolver.query — assert captured[0] == "DCIM/Bugzz/%"
       - Use Turbine: `repository.loadMediaItems().test { val list = awaitItem(); assertEquals(...); awaitComplete() }`
       - Remove all 4 @Ignore("Plan 06-05") annotations
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*CollectionRepositoryTest*" -x lintDebug</automated>
  </verify>
  <done>
    MediaItem + CollectionRepository compile. 4 CollectionRepositoryTest cases GREEN. Per-MIME URI namespace correctness verified. T-06-02 selectionArgs parameter binding asserted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CollectionViewModel + EmptyStateColumn + un-Ignore CollectionViewModelTest</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionViewModel.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/components/EmptyStateColumn.kt,
    app/src/test/java/com/bugzz/filter/camera/ui/collection/CollectionViewModelTest.kt
  </files>
  <behavior>
    CollectionViewModelTest cases (un-Ignored, pure JVM with mocked Repository):
    - emptyList_setsIsEmptyTrue: repository emits empty list → uiState.isEmpty == true, items empty
    - nonEmptyList_setsIsEmptyFalse: repository emits 2 MediaItems → uiState.isEmpty == false, items.size == 2
  </behavior>
  <action>
    1. **Create `app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionViewModel.kt`** (~80 lines):
       - Package `com.bugzz.filter.camera.ui.collection`
       - `data class CollectionUiState(items: List<MediaItem>, videoThumbnails: Map<Uri, Bitmap?>, isLoading: Boolean, isEmpty: Boolean)` per `<interfaces>` block
       - @HiltViewModel + @Inject constructor(@ApplicationContext context, repository: CollectionRepository)
       - `private val _uiState = MutableStateFlow(CollectionUiState(isLoading = true))`; expose `val uiState: StateFlow<CollectionUiState> = _uiState.asStateFlow()`
       - init {} block: viewModelScope.launch — collect repository.loadMediaItems(), update state, then for each video item launch a coroutine to extract+cache thumbnail
       - `private suspend fun extractAndCacheThumbnail(uri: Uri)` per `<interfaces>` block — withContext(Dispatchers.IO), MediaMetadataRetriever.use{}, getFrameAtTime(0, OPTION_CLOSEST_SYNC), update _uiState.videoThumbnails
       - KDoc references UX-05, UX-07, RESEARCH Pattern 5 + Open Question 8 (pure data MediaItem; thumbnails in separate state Map)

    2. **Create `app/src/main/java/com/bugzz/filter/camera/ui/components/EmptyStateColumn.kt`** (~50 lines per UI-SPEC §8):
       - Package `com.bugzz.filter.camera.ui.components`
       - Composable signature per `<interfaces>` block — heading, ctaLabel, onCta, modifier, animationAsset (default "lottie/home_lottie.json")
       - Layout per UI-SPEC §8:
         - Column horizontalAlignment CenterHorizontally verticalArrangement Center modifier.fillMaxSize()
         - LottiePlayer(assetPath = animationAsset, modifier = Modifier.size(120.dp), iterations = LottieConstants.IterateForever)
         - Spacer height 16.dp
         - Text(heading, style = TextStyle(fontSize = 16.sp, fontWeight = FontWeight.Medium), color = MaterialTheme.colorScheme.onSurface)
         - Spacer height 16.dp
         - Button(onClick = onCta) { Text(ctaLabel, style = MaterialTheme.typography.labelLarge) }
       - Accessibility: Lottie has `Modifier.semantics { contentDescription = "Empty state animation" }`
       - KDoc references D-14, D-26, UI-SPEC §8

    3. **Un-Ignore `CollectionViewModelTest.kt`** — implement 2 cases:
       - Pure JVM (no @RunWith)
       - Mock CollectionRepository: `mock<CollectionRepository> { onBlocking { loadMediaItems() } doReturn flowOf(emptyList()) }` for empty case
       - For non-empty: `flowOf(listOf(MediaItem(mock(), "image/jpeg", "a.jpg", 0L), MediaItem(mock(), "video/mp4", "b.mp4", 0L)))`
       - Mock Context (just construct VM — context not used in basic uiState assertions; if VM init eagerly tries thumbnails, mock the retrieve path or move thumbnail extraction to a separately-callable method to keep test isolated)
       - **Implementation note for testability:** Accept that thumbnail extraction in init can happen for the non-empty test — to avoid Robolectric bootup cost in this pure-JVM test, gate thumbnail extraction behind a `private val enableThumbnails: Boolean = true` constructor param OR move thumbnail launching into a public init method. Simplest: catch Exception in extractAndCacheThumbnail (already done) so test passes even if MediaMetadataRetriever fails on JVM mock.
       - Use Turbine: `viewModel.uiState.test { skipItems(1); val state = awaitItem(); assertEquals(true/false, state.isEmpty) }`
       - Remove both @Ignore("Plan 06-05") annotations
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*Collection*" -x lintDebug</automated>
  </verify>
  <done>
    CollectionViewModel + EmptyStateColumn compile. 2 CollectionViewModelTest cases GREEN. 4 CollectionRepositoryTest from Task 1 still GREEN. Total 6 collection-test cases GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CollectionScreen + wire BugzzApp CollectionRoute composable</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionScreen.kt,
    app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
  </files>
  <action>
    1. **Create `app/src/main/java/com/bugzz/filter/camera/ui/collection/CollectionScreen.kt`** (~150 lines per UI-SPEC §6):
       - Package `com.bugzz.filter.camera.ui.collection`
       - Composable signature per `<interfaces>` block — onBack, onItemTap, onOpenCamera, viewModel
       - Read `val uiState by viewModel.uiState.collectAsStateWithLifecycle()`
       - Layout:
         - Scaffold(topBar = { SmallTopAppBar(title = { Text("My Collection") }, navigationIcon = { IconButton(onClick = onBack) { Icon(Icons.Default.ArrowBack, contentDescription = "Back") } }) }) { padding -> ... }
         - Inside content: branch on uiState:
           - isLoading=true → CircularProgressIndicator centered (subjective; if too brief may flash — use `if (uiState.isLoading && uiState.items.isEmpty())` to defer)
           - !isLoading && isEmpty → EmptyStateColumn(heading = "No bugs captured yet", ctaLabel = "Open Camera", onCta = onOpenCamera, modifier = Modifier.padding(padding))
           - else → LazyVerticalGrid per UI-SPEC §6:
             ```kotlin
             LazyVerticalGrid(
                 columns = GridCells.Adaptive(minSize = 120.dp),
                 verticalArrangement = Arrangement.spacedBy(4.dp),
                 horizontalArrangement = Arrangement.spacedBy(4.dp),
                 modifier = Modifier.fillMaxSize().padding(padding),
             ) {
                 items(uiState.items, key = { it.uri.toString() }) { item ->
                     CollectionThumbnail(
                         item = item,
                         videoThumbnail = uiState.videoThumbnails[item.uri],
                         onTap = { onItemTap(item) },
                     )
                 }
             }
             ```
       - `private fun CollectionThumbnail(item: MediaItem, videoThumbnail: Bitmap?, onTap: () -> Unit)` per UI-SPEC §6:
         - Box aspectRatio(1f) clip(RectangleShape) clickable { onTap() } semantics { role = Role.Button; contentDescription = item.displayName }
         - If item.mimeType.startsWith("image"): AsyncImage(model = item.uri, contentScale = ContentScale.Crop, fillMaxSize, placeholder/error = ColorPainter(Color(0xFF2A2A2A)))
         - Else (video): show videoThumbnail bitmap if non-null via `Image(bitmap = videoThumbnail.asImageBitmap(), ...)` else gray placeholder; overlay 24dp white PlayArrow icon centered with semi-transparent black scrim Box
       - KDoc references UX-05, UX-06, UX-07, D-11, D-13 + UI-SPEC §6

    2. **Edit `app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt`**:
       - Remove import `com.bugzz.filter.camera.ui.screens.CollectionScreen` (stub — but DO NOT delete StubScreens.kt yet, Plan 07 owns it)
       - Add import `com.bugzz.filter.camera.ui.collection.CollectionScreen` (production)
       - Replace `composable<CollectionRoute> { CollectionScreen(onBack = { navController.popBackStack() }) }` with:
         ```kotlin
         composable<CollectionRoute> {
             CollectionScreen(
                 onBack = { navController.popBackStack() },
                 onItemTap = { item -> navController.navigate(PreviewRoute(item.uri.toString())) },
                 onOpenCamera = {
                     navController.navigate(HomeRoute) {
                         popUpTo(CollectionRoute) { inclusive = true }
                     }
                 }
             )
         }
         ```
       - DO NOT touch other composables in this task
       - HomeScreen → CollectionRoute lambda already wired Phase 4 (`onMyCollection` closure in `composable<HomeRoute>` already calls `navigate(CollectionRoute)`) — Plan 06 verifies still wired

    3. Build + run full test suite + grep:
       - `./gradlew :app:assembleDebug :app:testDebugUnitTest -x lintDebug` — must be GREEN
       - 9 D-32 grep-asserts re-run
       - Spacing on-grid grep `\.dp\b` in CollectionScreen.kt → values ∈ {4, 24, 120, 32} only (UI-SPEC §6 specifics)
       - Verify CollectionScreen has `LazyVerticalGrid` + `GridCells.Adaptive(minSize = 120.dp)` exactly once
       - Verify CollectionScreen has `Arrangement.spacedBy(4.dp)` ≥ 1 (the 4dp grid spacing per D-11)
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest :app:assembleDebug -x lintDebug</automated>
  </verify>
  <done>
    CollectionScreen compiles. BugzzApp CollectionRoute wired to production. 6 collection unit tests GREEN. Phase 5 + Wave 0-3 suite still GREEN. APK assembles. D-32 invariants preserved. UI-SPEC §6 grid layout values verified verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ MediaStore ContentProvider | New: read-only query against `MediaStore.Files.getContentUri("external")`. App reads only files it created (DCIM/Bugzz/) — no `READ_MEDIA_IMAGES` permission needed on minSdk 28+ (RESEARCH Open Question 5). |
| App ↔ MediaMetadataRetriever | New: reads MP4 byte stream of own files for thumbnail extraction. Native code path; release() in finally block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | MediaStore query exposing files outside DCIM/Bugzz | mitigate | `selection = "RELATIVE_PATH LIKE ? AND ..."` + `selectionArgs = ["DCIM/Bugzz/%", ...]` — ContentResolver bind-parameters the args; SQL injection prevented by ContentResolver implementation. Verified by Task 1 test `selectionArgsBindRelativePath`. Even if a row leaks past the filter, app only displays its own thumbnails — no destructive op. |
| (T-06-03 inherited from Plan 04) | DoS (self) | ExoPlayer leak | mitigate | Plan 04 mitigation already in place; CollectionScreen does NOT instantiate ExoPlayer (only thumbnail Bitmap rendering via Coil/MediaMetadataRetriever). |
</threat_model>

<rollback>
If Task 1 fails (Repository tests):
1. `git restore` MediaItem.kt + CollectionRepository.kt + CollectionRepositoryTest.kt
2. Re-run Wave 3 baseline `./gradlew :app:testDebugUnitTest`

If Task 2 fails (ViewModel + EmptyStateColumn):
1. `git restore` Task 1 + Task 2 files (5 files)
2. Re-run Wave 3 baseline

If Task 3 fails (Screen + BugzzApp wire):
1. `git restore` ALL 8 files this plan touched — single revert returns to Wave 3 baseline (Plan 04 GREEN)
2. Re-run Wave 3 baseline
</rollback>

<verification>
- 6 new tests GREEN (4 Repository + 2 ViewModel)
- Suite GREEN
- APK assembles
- MediaItem + CollectionRepository + CollectionViewModel + CollectionScreen + EmptyStateColumn files exist
- BugzzApp CollectionRoute composable rewired to production with onItemTap → PreviewRoute(uri.toString())
- HomeScreen → My Collection wiring still works (Phase 4 wiring untouched)
- StubScreens.kt UNCHANGED
- D-32 grep-asserts pass
- UI-SPEC §6 grid spacing verbatim
</verification>

<success_criteria>
- [ ] MediaItem + CollectionRepository + CollectionViewModel + CollectionScreen + EmptyStateColumn shipped
- [ ] BugzzApp CollectionRoute wired with onItemTap → PreviewRoute(uri.toString()) and onOpenCamera → HomeRoute popUpTo
- [ ] 6 new unit tests GREEN
- [ ] T-06-02 mitigation grep-evidence (selection LIKE + selectionArgs)
- [ ] Per-MIME URI namespace correctness (Images.Media for image/jpeg, Video.Media for video/mp4)
- [ ] Phase 5 + Waves 0-3 suite untouched (D-32 invariants intact)
- [ ] APK assembles
</success_criteria>

<output>
Create `.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-05-SUMMARY.md` documenting:
- Files created (5 production + 1 modified BugzzApp)
- 6 GREEN tests added
- T-06-02 mitigation grep-evidence
- HomeScreen → Collection → tap thumbnail → Preview round-trip end-to-end (composable level)
- Wave 5 (Plans 06+07 — DeleteConfirmDialog + Share + Settings + StubScreens delete) readiness
</output>
