---
phase: 02-camera-preview-face-detection-coordinate-validation
plan: 06
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md
autonomous: false
requirements: [CAM-01, CAM-02, CAM-06, CAM-07, CAM-08]
user_setup:
  - service: xiaomi-13t
    why: "Physical device verification — emulator cannot simulate real camera + ML Kit face detection + OverlayEffect baking into encoded MP4"
    env_vars: []
    dashboard_config:
      - task: "Enable Developer Options + USB Debugging on Xiaomi 13T"
        location: "Settings → About phone → tap MIUI version 7 times; Settings → Additional settings → Developer options → USB debugging"
      - task: "Connect USB cable (data-capable, not charge-only) between PC and phone; tap 'Allow' on RSA fingerprint prompt when prompted"
        location: "phone screen popup on first adb connection"
      - task: "Grant CAMERA permission when app prompts on first CameraRoute entry"
        location: "system permission dialog inside the app"
must_haves:
  truths:
    - "Clean debug APK exists at `app/build/outputs/apk/debug/app-debug.apk` and installs on Xiaomi 13T via adb without INSTALL_FAILED_* errors"
    - "02-HANDOFF.md exists at `.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md` with a 12-step device runbook (analogous to Phase 1's 01-04-HANDOFF.md format)"
    - "Runbook step 1 proves CAM-01 (CameraXViewfinder live preview, front cam)"
    - "Runbook step 2 proves CAM-02 (10× flip with zero CameraInUseException; <500ms per flip)"
    - "Runbook step 3 proves CAM-07 (red rect + landmark dots pixel-aligned across 4 device rotations × 2 lenses on Xiaomi 13T HyperOS)"
    - "Runbook step 4 proves CAM-08 (trackingId stable 60+ consecutive frames via logcat grep `FaceTracker`)"
    - "Runbook step 5 proves CAM-06 three-stream overlay compositing (TEST RECORD MP4 playback in Google Photos shows red rect baked into every frame)"
    - "Runbook documents the expected PITFALLS #13 multi-face partial-contour behavior as an expected finding (not a bug)"
    - "Runbook calls out Xiaomi MIUI / HyperOS OEM quirks: if alignment issue surfaces, it is documented in HANDOFF as Phase 7 cross-OEM matrix item, NOT worked around in Phase 2"
  artifacts:
    - path: ".planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md"
      provides: "Device verification runbook — 12 steps covering adb prerequisites + CAM-01/02/06/07/08 manual verification on Xiaomi 13T"
      contains: "adb install"
    - path: "app/build/outputs/apk/debug/app-debug.apk"
      provides: "Installable debug APK containing Phase 2 camera pipeline"
      min_lines: 0
  key_links:
    - from: "02-HANDOFF.md step 3 (rotation × lens matrix)"
      to: "CameraController.setTargetRotation (Plan 04) + OrientationEventListener (Plan 05) + canvas.setMatrix(frame.sensorToBufferTransform) (Plan 04)"
      via: "manual device verification"
      pattern: "4 device rotations.*2 lenses"
    - from: "02-HANDOFF.md step 5 (TEST RECORD MP4 playback)"
      to: "OverlayEffect targets (Plan 04) + CameraController.startTestRecording (Plan 04) + MediaStore DCIM/Bugzz/"
      via: "Google Photos playback of saved MP4"
      pattern: "DCIM/Bugzz"
    - from: "02-HANDOFF.md step 4 (trackingId logcat)"
      to: "FaceDetectorClient Timber FaceTracker tag (Plan 03)"
      via: "adb logcat filter"
      pattern: "FaceTracker"
---

<objective>
Close Phase 2 with a clean debug APK assembled from master + a 12-step device-verification runbook for Xiaomi 13T. The runbook is the exit gate — once the user reports 12/12 PASS, Phase 2 is complete and the project advances to Phase 3 (First Filter End-to-End + Photo Capture).

Purpose: Phase 2 exists specifically to front-load PITFALLS #1 (coord-space chaos) and #2 (video-overlay compositing) before Phase 3 draws production sprites. The runbook validates both pitfalls directly on the user's test hardware before one line of Phase 3 code is written. Without this manual gate, Phase 3 could invalidate the entire Phase 2 pipeline on device.

Output: A clean `./gradlew clean :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` run, and a single `02-HANDOFF.md` document capturing the runbook in the same format as Phase 1's `01-04-HANDOFF.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-CONTEXT.md
@.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-RESEARCH.md
@.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-VALIDATION.md
@.planning/phases/01-foundation-skeleton/01-04-HANDOFF.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Format reference for this plan's deliverable: Phase 1's handoff structure -->
<!-- Phase 1 handoff template structure:
  1. Prerequisites (user-side) — Dev Options + USB debug + cable + RSA allow
  2. What you are verifying — enumerate requirement IDs
  3. Numbered step sections — each with exact shell command (adb) + expected output + failure remediation
  4. Final sign-off block — user pastes 12/12 PASS into STATE.md
-->

Phase 2 runbook adapts this structure with 12 steps:
  1. Confirm APK exists (adb-independent check)
  2. Confirm phone detected (adb devices)
  3. Install APK (adb install)
  4. Launch app (adb shell am start)
  5. Grant CAMERA permission when prompted
  6. Navigate Splash → Home → Camera → observe live preview (CAM-01)
  7. Tap flip button 10× — verify no CameraInUseException in logcat (CAM-02)
  8. Hold device in portrait — verify red rect + landmark dots wrap face on front cam
  9. Rotate device through 4 positions × 2 lenses — verify alignment holds (CAM-07)
  10. Observe logcat Timber FaceTracker output — verify trackingId stable 60+ frames (CAM-08)
  11. Tap TEST RECORD — wait 5s auto-stop — play MP4 in Google Photos — verify red rect baked in (CAM-06 end-to-end)
  12. Record results in HANDOFF sign-off section + update STATE.md

Phase 2 absolute-path prefix for adb on Windows (already validated in Phase 1):
`"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run clean debug build and confirm assembleDebug + testDebugUnitTest + lintDebug all green</name>
  <files>(produces) app/build/outputs/apk/debug/app-debug.apk</files>
  <read_first>
    - .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-VALIDATION.md §Sampling Rate (full-suite command for wave merge)
    - .planning/phases/01-foundation-skeleton/01-04-PLAN.md §Task 1 (format reference for clean-build gate)
  </read_first>
  <action>
Run the three gradle tasks in sequence from project root. All three MUST succeed:

```bash
./gradlew clean
./gradlew :app:assembleDebug
./gradlew :app:testDebugUnitTest
./gradlew :app:lintDebug
```

Expected outcomes:
- `clean` — removes stale `app/build/`; takes <30s.
- `assembleDebug` — produces `app/build/outputs/apk/debug/app-debug.apk` (~40-50MB debug build with multi-dex Compose + CameraX + ML Kit + Hilt); completes with zero warnings for Phase 2 code. (Pre-existing Phase 1 lint warnings are allowed.)
- `testDebugUnitTest` — runs all 9 Phase 2 Nyquist tests (4 OneEuroFilter + 1 FaceDetectorOptions + 2 OverlayEffectBuilder + 2 CameraController) plus 1 Phase 1 ExampleUnitTest. Expected: `BUILD SUCCESSFUL`, 10/10 tests pass.
- `lintDebug` — runs Android lint; expected: zero new errors for Phase 2 files (`camera/`, `detector/`, `render/`, `ui/camera/`, `di/CameraModule.kt`). Pre-existing warnings from Phase 1 code are acceptable.

If `assembleDebug` fails with "Unresolved reference: ImplementationMode.EXTERNAL" — Plan 05 Task 3 already documented the fallback: change `ImplementationMode.EXTERNAL` → `ImplementationMode.PERFORMANCE` in `CameraScreen.kt`, commit as `fix(02-05): fall back to ImplementationMode.PERFORMANCE (research §Open Questions #1)`, then re-run this gate.

If `assembleDebug` fails with any ML Kit / CameraX 1.6.0 "Could not resolve" error — verify Plan 02 catalog edits landed correctly via `grep camerax = gradle/libs.versions.toml` and re-check the 8 CameraX + 1 ML Kit + 1 Timber aliases were added.

If `testDebugUnitTest` fails — the specific failing test reveals a Phase 2 regression; address inline before proceeding.
  </action>
  <verify>
    <automated>./gradlew clean :app:assembleDebug :app:testDebugUnitTest :app:lintDebug &amp;&amp; test -f app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew clean` exits 0
    - `./gradlew :app:assembleDebug` exits 0; writes APK to `app/build/outputs/apk/debug/app-debug.apk`; APK file size >= 1MB (sanity — actual ~40-50MB)
    - `./gradlew :app:testDebugUnitTest` exits 0; test report at `app/build/reports/tests/testDebugUnitTest/index.html` shows 10/10 tests passing (9 Phase 2 Nyquist + 1 Phase 1 example)
    - `./gradlew :app:lintDebug` exits 0; no new Phase 2 lint errors (existing Phase 1 warnings unchanged)
    - APK's `AndroidManifest.xml` (inside the APK, extracted via `aapt dump xmltree` or inspected) contains `android:screenOrientation="portrait"` on MainActivity (optional spot-check; Plan 02 acceptance already verified source)
  </acceptance_criteria>
  <done>Clean debug APK assembled, 10/10 unit tests GREEN, lint clean for Phase 2 code; APK ready for device install.</done>
</task>

<task type="auto">
  <name>Task 2: Produce 02-HANDOFF.md with 12-step Xiaomi 13T device runbook</name>
  <files>.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md</files>
  <read_first>
    - .planning/phases/01-foundation-skeleton/01-04-HANDOFF.md (full file — authoritative format template; structure/tone/step numbering convention to preserve)
    - .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-CONTEXT.md D-28 (exit artifact spec — 12 concrete steps enumerated)
    - .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-VALIDATION.md §Manual-Only Verifications (maps each CAM-XX to exact test instructions)
    - .planning/research/PITFALLS.md §1 (coord-space), §7 (Xiaomi MIUI), §13 (multi-face contour) — each must appear as expected-behavior/caveat callouts
  </read_first>
  <action>
Create `.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md` with this EXACT content (12 steps plus preamble + sign-off). Preserve the formatting / code-fencing style from Phase 1's HANDOFF:

````markdown
# Phase 2 Handoff — Camera Preview + Face Detection + Coordinate Validation Device Runbook

**Device:** Xiaomi 13T (HyperOS / MIUI)
**APK:** `app/build/outputs/apk/debug/app-debug.apk` (produced by Plan 06 Task 1)

**Prerequisites (user-side):**
1. Physical Xiaomi 13T with USB debugging enabled.
   - Settings → About phone → tap MIUI version 7 times (enables Developer Options)
   - Settings → Additional settings → Developer options → USB debugging: ON
   - Also in Developer options: "Install via USB": ON; "USB debugging (Security settings)": ON (MIUI-specific)
2. USB cable connected between phone and this PC (data-capable cable, NOT charge-only).
3. When prompted on the phone, tap "Allow" for the RSA fingerprint dialog.
4. `adb` from Android SDK platform-tools accessible at `C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe` (Phase 1 validated).

**What you are verifying:**
- CAM-01: CameraXViewfinder renders live preview on CameraScreen.
- CAM-02: Front/back flip button works 10× without CameraInUseException (<500ms per flip).
- CAM-06: OverlayEffect bakes the red-rect debug overlay into preview AND into the saved MP4 (three-stream compositing validated end-to-end).
- CAM-07: Sensor-to-buffer transform pairing holds across 4 device rotations × 2 lenses on Xiaomi 13T HyperOS.
- CAM-08: ML Kit trackingId remains stable for the same face across 60+ consecutive frames.

**Known expected findings (NOT bugs — flag only if different):**
- PITFALLS #13 — secondary faces (when two people are in frame) show boundingBox + landmarks but NOT contour dots. ML Kit documented behavior: contour populates only for primary face.
- Black letterbox bars on top/bottom of preview (D-10/D-11 — FIT_CENTER scale type). FILL_CENTER ships in Phase 6.
- Any Xiaomi MIUI / HyperOS OEM quirk surfaces here as a Phase 7 cross-OEM matrix item; **do not attempt to fix during Phase 2**. Document it under step 12 and move on.

***

## Step 1 — Confirm the APK exists

```bash
ls -la app/build/outputs/apk/debug/app-debug.apk
```

Expected: file listed, size 40-50 MB (debug unminified — CameraX + ML Kit + Compose + Hilt).

If missing: re-run `./gradlew :app:assembleDebug` (Plan 06 Task 1).

***

## Step 2 — Confirm the phone is detected

```bash
"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" devices
```

Expected output:
```
List of devices attached
<xiaomi-13t-serial>    device
```

If the status is `unauthorized`, tap "Allow" on the phone's RSA fingerprint dialog and re-run.
If `offline` — unplug/replug USB; try different port; toggle USB debugging OFF then ON.

***

## Step 3 — Install the APK

```bash
"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" install -r app/build/outputs/apk/debug/app-debug.apk
```

Expected output: `Performing Streamed Install ... Success`.

If `INSTALL_FAILED_UPDATE_INCOMPATIBLE`:
```bash
"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" uninstall com.bugzz.filter.camera
```
Then re-run the install.

If `INSTALL_FAILED_INSUFFICIENT_STORAGE`: free ~150MB on the device.

If MIUI pops up an "Install unknown apps" blocker: enable for "File Manager" or the transfer source, retry.

***

## Step 4 — Launch the app

```bash
"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" shell am start -n com.bugzz.filter.camera/.MainActivity
```

Expected: app opens on Xiaomi 13T landing on Splash screen.

In parallel, start logcat capture in a second terminal:
```bash
"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" logcat -c
"C:/Users/Admin/AppData/Local/Android/Sdk/platform-tools/adb.exe" logcat > phase2-run.log
```

Leave logcat running for steps 6-11. Stop with Ctrl-C when finished.

***

## Step 5 — Grant CAMERA permission

On the phone:
1. Tap "Continue" on Splash to advance to Home.
2. Tap "Open Camera" on Home. The CAMERA permission dialog appears.
3. Tap "While using the app" (or "Allow"). The permission is persisted; subsequent launches won't re-prompt.

Expected: after tapping Allow, the CameraScreen transitions from the permission rationale to the live camera preview.

If you accidentally tap "Deny":
- CameraScreen shows "Camera permission needed" text + "Grant permission" button + "Open Settings" button.
- Tap "Grant permission" to re-prompt (works once; second denial on MIUI forces the Settings route).
- Or tap "Open Settings" → Permissions → Camera → Allow — return to app.

Verify: NO RECORD_AUDIO prompt appears in Phase 2 (D-26 enforced). If you see an audio prompt, stop and file a bug — the build is leaking Phase 5 scope into Phase 2.

***

## Step 6 — Verify live preview renders (CAM-01)

Expected:
- Live camera preview fills the screen (FIT_CENTER scale type — black bars top + bottom are expected, not a bug; D-10).
- Front camera is active by default (D-24 — selfie-first).
- Your face appears in the preview, properly oriented.
- A flip icon (Cameraswitch) is visible in the top-right corner.
- A `TEST RECORD 5s` button is visible centered at the bottom (debug-only — D-04).

If preview is black / frozen / crashing: grep your `phase2-run.log` for `FATAL EXCEPTION` or `CameraUnavailableException`; most likely the MIUI camera service is claimed by another app — close all camera-using apps (background or running) and relaunch Bugzz.

✅ If the above matches: CAM-01 PASS.

***

## Step 7 — Verify lens flip works 10× with no CameraInUseException (CAM-02)

Tap the top-right flip icon 10 times in a row, waiting ~500ms between taps. Each tap should swap front↔back camera in <500ms with no black frame holdover longer than ~200ms.

Then in the logcat terminal, stop the capture (Ctrl-C), and grep:
```bash
grep "CameraInUseException\|Camera in use" phase2-run.log
```

Expected: **zero matches**. If one match appears — flag as OEM quirk finding for step 12; may indicate MIUI camera session teardown is slower than CameraX expects. Do NOT attempt a fix; this is a Phase 7 cross-OEM matrix item (as per 02-CONTEXT.md D-09 / PITFALLS #7 acceptance).

Also grep for:
```bash
grep "FaceTracker" phase2-run.log | head -20
```
Expected: Timber verbose lines showing `t=..., id=..., bb=..., contours=...` — confirms debug Timber.DebugTree is planted (Plan 02) and FaceDetectorClient is emitting logs (Plan 03).

✅ If zero `CameraInUseException` AND `FaceTracker` log lines present: CAM-02 PASS.

Restart logcat capture for the remaining steps: `logcat -c; logcat > phase2-run.log`.

***

## Step 8 — Verify red rect + landmark dots wrap face in portrait (front cam)

In the app with front camera live:

Expected:
- A RED STROKED RECTANGLE tightly wraps your face (bounding box).
- ORANGE-RED FILLED CIRCLES appear along your face contour — along the jawline, down the nose bridge, around the eyes, on the cheeks, along the upper/lower lips.
- Both the rectangle and the dots track your face as you move — slight lag is expected (1-2 frames). NO visible jitter on a still head (CAM-09 1€ filter is smoothing).
- When you leave the frame entirely: BOTH the rectangle AND the dots DISAPPEAR immediately. No last-frame ghost.

If the rectangle appears but in the wrong place (e.g., top-left corner of screen when your face is center): this is PITFALLS #1 — coord-space chaos. Likely cause: `COORDINATE_SYSTEM_SENSOR` + `canvas.setMatrix(frame.sensorToBufferTransform)` pairing broke. Stop and report — this is a Phase 2 blocker.

If the rectangle wraps the face but landmark dots are missing: acceptable only if `contours.size` in your FaceTracker logs is consistently 0 (that would indicate CONTOUR_MODE_ALL wasn't set — Plan 03 regression). If contours count is > 0 but no dots draw: DebugOverlayRenderer BuildConfig.DEBUG gate may have evaluated false (shouldn't happen on debug APK).

✅ If rectangle + dots track face smoothly on still head + disappear on no-face: CAM-01/CAM-09 front-lens PASS.

***

## Step 9 — Verify alignment across 4 rotations × 2 lenses (CAM-07)

For EACH of the 8 combinations below: hold the phone in that orientation (physically rotate the device — UI stays portrait-locked, that is correct), observe that the red rectangle + landmark dots remain pixel-aligned to your face, no 90° jump, no mirror flip offset.

| # | Lens | Device orientation | Expected |
|---|------|--------------------|----------|
| 1 | FRONT | Portrait (speaker up) | red rect aligned |
| 2 | FRONT | Landscape (speaker right) | red rect aligned |
| 3 | FRONT | Reverse-portrait (speaker down) | red rect aligned |
| 4 | FRONT | Reverse-landscape (speaker left) | red rect aligned |
| 5 | BACK  | Portrait (tap flip to back first) | red rect aligned |
| 6 | BACK  | Landscape | red rect aligned |
| 7 | BACK  | Reverse-portrait | red rect aligned |
| 8 | BACK  | Reverse-landscape | red rect aligned |

Between 4 and 5: tap the flip button — the phone stays in its current orientation, and the overlay should remain aligned after the flip completes.

If any of the 8 configurations misaligns by more than ~5 pixels: PITFALLS #1 or #7 — likely Xiaomi MIUI OrientationEventListener threshold issue. Capture which configuration(s) fail and record in step 12 HANDOFF notes; **do NOT attempt a workaround** — Phase 7 handles OEM-specific fixes.

✅ If all 8 pass: CAM-07 PASS.

***

## Step 10 — Verify trackingId stability 60+ frames (CAM-08)

With one face in view (yours), keep still for ~3 seconds. Then stop the logcat capture (Ctrl-C) and:

```bash
grep "FaceTracker" phase2-run.log | tail -120
```

Look at the `id=<number>` field across the last 60-120 lines. Expected: the same `id` value repeats across all the lines from the time your face appeared to when you stopped logcat.

Example passing output (id=3 stable across all frames):
```
V FaceTracker: t=12345..678 id=3 bb=360,640 contours=9
V FaceTracker: t=12345..967 id=3 bb=362,639 contours=9
V FaceTracker: t=12346..255 id=3 bb=361,641 contours=9
...
```

If the `id` churns (value jumps every few frames) while your face stayed still: `enableTracking()` is likely not active or is resetting internal state. Regression in Plan 03's `FaceDetectorClient.buildOptions()` — check unit test `FaceDetectorOptionsTest.isTrackingEnabled` is still GREEN.

✅ If single stable `id` across 60+ frames: CAM-08 PASS.

***

## Step 11 — Verify TEST RECORD bakes overlay into saved MP4 (CAM-06 end-to-end)

In the app:
1. Ensure a face is visible in the preview (your face — front cam).
2. Tap the bottom `TEST RECORD 5s` button. Label changes to `REC...`.
3. Wait. At 5 seconds the recording auto-stops; a toast appears: `Saved: content://...video_id=.../...` (the MediaStore URI).
4. Label reverts to `TEST RECORD 5s`. Record the toast URI.

Open Google Photos (or the MIUI Gallery) on the phone. The newest video under `DCIM/Bugzz/` should be named `bugzz_test_<timestamp>.mp4`. Play it.

Expected during MP4 playback:
- The red rectangle and orange-red landmark dots are VISIBLE throughout the 5 seconds, tracking your face in the recorded video (not just preview).
- NO audio track (D-05 — `.withAudioEnabled()` was NOT called).
- Video resolution ~HD (QualitySelector.from(Quality.HD)).

If the MP4 plays but the red rectangle is MISSING (video looks "clean"): PITFALLS #2 — overlay target mask does not cover `CameraEffect.VIDEO_CAPTURE`. Regression — check OverlayEffectBuilderTest (Plan 01) is still GREEN and `OverlayEffectBuilder.TARGETS` includes `CameraEffect.VIDEO_CAPTURE`.

If the MP4 fails to save / no toast appears: check logcat for `CameraController` error lines; likely MediaStore insert permission issue (shouldn't happen on debug APK + MIUI since DCIM/ is always writable).

✅ If MP4 plays back with red rect + dots baked into every frame: CAM-06 end-to-end PASS.

***

## Step 12 — Sign-off and record results

Fill in this block in the file (replace TBD with PASS / FAIL + notes):

```markdown
## PHASE 2 SIGN-OFF (verified <YYYY-MM-DD> on Xiaomi 13T HyperOS <version>)

- [ ] Step 1: APK exists — TBD
- [ ] Step 2: Device detected via adb — TBD
- [ ] Step 3: APK installed — TBD
- [ ] Step 4: App launched + logcat capturing — TBD
- [ ] Step 5: CAMERA permission granted; NO RECORD_AUDIO prompt seen — TBD
- [ ] Step 6 / CAM-01: Live preview renders — TBD
- [ ] Step 7 / CAM-02: 10× flip, zero CameraInUseException — TBD
- [ ] Step 8 / CAM-01+09: Red rect + dots track still head, no ghost on leave — TBD
- [ ] Step 9 / CAM-07: Alignment holds across 4 rotations × 2 lenses — TBD (fill in which if any fail)
- [ ] Step 10 / CAM-08: trackingId stable 60+ frames — TBD
- [ ] Step 11 / CAM-06: TEST RECORD MP4 has red rect baked in — TBD

### OEM Quirks Observed (if any)
TBD — record any Xiaomi MIUI / HyperOS specific misbehavior here as a Phase 7 item.

### Result
[ ] 12/12 PASS → Phase 2 complete. Proceed to `/gsd-plan-phase 3`.
[ ] Partial PASS → file blockers in STATE.md Blockers section; do NOT advance to Phase 3.
```

After filling in PASS/FAIL:
1. Commit `02-HANDOFF.md` with the user's sign-off block populated: `docs(02-06): phase 2 sign-off — N/12 PASS on Xiaomi 13T`.
2. If 12/12 PASS: update `.planning/STATE.md` Phase 2 row to `[x]` and advance `completed_phases` counter.
3. If partial PASS: update STATE.md Blockers section with the failing step(s) and specific device/config; do NOT mark Phase 2 complete.

***

*Runbook for Phase 2. Analogous to Phase 1's `01-04-HANDOFF.md` (format reference).*
*Phase 2 acceptance gate: 12/12 PASS on Xiaomi 13T.*
````

Do NOT promise compensation for MIUI-specific quirks — those are deferred to Phase 7 per D-09 / PITFALLS #7. Do NOT include Phase 3 work (photo capture, production filter) in this runbook — it's Phase 2 scope only.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md &amp;&amp; grep -q "Step 1 — Confirm the APK exists" .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md &amp;&amp; grep -q "Step 11 — Verify TEST RECORD" .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md &amp;&amp; grep -q "PHASE 2 SIGN-OFF" .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md` exists
    - Contains a Prerequisites section matching Phase 1 format (Dev Options, USB debug, cable, RSA allow)
    - Contains "What you are verifying:" section with CAM-01, CAM-02, CAM-06, CAM-07, CAM-08 explicitly enumerated
    - Contains all 12 numbered step headings (`## Step 1 — ...` through `## Step 12 — ...`)
    - Step 3 contains `adb install -r app/build/outputs/apk/debug/app-debug.apk`
    - Step 4 contains `adb shell am start -n com.bugzz.filter.camera/.MainActivity`
    - Step 5 explicitly notes "NO RECORD_AUDIO prompt" check (D-26 enforcement verification)
    - Step 7 contains `grep "CameraInUseException` command
    - Step 9 contains the 4-rotation × 2-lens matrix table with 8 rows
    - Step 10 contains `grep "FaceTracker"` command
    - Step 11 contains `DCIM/Bugzz/bugzz_test_` (MediaStore save path pattern)
    - Contains a "PHASE 2 SIGN-OFF" block with 12 checkbox rows for steps 1-11 (step 12 IS the sign-off)
    - Contains an "OEM Quirks Observed" section referencing Phase 7 deferral (PITFALLS #7 / D-09)
    - Does NOT contain Phase 3 scope (no mention of production filter / photo capture / filter catalog)
  </acceptance_criteria>
  <done>02-HANDOFF.md exists with full 12-step Xiaomi 13T runbook + sign-off block; matches Phase 1 format; CAM-01/02/06/07/08 all have specific manual-verification steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User executes runbook on Xiaomi 13T and reports N/12 PASS</name>
  <what-built>Phase 2 debug APK + 12-step device runbook (02-HANDOFF.md). Everything from Plan 01-05 is wired; this is the on-device validation of CAM-01, CAM-02, CAM-06, CAM-07, CAM-08 — the five manually-verified success criteria that unit tests cannot cover.</what-built>
  <how-to-verify>
    Read `.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md`. Execute steps 1-11 in order on Xiaomi 13T. For each step, record PASS / FAIL in the Step 12 sign-off block.

    **Critical verification points:**
    - Step 7 / CAM-02: 10× flip must produce ZERO `CameraInUseException` in logcat. If ≥1 appears, file as Xiaomi MIUI OEM quirk for Phase 7 — this is NOT a Phase 2 blocker unless every single flip fails.
    - Step 9 / CAM-07: ALL 8 rotation × lens combinations must show aligned overlay. ANY alignment failure is a Phase 2 blocker (most likely a coord-transform regression in OverlayEffectBuilder.build() setOnDrawListener).
    - Step 11 / CAM-06: The saved MP4 MUST show the red rect + landmark dots during playback in Google Photos. If absent, OverlayEffect target mask is wrong — block immediately, do NOT advance.

    **Expected findings (not blockers):**
    - PITFALLS #13: secondary face (if you get someone else in frame) shows boundingBox but no contour dots — documented behavior.
    - Black letterbox bars top/bottom from FIT_CENTER preview — documented behavior (D-10).
    - Minor ~1-2 frame lag on overlay after fast head motion — expected; 1€ filter tuning is Phase 3.

    **On 12/12 PASS:**
    1. Fill in the sign-off block in 02-HANDOFF.md with PASS + your device model / HyperOS version.
    2. Commit: `docs(02-06): phase 2 sign-off — 12/12 PASS on Xiaomi 13T`.
    3. Update `.planning/STATE.md`: Phase 2 row → `[x]` + increment `completed_phases: 2`.
    4. Type "approved" to this checkpoint.

    **On partial PASS (< 12/12):**
    1. Fill in the sign-off block with PASS/FAIL per step + notes on what failed.
    2. Update `.planning/STATE.md` Blockers section listing the failing step(s) and which requirement ID they map to.
    3. Describe the failure here (the checker will create a gap-closure plan via `/gsd-plan-phase 2 --gaps`).
  </how-to-verify>
  <resume-signal>Type "approved" with step-by-step PASS/FAIL results, or describe the failing step(s) for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PC gradle build → debug APK artifact | APK goes to user's physical device; no signing key exposure (debug signing key is Android Studio debug.keystore — unversioned, per Phase 1 gitignore) |
| PC adb → Xiaomi 13T USB | RSA trust established in Phase 1 — same device, no new trust boundary |
| Xiaomi 13T MediaStore → DCIM/Bugzz/ | TEST RECORD writes MP4 containing biometric face data to shared storage — documented in runbook step 11 so developer sees the file and can delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | I (Information Disclosure) | DCIM/Bugzz/bugzz_test_*.mp4 left on device after runbook execution contains face biometric data | accept (runbook discloses) | Runbook step 11 + step 12 instruct the developer to inspect the file; file is on developer-owned device under developer-accessible MediaStore path; developer can delete at any time via file manager. Not distributed externally. |
| T-02-09 | T (Tampering) | Developer skips step 9 (rotation × lens matrix) and proceeds to Phase 3 without CAM-07 validation | mitigate | Step 12 sign-off block requires explicit PASS/FAIL per step — if step 9 is left as `TBD`, the gate is unmet and STATE.md Phase 2 row MUST stay `[ ]`. GSD orchestrator (execute-phase) checks STATE.md before chaining phases. |
| T-02-10 | D (Denial of Service) | User tries to run runbook without Xiaomi 13T available | transfer | User-owned device; if unavailable, Phase 2 gate blocks until device is. Phase 2 `user_setup` frontmatter declares Xiaomi 13T requirement. No alternative device path in Phase 2 (reserved for Phase 7 multi-OEM matrix). |
</threat_model>

<verification>
After all three tasks:
- APK artifact at `app/build/outputs/apk/debug/app-debug.apk`.
- `02-HANDOFF.md` at `.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-HANDOFF.md` with 12 steps.
- User has executed the runbook and reported N/12 PASS via the checkpoint.
- If 12/12: STATE.md Phase 2 row = `[x]`; ready for `/gsd-plan-phase 3`.
- If partial: STATE.md Blockers populated; `/gsd-plan-phase 2 --gaps` creates a gap-closure plan (future Wave 6+).
- Commit messages:
  - `build(02-06): clean debug APK + unit tests green` (Task 1)
  - `docs(02-06): add 02-HANDOFF.md Xiaomi 13T device runbook` (Task 2)
  - `docs(02-06): phase 2 sign-off — N/12 PASS on Xiaomi 13T` (Task 3 after user verifies)
</verification>

<success_criteria>
- Clean debug APK exists with zero new warnings; 10/10 unit tests GREEN
- 02-HANDOFF.md exists with 12-step Xiaomi 13T runbook
- All CAM-01..CAM-09 requirements have been verified:
  - CAM-01 (manual, Step 6)
  - CAM-02 (manual, Step 7) + unit test (Plan 04 CameraControllerTest 4-use-case contract)
  - CAM-03 (unit test, Plan 04)
  - CAM-04 (unit test, Plan 03)
  - CAM-05 (unit test, Plan 04)
  - CAM-06 (manual, Step 11) + unit test (Plan 04 OverlayEffectBuilderTest targets)
  - CAM-07 (manual, Step 9)
  - CAM-08 (manual, Step 10 logcat grep)
  - CAM-09 (unit test, Plan 03 OneEuroFilterTest)
- User has signed off with 12/12 PASS OR filed specific failing steps for gap closure
- STATE.md reflects current phase completion status honestly — no rubber-stamp
</success_criteria>

<output>
After completion, create `.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-06-SUMMARY.md` documenting:
- Clean build results (APK path + size; unit test count; lint result)
- 02-HANDOFF.md path + 12-step outline
- User sign-off outcome (N/12 PASS; any OEM quirks recorded)
- STATE.md update performed (Phase 2 row transitioned from `[ ]` to `[x]` if 12/12)
- What advances to Phase 3 (first filter end-to-end + photo capture + MediaStore integration)
</output>
