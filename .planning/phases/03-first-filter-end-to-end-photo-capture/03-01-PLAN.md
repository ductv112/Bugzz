---
phase: 03-first-filter-end-to-end-photo-capture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/com/bugzz/filter/camera/detector/BboxIouTrackerTest.kt
  - app/src/test/java/com/bugzz/filter/camera/detector/FaceLandmarkMapperTest.kt
  - app/src/test/java/com/bugzz/filter/camera/detector/FaceDetectorClientTest.kt
  - app/src/test/java/com/bugzz/filter/camera/detector/LandmarkSmootherTest.kt
  - app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogTest.kt
  - app/src/test/java/com/bugzz/filter/camera/filter/AssetLoaderTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/BugBehaviorTest.kt
  - app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt
autonomous: true
requirements: []
threat_refs: [T-03-02, T-03-04, T-03-05]

must_haves:
  truths:
    - "8 new test files + 1 extended test file exist in app/src/test/java/com/bugzz/filter/camera/"
    - "All new Wave 0 test files compile in their own sourceset (guarded by stubs for unlanded SUTs where needed)"
    - "Every REN-01..07, CAP-01/03, and ADR-01 #1..3 behavior has at least one RED @Test"
    - "`./gradlew :app:testDebugUnitTest` exits 0 with all Wave 0 tests either RED-failing for unlanded SUTs OR @Ignore'd with TODO-Plan-03-0X KDoc pointing to the wave that lands the SUT"
    - "Phase 2's 10 pre-existing GREEN tests (4 OneEuroFilter + 1 FaceDetectorOptions + 2 OverlayEffectBuilder + 2 CameraController + 1 placeholder) remain GREEN after Wave 0"
  artifacts:
    - path: "app/src/test/java/com/bugzz/filter/camera/detector/BboxIouTrackerTest.kt"
      provides: "Nyquist tests for ADR-01 #1 — IoU math, greedy match, MAX_TRACKED_FACES=2 cap, MAX_DROPOUT_FRAMES=5 retention, monotonic nextId assignment"
      min_lines: 80
    - path: "app/src/test/java/com/bugzz/filter/camera/detector/FaceLandmarkMapperTest.kt"
      provides: "Nyquist tests for REN-03 — all 7 Anchor enum values resolve primary path + fallback + ultimate bbox fallback"
      min_lines: 80
    - path: "app/src/test/java/com/bugzz/filter/camera/detector/FaceDetectorClientTest.kt"
      provides: "Nyquist tests for ADR-01 #3 — createAnalyzer consumer passes faces through tracker before SmoothedFace mapping; SMOOTHED_CONTOUR_TYPES includes LEFT_EYEBROW_TOP + RIGHT_EYEBROW_TOP"
      min_lines: 60
    - path: "app/src/test/java/com/bugzz/filter/camera/detector/LandmarkSmootherTest.kt"
      provides: "Nyquist tests for ADR-01 #2 — onFaceLost(id) clears that id's filter state only; same-id reappear reinitializes fresh"
      min_lines: 40
    - path: "app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogTest.kt"
      provides: "Nyquist test for REN-03 — catalog holds exactly 2 entries (ant_on_nose_v1, spider_on_forehead_v1) with correct anchor + behavior=STATIC + scaleFactor"
      min_lines: 30
    - path: "app/src/test/java/com/bugzz/filter/camera/filter/AssetLoaderTest.kt"
      provides: "Nyquist tests for REN-04 — sizeOf returns allocationByteCount; cache cap = min(32MB, maxMemory/8); get returns null before preload, Bitmap after; manifest.json parse via kotlinx.serialization"
      min_lines: 70
    - path: "app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt"
      provides: "Nyquist tests for REN-01/05/06/07 — onDraw early-returns on null face, flipbook frame-index = (elapsed/frameDurationNanos) % frameCount, setFilter before preload skips drawing, canvas.setMatrix called before drawBitmap"
      min_lines: 100
    - path: "app/src/test/java/com/bugzz/filter/camera/render/BugBehaviorTest.kt"
      provides: "Nyquist tests for REN-02 — BugBehavior sealed interface has exactly 4 variants; Static.tick sets position=anchor + velocity=(0,0); Crawl/Swarm/Fall throw NotImplementedError"
      min_lines: 40
    - path: "app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt"
      provides: "Extended with capturePhoto_invokesTakePicture + capturePhoto_mediaStoreOutputOptions_hasCorrectRelativePath + capturePhoto_onError_emitsFailure — covers CAP-01 + CAP-03"
      contains: "capturePhoto"
  key_links:
    - from: "Wave 0 test files"
      to: "Wave 1 + Wave 2 + Wave 3 SUT files"
      via: "RED → GREEN transition when SUT lands"
      pattern: "RedBeforeGreen"
    - from: "BboxIouTrackerTest.kt"
      to: "BboxIouTracker.kt companion IOU_MATCH_THRESHOLD=0.3f, MAX_TRACKED_FACES=2, MAX_DROPOUT_FRAMES=5"
      via: "const val assertions via reflection or direct reference post-landing"
      pattern: "companion\\s+object"
---

<objective>
Land all Nyquist test scaffolds for Phase 3 BEFORE any production code. Every test file either (a) references a SUT that doesn't yet exist — compile-failing and resolved when Wave 1+ lands the SUT, or (b) uses minimal inline stubs to keep the testDebugUnitTest sourceset compilable, with `@Ignore("Plan 03-0X TODO")` markers pointing at the wave that flips RED→GREEN. This is the Phase 2 pattern (see 02-03-SUMMARY.md Rule 3 auto-fix: "OverlayEffectBuilderTest.kt + CameraControllerTest.kt committed by Plan 02-01 reference SUTs not landing until Plan 02-04, blocking the whole test sourceset from compiling. Shipped minimal placeholder SUTs to keep compile GREEN.").

Purpose: Nyquist gate — test files MUST commit before `feat()` commits implementing the SUT. This forces executors to write failing tests first, catches API-drift during impl, and gives us ~45s feedback latency per task commit in Wave 1+. Every Phase 3 requirement ID (REN-01..08 + CAP-01..06) — except REN-08/CAP-02/04/05/06 (manual-only per VALIDATION.md) — gets at least one RED test in this plan.

Output: 8 new test files + 1 extended test file. All in `app/src/test/java/com/bugzz/filter/camera/`. Zero production code changes — this plan is test-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-first-filter-end-to-end-photo-capture/03-CONTEXT.md
@.planning/phases/03-first-filter-end-to-end-photo-capture/03-RESEARCH.md
@.planning/phases/03-first-filter-end-to-end-photo-capture/03-VALIDATION.md
@.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-ADR-01-no-ml-kit-tracking-with-contour.md
@.planning/phases/02-camera-preview-face-detection-coordinate-validation/02-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing Phase 2 contracts the Wave 0 tests reference: -->
<!-- - com.bugzz.filter.camera.detector.SmoothedFace(trackingId: Int, boundingBox: Rect, contours: Map<Int, List<PointF>>, landmarks: Map<Int, PointF>) -->
<!-- - com.bugzz.filter.camera.detector.FaceSnapshot(faces: List<SmoothedFace>, timestampNanos: Long) + FaceSnapshot.EMPTY -->
<!-- - com.bugzz.filter.camera.detector.FaceLandmarkMapper.Anchor enum: NOSE_TIP, FOREHEAD, LEFT_CHEEK, RIGHT_CHEEK, CHIN, LEFT_EYE, RIGHT_EYE -->
<!-- - com.bugzz.filter.camera.detector.LandmarkSmoother(minCutoff, beta, dCutoff) with smoothPoint/retainActive/clear methods -->
<!-- - com.bugzz.filter.camera.detector.FaceDetectorClient(@Named("cameraExecutor") Executor) with createAnalyzer() + latestSnapshot -->
<!-- - com.bugzz.filter.camera.camera.CameraController internal constructor (primary) + @Inject constructor (secondary) — Phase 2 D-14 split pattern -->
<!-- - com.google.mlkit.vision.face.Face (mockable via mockito-kotlin); face.boundingBox: android.graphics.Rect -->

<!-- Phase 3 NEW types referenced by Wave 0 tests (SUTs land in Wave 1/2/3): -->
<!-- - com.bugzz.filter.camera.detector.BboxIouTracker — @Singleton @Inject; fun assign(List<Face>): TrackerResult; data class TrackerResult(tracked: List<TrackedFace>, removedIds: List<Int>); data class TrackedFace(id: Int, face: Face); companion IOU_MATCH_THRESHOLD=0.3f / MAX_TRACKED_FACES=2 / MAX_DROPOUT_FRAMES=5 -->
<!-- - com.bugzz.filter.camera.filter.FilterDefinition(id, displayName, anchorType, behavior, frameCount, frameDurationMs, scaleFactor, assetDir, mirrorable) -->
<!-- - com.bugzz.filter.camera.filter.FilterCatalog — object with `val all: List<FilterDefinition>`, `fun byId(id: String): FilterDefinition?` -->
<!-- - com.bugzz.filter.camera.filter.AssetLoader(@ApplicationContext, @Named("cameraExecutor") Executor); suspend fun preload(filterId); fun get(filterId, frameIdx): Bitmap?; private cache capacity via companion -->
<!-- - com.bugzz.filter.camera.filter.SpriteManifest(@Serializable): id, displayName, frameCount, frameDurationMs, anchorType, behavior, scaleFactor, mirrorable=true, bitmapConfig=null -->
<!-- - com.bugzz.filter.camera.render.FilterEngine(@Singleton @Inject(assetLoader)): setFilter(FilterDefinition?) + onDraw(Canvas, OverlayEffect.Frame, SmoothedFace?) -->
<!-- - com.bugzz.filter.camera.render.BugBehavior sealed interface: fun tick(BugState, SmoothedFace, PointF, Long). Variants: Static (impl), Crawl/Swarm/Fall (TODO("Phase 4")) -->
<!-- - com.bugzz.filter.camera.render.BugState(position, velocity, pathProgress, lastFrameIndex, lastFrameAdvanceTimestampNanos) — all vars defaults -->

<!-- Test framework already on classpath (libs.versions.toml verified): -->
<!-- - JUnit 4 4.13.2; mockito-core 5.11.0 + mockito-kotlin 5.2.1; Robolectric 4.13 -->
<!-- - kotlinx-serialization-json 1.8.0 (use for SpriteManifest parse tests) -->
<!-- - No new deps needed — RESEARCH.md §Environment Availability confirmed -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assets in APK → AssetLoader → Bitmap cache | User-controlled data only insofar as APK author controls assets; malformed sprite asset = self-DoS (covered by Wave 2 FilterEngine error path, Wave 0 tests pin the fallback contract) |
| ML Kit Face → BboxIouTracker → LandmarkSmoother | No external input — detector output feeds tracker; tests exercise the boundary with synthetic Face mocks |
| ImageCapture callback → MediaStore URI → ViewModel Channel | ImageCapture owns user-initiated photo write; Wave 0 tests pin that error path emits OneShotEvent.Error, not silent failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of Service (OOM) | AssetLoader (malformed sprite → BitmapFactory throws) | mitigate | Wave 0 test `AssetLoaderTest.preload_malformedPng_throwsIllegalArgument` pins that decode failure surfaces as exception (Wave 2 implementation wraps in try/catch + OneShotEvent.Error) |
| T-03-04 | Denial of Service (memory leak) | ImageCapture callback closure retains ViewModel | mitigate | Wave 0 test `CameraControllerTest.capturePhoto_onError_emitsFailure` covers the callback-failure path; Wave 4 HANDOFF runbook exercises 30-capture + LeakCanary verification (CAP-06) |
| T-03-05 | Information Disclosure (biometric logs) | FilterEngine Timber calls must not log SmoothedFace contours | mitigate | Wave 0 test `FilterEngineTest.onDraw_doesNotLogLandmarkCoords` pins aggregate-only logging policy inherited from Phase 2 T-02-06; Wave 2 implementation MUST log only filter id + frame index, never PointF lists |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detector-layer Wave 0 tests (BboxIouTracker + LandmarkSmoother + FaceDetectorClient + FaceLandmarkMapper)</name>
  <files>
    app/src/test/java/com/bugzz/filter/camera/detector/BboxIouTrackerTest.kt,
    app/src/test/java/com/bugzz/filter/camera/detector/LandmarkSmootherTest.kt,
    app/src/test/java/com/bugzz/filter/camera/detector/FaceDetectorClientTest.kt,
    app/src/test/java/com/bugzz/filter/camera/detector/FaceLandmarkMapperTest.kt
  </files>
  <read_first>
    app/src/main/java/com/bugzz/filter/camera/detector/FaceDetectorClient.kt,
    app/src/main/java/com/bugzz/filter/camera/detector/OneEuroFilter.kt,
    app/src/main/java/com/bugzz/filter/camera/detector/FaceLandmarkMapper.kt,
    app/src/main/java/com/bugzz/filter/camera/detector/FaceSnapshot.kt,
    app/src/test/java/com/bugzz/filter/camera/detector/OneEuroFilterTest.kt,
    app/src/test/java/com/bugzz/filter/camera/detector/FaceDetectorOptionsTest.kt,
    .planning/phases/03-first-filter-end-to-end-photo-capture/03-RESEARCH.md (§Code Example 3, §Code Example 4),
    .planning/phases/02-camera-preview-face-detection-coordinate-validation/02-ADR-01-no-ml-kit-tracking-with-contour.md
  </read_first>
  <behavior>
    **BboxIouTrackerTest.kt** (ADR-01 #1 — pure JVM JUnit 4; mock `com.google.mlkit.vision.face.Face` via mockito-kotlin; stub `face.boundingBox` returning `android.graphics.Rect` — Robolectric runner needed ONLY if Rect lacks a unit-test shadow; prefer pure JVM first, flip to `@RunWith(RobolectricTestRunner::class) @Config(sdk = [34])` if Rect constructor throws):
      - Test 1 — `iou_identicalBoxes_returns1`: `BboxIouTracker.iou(Rect(0,0,100,100), Rect(0,0,100,100))` returns `1.0f ± 1e-6`
      - Test 2 — `iou_disjointBoxes_returns0`: `BboxIouTracker.iou(Rect(0,0,100,100), Rect(200,200,300,300))` returns `0.0f`
      - Test 3 — `iou_halfOverlap_returnsOneThird`: `BboxIouTracker.iou(Rect(0,0,100,100), Rect(50,0,150,100))` returns `≈0.333f` (intersection=50×100=5000, union=100×100+100×100−5000=15000, IoU=1/3)
      - Test 4 — `assign_firstFrame_assignsMonotonicNewIds`: tracker with empty state; input 2 faces with non-overlapping bboxes → result.tracked.size=2, IDs are 0 and 1 (monotonic from nextId=0); result.removedIds.isEmpty()
      - Test 5 — `assign_sameFaceNextFrame_retainsSameId`: frame1: 1 face at Rect(100,100,300,300) → id=0. frame2: same face at Rect(105,105,305,305) (IoU ≈ 0.90) → id=0 again
      - Test 6 — `assign_threeFaces_capsAtMaxTrackedFaces`: input 3 faces with different bboxes; tracker returns only 2 (the 2 largest by area); verify tracker.MAX_TRACKED_FACES == 2 via reflection or direct reference
      - Test 7 — `assign_faceDisappears_dropsAfterFiveFrames`: frame1: 1 face → id=0. frame2..frame6: empty faces list (framesSinceLastSeen increments 1→5, still tracked). frame7 (framesSinceLastSeen=6 > 5): entry removed. Verify result.removedIds on the removal frame contains [0]
      - Test 8 — `assign_faceReappearsAsDifferent_assignsNewId`: frame1: face at Rect(100,100,200,200) → id=0. frame2..frame7 empty (id=0 removed after 5 dropouts). frame8: face at Rect(100,100,200,200) again → id=1 (monotonic; old ID not recycled per D-23)
      - Test 9 — `companion_constantsMatchSpec`: assert `BboxIouTracker.IOU_MATCH_THRESHOLD == 0.3f`, `MAX_DROPOUT_FRAMES == 5`, `MAX_TRACKED_FACES == 2` via direct reference
      - Test 10 — `assign_lowIouNewFace_assignsNewId`: frame1: face at Rect(0,0,100,100) → id=0. frame2: face at Rect(500,500,600,600) (IoU with id=0's bbox is 0, below 0.3 threshold) → new id=1 AND id=0 enters dropout retention

    **LandmarkSmootherTest.kt** (ADR-01 #2 — pure JVM):
      - Test 1 — `onFaceLost_clearsThatIdOnly_otherIdsSurvive`: smoother with state for id=5 and id=9 (seeded by calling `smoothPoint(5, "nose", 10f, 10f, 1000L)` + `smoothPoint(9, "nose", 20f, 20f, 1000L)`). Call `smoother.onFaceLost(5)`. Reflect or re-invoke `smoothPoint(5, "nose", 100f, 100f, 2000L)` — this should behave as first-call (returns x=100, y=100 un-smoothed, since 1€ is uninitialized). `smoothPoint(9, ...)` should continue with prior state (seeded value dominates; delta small)
      - Test 2 — `sameIdReappears_startsFreshState`: seed id=3 at t=1000L. Call `onFaceLost(3)`. Call `smoothPoint(3, "nose", 100f, 100f, 2000L)` — verify returned value == 100f exactly (proof of fresh OneEuroFilter initialization)
      - Test 3 — `onFaceLost_unknownId_isNoOp`: smoother with no state for id=42; `smoother.onFaceLost(42)` does not throw; subsequent `smoothPoint(42, ...)` starts fresh

    **FaceDetectorClientTest.kt** (ADR-01 #3 — Robolectric needed for ML Kit FaceDetection.getClient internals on JVM; pattern = same `@RunWith(RobolectricTestRunner::class) @Config(sdk = [34])` as CameraControllerTest). MAY need to @Ignore these tests if getClient trips Robolectric shadows; label with "TODO Plan 03-02 unignore once tracker wired" and pin the contract:
      - Test 1 — `buildOptions_excludesEnableTracking_contourModeAll`: inherit from Phase 2 FaceDetectorOptionsTest invariant — assert `toString()` shows `trackingEnabled=false` (ADR-01 enforcement)
      - Test 2 — `smoothedContourTypes_includesForeheadEyebrows`: assert `SMOOTHED_CONTOUR_TYPES` (via reflection on companion, or expose the list internal for tests) contains `FaceContour.LEFT_EYEBROW_TOP (=2)` and `FaceContour.RIGHT_EYEBROW_TOP (=4)` — needed for FaceLandmarkMapper.FOREHEAD resolution
      - Test 3 (@Ignore until Wave 1) — `createAnalyzer_passesFacesThroughTracker_beforeSmoothing`: construct FaceDetectorClient(cameraExecutor, tracker=mockBboxIouTracker); simulate MlKitAnalyzer consumer with mock faces; verify `tracker.assign(faces)` called with ordering = BEFORE `smoother.smoothPoint`. Mark with `@org.junit.Ignore("Plan 03-02 — flip to GREEN when tracker constructor param lands")`

    **FaceLandmarkMapperTest.kt** (REN-03 — Robolectric for `android.graphics.PointF` + `android.graphics.Rect` shadows; pattern = same as `DebugOverlayRendererTest`):
      - Test 1 — `noseTip_fromNoseBridge_returnsLastPoint`: SmoothedFace with `contours = mapOf(FaceContour.NOSE_BRIDGE to listOf(PointF(10f,10f), PointF(20f,20f), PointF(30f,40f)))` → `anchorPoint(face, NOSE_TIP)` returns PointF(30f, 40f)
      - Test 2 — `noseTip_fallbackToNoseBottom`: NOSE_BRIDGE empty; NOSE_BOTTOM has 2 points → returns first NOSE_BOTTOM point
      - Test 3 — `noseTip_ultimateFallback_bboxCenter`: contours empty map; bbox=Rect(0,0,100,100) → returns PointF(50f, 50f)
      - Test 4 — `forehead_fromEyebrowTops_withUpwardOffset`: LEFT_EYEBROW_TOP=[PointF(20f,30f)], RIGHT_EYEBROW_TOP=[PointF(80f,30f)]; bbox height=200 → expected mid.y = 30 - 200*0.15 = 0; returns PointF(50f, 0f)
      - Test 5 — `leftCheek_fromFaceContour_atFortyPercent`: FACE contour has exactly 10 points; idx = (10 * 0.40).toInt() = 4; returns contour[4]
      - Test 6 — `rightCheek_fromFaceContour_atSixtyPercent`: FACE contour 10 points; idx = (10 * 0.60).toInt() = 6; returns contour[6]
      - Test 7 — `chin_fromFaceContour_atFiftyPercent`: FACE contour 10 points; returns contour[5]
      - Test 8 — `leftEye_fromContourCentroid`: LEFT_EYE = 4 points; expected = mean; RIGHT_EYE independent
      - Test 9 — `allAnchors_neverReturnsNull_whenBboxPresent`: parameterized test across all 7 Anchor values with empty contours map + non-empty bbox → all return non-null
  </behavior>
  <action>
    Create 4 test files. For each file, use `package com.bugzz.filter.camera.detector` and JUnit 4 `@Test` annotations. Mock Face via `org.mockito.kotlin.mock<Face>()` + `stub { on { boundingBox } doReturn Rect(...) }`. For tests that reference SUTs not yet landed (BboxIouTracker, FaceDetectorClient tracker-param, LandmarkSmoother.onFaceLost), include the import and call the method — compilation will fail at the class-file level UNTIL Wave 1 lands the SUT. This is the intended RED state.

    To keep the test sourceset COMPILABLE in Wave 0 (Phase 2 02-03-SUMMARY.md Rule 3 auto-fix precedent), ship minimal placeholder stub `.kt` files under `app/src/main/java/com/bugzz/filter/camera/detector/`:
    - `BboxIouTracker.kt` — `@Singleton class BboxIouTracker @Inject constructor() { data class TrackedFace(val id: Int, val face: Face); data class TrackerResult(val tracked: List<TrackedFace>, val removedIds: List<Int>); fun assign(faces: List<Face>): TrackerResult = TODO("Plan 03-02"); companion object { const val IOU_MATCH_THRESHOLD = 0.0f; const val MAX_DROPOUT_FRAMES = 0; const val MAX_TRACKED_FACES = 0; internal fun iou(a: Rect, b: Rect): Float = TODO("Plan 03-02") } }`.
      The wrong constants (0.0f / 0 / 0) keep the `companion_constantsMatchSpec` test intentionally RED at runtime. KDoc: "STUB — Plan 03-02 REPLACES body + companion constants per D-21/22/23".
    - Add `fun onFaceLost(id: Int): Unit = TODO("Plan 03-02")` to existing `LandmarkSmoother` class in `OneEuroFilter.kt` (additive-only; Phase 2 smoothPoint / retainActive / clear methods unchanged).

    `./gradlew :app:testDebugUnitTest` then runs with Wave 0 tests RED-failing at `TODO("Plan 03-02")`. This is the Nyquist RED gate.
  </action>
  <acceptance_criteria>
    - File `app/src/test/java/com/bugzz/filter/camera/detector/BboxIouTrackerTest.kt` exists and contains `@Test fun iou_identicalBoxes_returns1`, `assign_sameFaceNextFrame_retainsSameId`, `assign_threeFaces_capsAtMaxTrackedFaces`, `companion_constantsMatchSpec`
    - File `app/src/test/java/com/bugzz/filter/camera/detector/LandmarkSmootherTest.kt` exists and contains `onFaceLost_clearsThatIdOnly_otherIdsSurvive` + `sameIdReappears_startsFreshState`
    - File `app/src/test/java/com/bugzz/filter/camera/detector/FaceDetectorClientTest.kt` exists; contains `buildOptions_excludesEnableTracking_contourModeAll` + `smoothedContourTypes_includesForeheadEyebrows`; the `createAnalyzer_passesFacesThroughTracker_beforeSmoothing` test is annotated `@org.junit.Ignore("Plan 03-02 — flip to GREEN when tracker constructor param lands")`
    - File `app/src/test/java/com/bugzz/filter/camera/detector/FaceLandmarkMapperTest.kt` exists and contains 9 @Test methods covering all 7 Anchor enum values
    - Placeholder file `app/src/main/java/com/bugzz/filter/camera/detector/BboxIouTracker.kt` exists with `@Singleton class BboxIouTracker @Inject constructor()` + companion object WITH wrong const values (0.0f / 0 / 0) + `TODO("Plan 03-02")` KDoc pointing to Wave 1
    - `app/src/main/java/com/bugzz/filter/camera/detector/OneEuroFilter.kt` now has `fun onFaceLost(id: Int): Unit = TODO("Plan 03-02")` inside `class LandmarkSmoother`
    - `./gradlew :app:testDebugUnitTest` completes — compile GREEN; tests referencing TODO-stubs fail at RED; Phase 2's pre-existing 10 tests remain GREEN
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*BboxIouTrackerTest*" --tests "*LandmarkSmootherTest*" --tests "*FaceDetectorClientTest*" --tests "*FaceLandmarkMapperTest*"</automated>
  </verify>
  <done>Compile GREEN; 4 new detector test files exist; Wave 1 target SUTs have placeholder stubs; Phase 2's 10 pre-existing tests still GREEN; Wave 0 new tests RED (failing on TODO/wrong-constants assertions as expected).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter + render + CameraController Wave 0 tests (FilterCatalog + AssetLoader + FilterEngine + BugBehavior + CameraController.capturePhoto)</name>
  <files>
    app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogTest.kt,
    app/src/test/java/com/bugzz/filter/camera/filter/AssetLoaderTest.kt,
    app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt,
    app/src/test/java/com/bugzz/filter/camera/render/BugBehaviorTest.kt,
    app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt
  </files>
  <read_first>
    app/src/main/java/com/bugzz/filter/camera/camera/CameraController.kt,
    app/src/main/java/com/bugzz/filter/camera/render/DebugOverlayRenderer.kt,
    app/src/main/java/com/bugzz/filter/camera/render/OverlayEffectBuilder.kt,
    app/src/main/java/com/bugzz/filter/camera/detector/FaceSnapshot.kt,
    app/src/main/java/com/bugzz/filter/camera/detector/FaceLandmarkMapper.kt,
    app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt,
    app/src/test/java/com/bugzz/filter/camera/render/DebugOverlayRendererTest.kt,
    .planning/phases/03-first-filter-end-to-end-photo-capture/03-RESEARCH.md (§Code Example 2, §Code Example 5, §Code Example 6, §Pitfall 2),
    .planning/phases/03-first-filter-end-to-end-photo-capture/03-CONTEXT.md (D-01, D-02, D-06, D-08, D-09, D-11, D-29, D-31, D-32)
  </read_first>
  <behavior>
    **FilterCatalogTest.kt** (REN-03 extended — pure JVM; relies on Wave 2 SUT but uses placeholder):
      - Test 1 — `catalog_hasExactlyTwoFilters`: `FilterCatalog.all.size == 2`
      - Test 2 — `catalog_byId_antOnNose_resolves`: `FilterCatalog.byId("ant_on_nose_v1")` non-null; anchorType == NOSE_TIP; behavior === BugBehavior.Static; scaleFactor == 0.20f; assetDir == "sprites/ant_on_nose_v1"
      - Test 3 — `catalog_byId_spiderOnForehead_resolves`: `FilterCatalog.byId("spider_on_forehead_v1")` non-null; anchorType == FOREHEAD; behavior === BugBehavior.Static
      - Test 4 — `catalog_byId_unknown_returnsNull`: `FilterCatalog.byId("nonexistent_filter_v99") == null`

    **AssetLoaderTest.kt** (REN-04 — Robolectric; inject synthetic AssetManager via `ApplicationProvider.getApplicationContext()`; decode tiny in-memory PNG fixtures placed under `app/src/test/resources/sprites/test_filter/frame_00.png` + manifest.json):
      - Test 1 — `sizeOf_returnsAllocationByteCount_notByteCount`: instantiate AssetLoader; reflect/test the private cache's sizeOf indirectly by decoding a bitmap and asserting `bitmap.allocationByteCount > 0` — OR expose `internal fun sizeOfForTest(bitmap: Bitmap): Int` as a test seam (prefer the test seam; document in KDoc "Test seam — do not invoke from prod")
      - Test 2 — `cacheSize_capsAt32MBOrMaxMemoryDivEight`: reflect companion `computeCacheSize()` (or invoke through a test seam); expect `result == minOf(32*1024*1024, (Runtime.getRuntime().maxMemory() / 8L).toInt())` and `result >= 8*1024*1024` floor
      - Test 3 — `get_beforePreload_returnsNull`: fresh AssetLoader; `get("test_filter", 0) == null`
      - Test 4 — `preload_populatesCache_getReturnsBitmap`: `runBlocking { loader.preload("test_filter") }`; `get("test_filter", 0) != null`; `.width > 0 && .height > 0`
      - Test 5 — `preload_idempotent_multipleCallsDoNotReDecode`: call preload twice; verify cache size unchanged (reflect size via test seam)
      - Test 6 — `preload_malformedPng_throwsIllegalArgument`: set up a sprite dir with a zero-byte PNG; `assertThrows<IllegalArgumentException> { runBlocking { loader.preload("bad_filter") } }`
      - Test 7 — `manifestJson_deserializesViaKotlinxSerialization`: put canonical manifest.json under test resources; parse via `Json.decodeFromString<SpriteManifest>(text)`; assert fields match (id, frameCount, frameDurationMs, anchorType="NOSE_TIP", behavior="STATIC", scaleFactor)

    **FilterEngineTest.kt** (REN-01/05/06/07 — Robolectric for `android.graphics.Canvas`/Paint; mock Canvas via mockito-kotlin):
      - Test 1 — `onDraw_nullFace_returnsEarly_neverCallsDrawBitmap`: FilterEngine with active filter set; `onDraw(mockCanvas, mockFrame, null)`; `verify(mockCanvas, never()).drawBitmap(any<Bitmap>(), any<Float>(), any<Float>(), any<Paint>())`
      - Test 2 — `onDraw_nullActiveFilter_returnsEarly`: FilterEngine without setFilter called; `onDraw(mockCanvas, mockFrame, mockFace)`; `verify(mockCanvas, never()).drawBitmap(...)`
      - Test 3 — `setFilter_beforePreload_onDrawSkipsDrawing`: `engine.setFilter(filterA)`; do NOT preload; `onDraw(mockCanvas, mockFrame, mockFace)` → `verify(canvas, never()).drawBitmap(...)` (D-11 no-ghost contract — assetLoader.get returns null)
      - Test 4 — `setFilter_afterPreload_onDrawCallsDrawBitmap`: preload assetLoader; `engine.setFilter(filterA)`; `onDraw(mockCanvas, mockFrame, mockFace)` → `verify(canvas).drawBitmap(any<Bitmap>(), any(), any(), any())` exactly once
      - Test 5 — `flipbookIndex_advancesOverTime`: with synthetic frameTimeNanos driven by test clock. filter.frameCount=3, frameDurationMs=100 (100ms/frame = 100_000_000 ns). Sequence: frame.timestamp = 0L (idx=0), 120_000_000L (idx=1), 220_000_000L (idx=2), 320_000_000L (idx=0 wrap). Assert `assetLoader.get(filterId, idx)` called with the correct idx each tick — verify via `argumentCaptor<Int>()`
      - Test 6 — `onDraw_callsCanvasSetMatrixBeforeDrawBitmap`: verify InOrder(canvas) { verify(canvas).setMatrix(any()); verify(canvas).drawBitmap(...) } — pins CAM-07 matrix-before-draw contract inherited from Phase 2
      - Test 7 — `onDraw_doesNotLogLandmarkCoords`: spy on Timber; invoke onDraw; assert no Timber call string CONTAINS "PointF" or "x=" digit-pair patterns (T-03-05 biometric log policy)
      - Test 8 — `setFilter_swap_resetsBugStateFrameIndex`: preload filterA, setFilter(A), onDraw (advances frame index to e.g. 2); setFilter(filterB); immediately onDraw — assert `lastFrameIndex == 0` for filterB (fresh state, no carry-over)

    **BugBehaviorTest.kt** (REN-02 — pure JVM):
      - Test 1 — `sealedInterface_hasExactlyFourVariants`: `BugBehavior::class.sealedSubclasses.size == 4` (Static, Crawl, Swarm, Fall)
      - Test 2 — `static_tick_setsPositionToAnchor`: BugState with position=(0,0); Static.tick(state, mockFace, anchor=PointF(50f, 75f), dtMs=16L); `state.position.x == 50f && state.position.y == 75f`
      - Test 3 — `static_tick_setsVelocityToZero`: pre-set `state.velocity=PointF(100f,100f)`; Static.tick(state, face, anchor, 16L); `state.velocity.x == 0f && state.velocity.y == 0f`
      - Test 4 — `crawl_tick_throwsNotImplementedError`: `assertThrows<NotImplementedError> { Crawl.tick(...) }`
      - Test 5 — `swarm_tick_throwsNotImplementedError`: same pattern
      - Test 6 — `fall_tick_throwsNotImplementedError`: same pattern

    **CameraControllerTest.kt extension** (CAP-01/03 — Robolectric already wired; append 3 new @Test methods):
      - Test `capturePhoto_invokesTakePicture_onBoundImageCapture`: bind() the controller; call `capturePhoto { }`; verify the internal `imageCapture.takePicture(outputOptions, executor, callback)` call happens — use a mock ImageCapture injected via a test seam (mirror the Phase 2 `providerFactory` split pattern: add an `internal` primary constructor param `imageCaptureFactory: () -> ImageCapture = { ImageCapture.Builder().build() }` in Wave 3; TEST intercepts the factory)
      - Test `capturePhoto_outputFileOptions_hasDCIMBugzzRelativePath`: capture the ContentValues built inside capturePhoto; assert `getAsString(MediaStore.Images.Media.RELATIVE_PATH) == "DCIM/Bugzz"`, `MIME_TYPE == "image/jpeg"`, `DISPLAY_NAME` matches regex `Bugzz_\d{8}_\d{6}\.jpg`
      - Test `capturePhoto_controllerNotBound_emitsFailure`: on a CameraController that has NOT been bind()'d (imageCapture == null); call `capturePhoto { result -> … }`; assert `result.isFailure && result.exceptionOrNull() is IllegalStateException`

    All CameraController extension tests MUST start `@org.junit.Ignore("Plan 03-04 — flip to GREEN when capturePhoto method lands")` UNTIL Wave 3 implements the method. The test bodies are fully written; only the @Ignore blocks them from failing Wave 0 CI.
  </behavior>
  <action>
    Create/extend 5 test files. Scaffold minimal placeholder SUT stubs in main/ to keep sourceset compile-GREEN:
    - `app/src/main/java/com/bugzz/filter/camera/filter/FilterDefinition.kt` — data class stub with `TODO("Plan 03-03")` factory; data class body ships full per D-29
    - `app/src/main/java/com/bugzz/filter/camera/filter/FilterCatalog.kt` — `object FilterCatalog { val all: List<FilterDefinition> = emptyList(); fun byId(id: String): FilterDefinition? = null }` — KDoc "STUB Plan 03-03 replaces"
    - `app/src/main/java/com/bugzz/filter/camera/filter/SpriteManifest.kt` — full @Serializable data class per RESEARCH §Example 6 (no TODO — the contract is stable; Plan 03-03 uses it)
    - `app/src/main/java/com/bugzz/filter/camera/filter/AssetLoader.kt` — @Singleton @Inject constructor stub with `suspend fun preload(filterId: String): Unit = TODO("Plan 03-03")` + `fun get(filterId: String, frameIdx: Int): Bitmap? = null`
    - `app/src/main/java/com/bugzz/filter/camera/render/BugBehavior.kt` + `BugState.kt` — per RESEARCH §Example 2 pattern; Static body TODO, Crawl/Swarm/Fall throw NotImplementedError (final — these are stable contracts)
    - `app/src/main/java/com/bugzz/filter/camera/render/FilterEngine.kt` — @Singleton stub `fun setFilter(def: FilterDefinition?): Unit = TODO` + `fun onDraw(canvas, frame, face): Unit = TODO`

    Add test resources at `app/src/test/resources/sprites/test_filter/manifest.json` + `frame_00.png` (use a minimal 1×1 transparent PNG byte array — can hand-craft in test setUp via `Bitmap.createBitmap(1,1,ARGB_8888).compress(PNG,100,outputStream)`).

    For `CameraControllerTest.kt`, APPEND the 3 new @Ignore'd @Test methods after the existing Phase 2 tests — do NOT rewrite the file. Existing Phase 2 tests stay GREEN.

    Wave 0 commit state: all new tests RED (TODOs) or @Ignore'd (Wave 3 dependencies); Phase 2's 10 pre-existing tests GREEN.
  </action>
  <acceptance_criteria>
    - File `app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogTest.kt` exists with 4 @Test methods (hasExactlyTwoFilters, antOnNose_resolves, spiderOnForehead_resolves, byId_unknown_returnsNull)
    - File `app/src/test/java/com/bugzz/filter/camera/filter/AssetLoaderTest.kt` exists with 7 @Test methods covering sizeOf, cache cap, get-before-preload, preload, idempotency, malformed PNG, manifest parse
    - File `app/src/test/java/com/bugzz/filter/camera/render/FilterEngineTest.kt` exists with 8 @Test methods covering early-returns, flipbook index, setMatrix-before-drawBitmap, no-biometric-log
    - File `app/src/test/java/com/bugzz/filter/camera/render/BugBehaviorTest.kt` exists with 6 @Test methods (sealed variant count, Static position/velocity, 3 TODO throws)
    - File `app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt` contains `capturePhoto_invokesTakePicture_onBoundImageCapture` + `capturePhoto_outputFileOptions_hasDCIMBugzzRelativePath` + `capturePhoto_controllerNotBound_emitsFailure`, all annotated `@Ignore("Plan 03-04 — flip to GREEN when capturePhoto method lands")`
    - Placeholder stubs exist at `app/src/main/java/com/bugzz/filter/camera/filter/FilterDefinition.kt`, `FilterCatalog.kt`, `SpriteManifest.kt`, `AssetLoader.kt`, and under `render/` for BugBehavior, BugState, FilterEngine
    - Test resources exist at `app/src/test/resources/sprites/test_filter/manifest.json` + `frame_00.png` (1×1 transparent PNG)
    - `./gradlew :app:testDebugUnitTest` compiles GREEN; new Wave 0 tests RED at TODO/assertion; Phase 2 tests stay GREEN; Wave 3 tests skipped (@Ignore)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "*FilterCatalogTest*" --tests "*AssetLoaderTest*" --tests "*FilterEngineTest*" --tests "*BugBehaviorTest*" --tests "*CameraControllerTest*capturePhoto*"</automated>
  </verify>
  <done>
    Wave 0 COMPLETE. 8 new test files + 1 extended test file landed. All Phase 3 REN-01..07 + CAP-01/03 + ADR-01 #1..3 behaviors have at least one pinning test. Compile GREEN; new tests RED (via TODO/wrong-const) or @Ignore'd (Wave 3 dependencies); Phase 2's 10 tests stay GREEN. Commit message: `test(03-01): Nyquist Wave 0 scaffolds for REN-01..07, CAP-01/03, ADR-01 #1..3 — all RED before SUTs land`.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :app:testDebugUnitTest` exits 0 (compile GREEN; all assertion failures are expected RED per Nyquist gate)
- Grep `grep -r "@Test" app/src/test/java/com/bugzz/filter/camera/` shows at least 30 new @Test methods across Wave 0 files (Phase 2 baseline was 11 methods; Wave 0 adds 30+ new)
- Grep `grep -r "@Ignore" app/src/test/java/com/bugzz/filter/camera/` shows `Plan 03-02` and `Plan 03-04` markers on tests gated on Wave 1 + Wave 3 SUTs
- `grep "TODO(\"Plan 03-" app/src/main/java/com/bugzz/filter/camera/` lists exactly the Wave 1 + Wave 2 + Wave 3 TODOs (no orphans)
- Phase 2 test count (10 GREEN tests) unchanged — Wave 0 does not touch Phase 2 test files except to APPEND @Ignore'd methods to `CameraControllerTest.kt`
</verification>

<success_criteria>
- 8 new test files + 1 extended test file exist
- All REN-01..07 behaviors, CAP-01/03 behaviors, and ADR-01 #1..3 behaviors have ≥1 @Test each (REN-08 + CAP-02/04/05/06 are manual-only per VALIDATION.md)
- Phase 2's 10 pre-existing tests remain GREEN after Wave 0 commit
- Compile is GREEN; Wave 0 tests are RED (via TODO / wrong-constant assertions) — the Nyquist RED gate is achieved
- Placeholder stubs exist for all Wave 1 + Wave 2 + Wave 3 SUTs, tagged with `TODO("Plan 03-0X")` for executor traceability
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-filter-end-to-end-photo-capture/03-01-SUMMARY.md` following the template.
</output>
