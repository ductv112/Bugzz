---
phase: 07
plan: 06
type: execute
wave: 4
status: draft
created: 2026-05-13
depends_on: [07-05]
files_modified:
  - .planning/phases/07-performance-device-matrix/07-HANDOFF.md
autonomous: true
requirements: [PRF-05]
threat_refs: []
must_haves:
  truths:
    - "07-HANDOFF.md exists at .planning/phases/07-.../07-HANDOFF.md with structure mirroring 06-HANDOFF.md (Pre-flight, Step 0 connectivity, hard gates per requirement, soft gates, OEM quirks, Final sign-off table)"
    - "Hard gates section maps 1:1 to PRF-01..05 (5 items) + 3 Phase-7-specific (D-19 thermal, D-20c LeakCanary, D-15 reference APK)"
    - "Cross-OEM matrix section per D-12/D-13/D-14 — primary Xiaomi 13T + secondary OEM placeholder (Samsung A-series OR Pixel A-series) + Firebase Test Lab 30 min/day opt-in fallback"
    - "Soft gates section folds 8 Phase 4+5 deferred UAT items per D-21 — verbatim from 06-HANDOFF.md Step 11"
    - "Vietnamese bilingual hints on key steps per memory `user_profile.md` (matches 06-HANDOFF.md precedent)"
    - "Device PASS criteria explicit: PRF-01..05 all GREEN on Xiaomi 13T AT MINIMUM + secondary OEM GREEN or N/A-documented per D-13"
    - "9 D-32 grep-asserts intact (verification step in runbook)"
  artifacts:
    - path: ".planning/phases/07-performance-device-matrix/07-HANDOFF.md"
      provides: "Device runbook for Phase 7 cross-OEM verification (PRF-01..05 + 8 deferred UAT + D-19 thermal + D-15 reference comparison + D-20c LeakCanary)"
  key_links:
    - from: "07-HANDOFF.md hard gates"
      to: "PRF-01..05 requirements"
      via: "1:1 mapping per requirement"
      pattern: "PRF-0[1-5]"
---

<objective>
Wave 4 — author `07-HANDOFF.md` Phase 7 cross-OEM device runbook. Pure documentation plan; no code changes. Plan 07-06 is autonomous; the BLOCKING device checkpoint lives in Plan 07-07.

The handoff folds three carry-forward concern sets into a single runbook:
1. **Phase 7 hard gates (PRF-01..05)** — verifies the work Plans 07-02/03/04 landed: release APK installs/boots, JankStats fps measurement ≥24, face detect ≤100ms median, 60s audio drift <50ms, runs on secondary OEM.
2. **Phase 7-only verification** — D-19 thermal stress, D-20c LeakCanary LAUNCHER disable, D-15 reference APK best-effort comparison.
3. **Phase 4+5 deferred UAT items (8 soft gates per D-21)** — verbatim from 06-HANDOFF.md Step 11. Phase 7 is the natural home for these per CONTEXT D-21.

Plus Vietnamese bilingual hints per memory `user_profile.md` (matches 06-HANDOFF.md precedent).

Output: one new markdown file (07-HANDOFF.md). Plan 07-07 executor will use this as the script for the closing device checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-device-matrix/07-CONTEXT.md
@.planning/phases/07-performance-device-matrix/07-RESEARCH.md
@.planning/phases/07-performance-device-matrix/07-VALIDATION.md
@.planning/phases/07-performance-device-matrix/07-PERF-REPORT.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-HANDOFF.md
@.planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-CHECKPOINT.md

<interfaces>
<!-- Pre-extracted section shapes for the 07-HANDOFF.md authoring task. -->

Mirror structure of 06-HANDOFF.md (Phase 6 handoff format — known good).

Required top-level sections (in order):
1. Title + device + APK references + Vietnamese summary
2. Pre-flight (user-side prerequisites)
3. What you are verifying — hard gates + Phase 7 only + soft gates tables
4. Known expected findings
5. Step 0 — APK + device connectivity
6. Step 1 — PRF-04 APK size verification
7. Step 2 — PRF-04 D-24 9 D-32 grep-asserts R8 survival
8. Step 3 — PRF-01 + PRF-02 measurement (debug APK; uses scripts from Plan 07-05)
9. Step 4 — PRF-03 60s audio drift via scripts/verify-audio-sync.sh
10. Step 5 — D-19 pre-warmed thermal stress
11. Step 6 — D-20c LeakCanary LAUNCHER disable on-device verification
12. Step 7 — D-15 reference APK comparison (best-effort)
13. Step 8 — PRF-05 secondary OEM smoke test (Samsung/Pixel OR Firebase Test Lab)
14. Step 11 — Phase 4+5 deferred UAT (8 soft gates, verbatim from 06-HANDOFF.md)
15. Logcat filter cheatsheet (Phase 7 additions on top of 06-HANDOFF tags)
16. OEM Quirks placeholder section
17. Final sign-off table (one row per Step + soft gates summary)
18. Sign-off protocol + Phase 7 gap-closure path (inline-fix vs new gap-plan per Phase 5 precedent)

CONTEXT cross-references:
- D-12: Xiaomi 13T primary
- D-13: Secondary OEM (user-sourced Samsung A or Pixel A); best-effort
- D-14: Firebase Test Lab free tier 30 min/day (RESEARCH §Alternatives correction — NOT 5 runs/day from CONTEXT D-14)
- D-15: Reference APK comparison best-effort; INSTALL_FAILED_MISSING_SPLIT acceptable defer
- D-19: Pre-warmed thermal stress methodology — 5×60s warmup + measurement
- D-20c: LeakCanary verification — monkey -c LAUNCHER selects MainActivity
- D-21: 8 Phase 4+5 deferred UAT items
- D-24: 9 D-32 grep-asserts survive R8 (decompile + grep verification using apkanalyzer dex packages)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 07-HANDOFF.md with all sections + Vietnamese bilingual hints</name>
  <files>
    .planning/phases/07-performance-device-matrix/07-HANDOFF.md
  </files>
  <read_first>
    - .planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-HANDOFF.md (FULL file — known-good Phase 6 handoff format; mirror its structure)
    - .planning/phases/06-ux-polish-splash-home-onboarding-preview-collection-share/06-CHECKPOINT.md (the "Soft Gates" + "Notes" tables — fold 8 deferred UAT items per D-21)
    - .planning/phases/07-performance-device-matrix/07-PERF-REPORT.md (if Plan 07-05 device session locked PRF-01..03 numbers, reference them in the relevant Steps)
    - .planning/phases/07-performance-device-matrix/07-CONTEXT.md D-12 through D-24
    - .planning/phases/07-performance-device-matrix/07-RESEARCH.md §Alternatives (Firebase Test Lab 30 min/day correction)
  </read_first>
  <action>
    Create `.planning/phases/07-performance-device-matrix/07-HANDOFF.md` mirroring 06-HANDOFF.md structure exactly, with the Phase 7 specifics below.

    **Section 1 — Title + device + APK references**:
    Title: "Phase 7 Handoff — Performance + Cross-OEM Device Matrix Runbook".
    Devices: primary Xiaomi 13T (HyperOS, Android 15); secondary Samsung A-series OR Pixel A-series user-sourced OR Firebase Test Lab opportunistic.
    APK references: both debug + release APK paths. Document why both are needed (debug for JankStats/Perf log measurement, release for size + R8 verification).
    Vietnamese summary block: "Đây là runbook verify Phase 7 trên Xiaomi 13T + ít nhất 1 OEM khác. Hard gates PRF-01..05 → fail block sign-off. Soft gate (Phase 4+5 carry-over) → log + defer."

    **Section 2 — Pre-flight**:
    Mirror 06-HANDOFF.md Pre-flight (USB debugging, MIUI settings, adb path, storage, share targets). Add Phase 7-specific:
    - Both debug + release APKs available
    - `ffprobe` installed on dev PC (`ffprobe -version`) per PRF-03 requirement
    - `apkanalyzer` available at Android SDK build-tools path (PRF-04 D-24 verification)
    - `bc` available in shell for audio drift math (if not, swap to awk equivalent — see Plan 07-05 script)

    Tiếng Việt: "Cần cả debug + release APK. Debug để đo fps + latency; release để verify size + boot."

    **Section 3 — What you are verifying** (single table per gate type):

    Hard gates table:
    | # | Requirement | Method | Acceptance |
    |---|-------------|--------|------------|
    | 1 | PRF-04 APK ≤40 MB | `du -h app/build/outputs/apk/release/*.apk` | ≤40 MB |
    | 2 | PRF-04 D-24 9 D-32 grep-asserts survive R8 | `apkanalyzer dex packages release.apk | grep <patterns>` | class names un-obfuscated |
    | 3 | PRF-01 Live preview ≥24 fps median | 30s Face Filter session + JankStats Logcat (debug APK) | Median frame duration ≤ 41 ms |
    | 4 | PRF-02 Face detection ≤100 ms median | 30s session + Timber Perf log (debug APK) | Median ≤100 ms, p95 ≤150 ms |
    | 5 | PRF-03 60s video audio drift <50 ms | Record 60s + scripts/verify-audio-sync.sh | Both drifts <0.050 s, frames 1795-1805 |
    | 6 | PRF-05 Secondary OEM works | Install + smoke test on second device OR FTL | Capture + record both succeed |

    Phase 7-only verification table:
    | # | Item | Method |
    |---|------|--------|
    | 7 | D-19 Pre-warmed thermal | 5×60s warmup + measurement session; ThermalMonitor log |
    | 8 | D-20c LeakCanary LAUNCHER disabled | monkey -c LAUNCHER selects MainActivity |
    | 9 | D-15 Reference APK comparison (best-effort) | adb install-multiple; skip-and-document if persists |

    Soft gates table (per D-21 — 8 items mapped to Phase 4+5):
    | # | Phase | Item | Source |
    |---|-------|------|--------|
    | 1 | 4 | Multi-face 2-person scene primary/secondary fallback | MOD-02 |
    | 2 | 4 | Subjective fps over 30s capture session | PRF-01 manual sanity |
    | 3 | 5 | Pinch + rotate gestures on InsectFilter sticker | MOD-06 |
    | 4 | 5 | Sticker survives camera flip + portrait-locked orientation | MOD-07 |
    | 5 | 5 | Audio sync subjective lip-sync | PRF-03 sanity |
    | 6 | 5 | Fresh-install RECORD_AUDIO permission dialog | VID-04 |
    | 7 | 5 | ThermalMonitor 60s+ extended stress | D-19 / PRF-01 sustained |
    | 8 | 5 | 05-gaps-02 sticker drag-axis direction polish | Phase 5 visual carry |

    **Section 4 — Known expected findings**:
    - Release APK ~25-35 MB (debug was 91.96 MB; R8 + LeakCanary strip + arm64-v8a + WebP = expected delta per RESEARCH §PRF-04)
    - JankStats first-60-frames jank is normal cold-start (RESEARCH Pitfall 9)
    - Per-frame `Perf detect=Xms frame=N` only emits in debug APK
    - On Xiaomi HyperOS pre-Plan-07-04: `monkey -c LAUNCHER` chose LeakCanary; post-Plan-07-04 should choose MainActivity (D-20c verification)
    - WebP sprites render identically to PNG (RESEARCH Pattern 5)
    - First-launch onboarding appears only on fresh data (DataStore-backed `onboarding_completed`)
    - Phase 4-6 features all carry forward (Face Filter, Insect Filter, Preview, Collection, Share, Settings, Splash, Onboarding)

    **Section 5 — Step 0** (APK + device connectivity):
    Mirror 06-HANDOFF.md Step 0 (adb devices, install -r, logcat -c, logcat > log &). Add ffprobe presence check + Phase 7-specific install: both debug + release APKs.

    **Section 6 — Step 1 (PRF-04 size)**:
    ```bash
    ls -la app/build/outputs/apk/release/app-release.apk
    du -h app/build/outputs/apk/release/app-release.apk
    ```
    Expected ≤40 MB. If >40 MB → audit + inline-fix. Tiếng Việt: "APK release phải ≤40 MB."

    PASS / FAIL line at end of each Step.

    **Section 7 — Step 2 (PRF-04 D-24 R8 survival)**:
    apkanalyzer dex packages + grep for the 9 patterns (relaxed forms per CONTEXT D-24): isCapturing, bindJob, OneShotEvent.FilterLoadError, captureFlash, FilterEngine, AssetLoader.preload, isRecording, CameraMode (FQN), DebugOverlayRenderer / canvas.setMatrix. Document classes that show up un-obfuscated; inline-fix narrow -keep rules if any are stripped.

    **Section 8 — Step 3 (PRF-01 + PRF-02 measurement, debug APK)**:
    Copy the measurement protocol from Plan 07-05 Task 2 step 4 verbatim:
    - Re-install debug APK
    - Grant CAMERA, RECORD_AUDIO permissions via `pm grant`
    - 30-second Face Filter session
    - logcat -s Perf:V capture
    - awk aggregation for median fps + median/p95 detect latency
    Tiếng Việt: "Đo trên debug APK (Perf log chỉ emit khi BuildConfig.DEBUG=true)."

    **Section 9 — Step 4 (PRF-03 audio drift)**:
    Record 60s + pull MP4 + run scripts/verify-audio-sync.sh. Acceptance per D-10/D-11. If Plan 07-05 already locked this number, reference 07-PERF-REPORT.md PRF-03 row instead of re-running.
    Tiếng Việt: "Record 60s rồi pull MP4 chạy ffprobe để check audio drift."

    **Section 10 — Step 5 (D-19 thermal stress)**:
    5×60s warmup + 1 measurement session. logcat capture for ThermalMonitor events. Document thermal status reached (NONE/LIGHT/MODERATE/SEVERE) + fps during stress (≥20 fps acceptable per D-19).

    **Section 11 — Step 6 (D-20c LeakCanary disable)**:
    ```bash
    adb shell pm dump com.bugzz.filter.camera | grep -A 5 "LAUNCHER"
    adb shell monkey -p com.bugzz.filter.camera -c android.intent.category.LAUNCHER 1
    adb logcat -d | grep "Activity\|monkey" | tail -5
    ```
    Expected: monkey selects MainActivity (not LeakLauncherActivity). Pre-Plan-07-04 selected LeakCanary on HyperOS (06-CHECKPOINT.md Notes); post-Plan-07-04 should select MainActivity.

    **Section 12 — Step 7 (D-15 reference APK comparison, best-effort)**:
    Try `adb install` then `adb install-multiple`. If `INSTALL_FAILED_MISSING_SPLIT` persists, document as DEFERRED — not a blocker. If succeeds, subjective filter render comparison.

    **Section 13 — Step 8 (PRF-05 secondary OEM)**:
    Three strategies (a) User has Samsung/Pixel: condensed install + smoke test
    (b) Firebase Test Lab opt-in: 30 min/day free tier (NOT 5 runs/day — RESEARCH §Alternatives correction). One Robo test per APK ≈ 3-5 min.
    (c) NEITHER available: document as N/A with reason; PRF-05 conditional PASS per D-13.
    Minimum smoke per secondary OEM: install + boot to Home + Face Filter live preview + capture photo + record 5s video + verify pull. Tiếng Việt: "Nếu chỉ có Xiaomi 13T → document N/A có lý do; không block phase."

    **Section 14 — Step 11 (8 soft gates per D-21)**:
    Verbatim from 06-HANDOFF.md Step 11 (the 8 items 11a-11h). Each with checkboxes + Notes column. Tiếng Việt: "8 items này từ Phase 4+5 — log result + defer là OK."

    **Section 15 — Logcat cheatsheet**:
    Phase 7 additions:
    ```bash
    adb logcat -s Perf:V ThermalMonitor:V FaceTracker:V *:S
    ```
    Plus Phase 1-6 tags from 06-HANDOFF.md (CameraVM, InsectFilterVM, PreviewVM, etc.).

    **Section 16 — OEM Quirks**:
    Empty placeholder per device (Xiaomi 13T / Samsung / Pixel) for free-form observations during Plan 07-07 session.

    **Section 17 — Final sign-off table**:
    One row per Step (Step 0-8 + Step 11 soft summary). Hard gate summary "All PASS: Yes / No". Soft gates: ✅/❌/N/A per item count.

    **Section 18 — Sign-off protocol + gap-closure path**:
    When hard gates pass: reply `PASS — proceed to Phase 7 close-out` with result table. Plan 07-07 Task 3 fires.
    Phase 7 gap-closure path:
    - Trivial fix → inline + amend SUMMARY (Phase 5 gaps-01 precedent)
    - Non-trivial → spawn 07-gaps-NN-PLAN.md
    - Environmental N/A → document; PRF-05 conditional PASS per D-13

    Footer: "Phase 7 handoff runbook — N steps covering PRF-01..05 + 3 phase-7-specific + 8 deferred UAT carries from Phase 4+5. Follows 06-HANDOFF.md format."

    After writing the file, verify structure:
    ```bash
    grep -c "^## Step " .planning/phases/07-performance-device-matrix/07-HANDOFF.md   # expect ≥ 9
    grep -c "PRF-0[1-5]" .planning/phases/07-performance-device-matrix/07-HANDOFF.md  # expect ≥ 5
    grep -c "PASS / FAIL" .planning/phases/07-performance-device-matrix/07-HANDOFF.md # expect ≥ 8
    grep -c "Tiếng Việt\|Vietnamese" .planning/phases/07-performance-device-matrix/07-HANDOFF.md  # expect ≥ 5 bilingual hints
    ```

    Commit:
    ```bash
    git add .planning/phases/07-performance-device-matrix/07-HANDOFF.md
    git commit -m "docs(07-06): author Phase 7 cross-OEM device runbook (PRF-01..05 + D-19/D-20c/D-15 + 8 deferred UAT per D-21)"
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/07-performance-device-matrix/07-HANDOFF.md &amp;&amp; grep -c "^## Step " .planning/phases/07-performance-device-matrix/07-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-performance-device-matrix/07-HANDOFF.md` exists, > 5 KB
    - `grep -c "^## Step " ...07-HANDOFF.md` returns ≥ 9 (Step 0..8 + Step 11)
    - `grep -c "PRF-0[1-5]" ...07-HANDOFF.md` returns ≥ 5
    - `grep -c "PASS / FAIL" ...07-HANDOFF.md` returns ≥ 8
    - `grep -c "Tiếng Việt\|Vietnamese" ...07-HANDOFF.md` returns ≥ 5 (bilingual hints landed)
    - Step 11 contains all 8 sub-items (11a-11h) per D-21
    - D-19 (thermal), D-20c (LeakCanary), D-15 (reference APK), D-24 (R8 grep survival) all referenced explicitly
    - Firebase Test Lab "30 min/day" wording present (NOT "5 runs/day" — RESEARCH correction)
    - Final sign-off table with Step rows for 0..8 + 11
    - 9 D-32 grep-asserts intact (run same patterns as Plan 07-01)
    - 1 atomic commit landed
  </acceptance_criteria>
  <done>
    07-HANDOFF.md ready for Plan 07-07 device execution. Mirrors 06-HANDOFF.md structure with all Phase 7 additions (PRF-01..05 + D-19/D-20c/D-15) and 8 D-21 deferred UAT items folded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (Documentation-only plan) | No new code paths; no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (No new threats — documentation-only plan) | — | — | — | Plan 07-06 only authors a markdown file. The device verification fires in Plan 07-07 against the production code already built by Plans 07-02/03/04 (whose threat models apply). |
</threat_model>

<rollback>
If Task 1 fails:
1. Markdown structure broken (unbalanced code fences or missing sections) — re-read 06-HANDOFF.md verbatim for missing piece + re-author the affected section.
2. If file size > 15 KB and getting unwieldy — split soft gates (Step 11) into a sub-section file like 07-HANDOFF-SOFT-GATES.md and reference from main. Document deviation in 07-06-SUMMARY.md.
</rollback>

<verification>
- 07-HANDOFF.md exists with all 18 sections
- ≥ 9 Step headings
- ≥ 5 PRF-0x references
- ≥ 8 PASS/FAIL sign-off lines
- ≥ 5 Vietnamese bilingual hints
- Step 11 = 8 sub-items per D-21
- 9 D-32 grep-asserts intact
- 1 atomic commit
</verification>

<success_criteria>
- [ ] 07-HANDOFF.md ready for Plan 07-07 execution
- [ ] Mirrors 06-HANDOFF.md structure
- [ ] 5 hard gates (PRF-01..05) + 3 Phase 7-only (D-19, D-20c, D-15) + 8 soft gates (D-21) all documented
- [ ] Vietnamese bilingual hints land on key steps
- [ ] 1 commit
</success_criteria>

<output>
Files created during this plan:
- `.planning/phases/07-performance-device-matrix/07-HANDOFF.md`
- `.planning/phases/07-performance-device-matrix/07-06-SUMMARY.md`
</output>
