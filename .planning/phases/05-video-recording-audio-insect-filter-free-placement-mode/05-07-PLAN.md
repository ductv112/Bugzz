---
phase: 05
plan: 07
type: execute
wave: 6
depends_on: [05-04, 05-05, 05-06]
files_modified:
  - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-HANDOFF.md
  - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md
  - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-07-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: []
threat_refs: [T-05-01, T-05-02, T-05-03, T-05-04, T-05-05, T-05-06, T-05-07]
must_haves:
  truths:
    - "Clean :app:assembleDebug succeeds (no warnings drift)"
    - "Full :app:testDebugUnitTest GREEN"
    - "05-HANDOFF.md exists with 15-step Xiaomi 13T runbook covering all VID-01..10 + MOD-03..07 gates"
    - "Phase 4 04-HUMAN-UAT 2 deferred items (multi-face 2-person + fps subjective) folded as bonus checks"
    - "User runs runbook on Xiaomi 13T; reports PASS/FAIL with logcat + ffmpeg artifacts"
    - "Post-PASS: 05-VALIDATION.md nyquist_compliant flipped true; 05-07-SUMMARY written; STATE.md + ROADMAP.md updated"
  artifacts:
    - path: ".planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-HANDOFF.md"
      provides: "Xiaomi 13T 15-step manual device runbook"
      contains: "adb shell"
    - path: ".planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-07-SUMMARY.md"
      provides: "Phase 5 closure summary post-device-PASS"
  key_links:
    - from: "05-HANDOFF.md"
      to: "VID-01..10 + MOD-03..07 device gates"
      via: "step-numbered acceptance criteria"
      pattern: "VID-\\|MOD-"
---

<objective>
Final wave: clean build, write 05-HANDOFF.md device runbook, pause for Xiaomi 13T device verification by user (autonomous=false), then post-PASS close out 05-VALIDATION nyquist flag + write 05-07-SUMMARY + update STATE.md/ROADMAP.md.

Mirrors Phase 2/3/4 closure pattern. Per memory `feedback_cadence.md`: stop-test per phase on Xiaomi 13T device.

Output: 05-HANDOFF runbook; device-verified PASS; phase closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-CONTEXT.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-RESEARCH.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-08-SUMMARY.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-HUMAN-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Clean debug APK build + full unit test suite</name>
  <files>
    (no files modified — build verification only)
  </files>
  <read_first>
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md §Sampling Rate
  </read_first>
  <action>
Run clean build chain:
```bash
cd "d:/ClaudeProject/appmobile/Bugzz"
JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew clean :app:assembleDebug :app:testDebugUnitTest
```

Verify:
- Clean exit code 0
- APK present at `app/build/outputs/apk/debug/app-debug.apk`
- All unit tests GREEN (count target: ≥30 — 10 Phase 2 + Phase 3 expansions + Phase 4 expansions + ~20 Phase 5 new/un-Ignored)
- No new warnings vs Phase 4 baseline

If build fails — debug Rule 3 auto-fix locally, document in 05-07-SUMMARY.
  </action>
  <acceptance_criteria>
    - `./gradlew clean :app:assembleDebug :app:testDebugUnitTest` exits 0
    - APK file exists at `app/build/outputs/apk/debug/app-debug.apk`
    - Unit test count ≥ Phase 4 baseline (no regression)
  </acceptance_criteria>
  <verify>
    <automated>cd "d:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew clean :app:assembleDebug :app:testDebugUnitTest</automated>
  </verify>
  <done>Clean APK + GREEN unit suite ready for device install.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 05-HANDOFF.md Xiaomi 13T 15-step runbook</name>
  <files>
    .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-HANDOFF.md
  </files>
  <read_first>
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-08-PLAN.md (model 04-HANDOFF.md structure for parity)
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-HUMAN-UAT.md (2 deferred items: multi-face 2-person, FPS subjective 30s)
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md §Manual-Only Verifications
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-CONTEXT.md §Specifics #5 (fold deferred Phase 4 items)
  </read_first>
  <action>
Create `05-HANDOFF.md` as a numbered 15-step device runbook. Structure mirrors 04-08-HANDOFF.md / 03-HANDOFF.md.

**Sections:**
1. **Setup** (preconditions: device USB ADB, scrcpy optional, `svc power stayon usb`)
2. **Install + first launch** (`adb install -r app-debug.apk`; verify CAMERA permission gate)
3. **Steps 1-13 — VID-01..10 + MOD-03..07 gates** (one step per requirement, plus integration steps)
4. **Steps 14-15 — Phase 4 04-HUMAN-UAT bonus** (multi-face 2-person + fps subjective)
5. **Sign-off table** (PASS/FAIL per step)
6. **Post-PASS instructions** (re-run executor for nyquist flip + summary write)

**15-step content (concrete examples):**

**Step 1 — Face Filter mode smoke (regression check from Phase 4):**
- Action: HomeScreen → Face Filter → wait for first face detection → confirm sprite renders.
- Pass criterion: sticker visible on face within 2s; logcat tag `OverlayEffect` shows `filter=<id> faces=1 draws=...`.

**Step 2 — VID-01 Record button visible:**
- Action: in Face Filter mode, observe BottomStart 56dp red circle with white border.
- Pass: button rendered; matches UI-SPEC §1 visual; idle state = filled red circle.

**Step 3 — VID-10 lazy RECORD_AUDIO permission:**
- Action: tap Record (first time in fresh install).
- Pass: system permission dialog appears for RECORD_AUDIO immediately.

**Step 4 — VID-10 deny permission flow:**
- Action: tap Deny.
- Pass: Snackbar "Microphone needed for video sound." with "Open Settings" action.

**Step 5 — VID-10 grant + record start:**
- Action: tap Record again → tap Allow → recording starts.
- Pass: red circle changes to white square inside; recording indicator appears at TopCenter.

**Step 6 — VID-07 recording indicator:**
- Action: observe red dot blinking (~1Hz) + timer counting up.
- Pass: timer "00:01" → "00:02" → ...; dot alpha pulses.

**Step 7 — VID-04 manual stop:**
- Action: tap Record button at ~00:05.
- Pass: indicator disappears; "Recording saved" Toast appears.

**Step 8 — VID-06 file in DCIM/Bugzz:**
- Action: `adb shell ls /sdcard/DCIM/Bugzz/*.mp4`.
- Pass: file exists with name `Bugzz_YYYYMMDD_HHmmss.mp4`.

**Step 9 — VID-02 + VID-03 overlay + audio in MP4:**
- Action: `adb pull <file>.mp4`; `ffmpeg -i video.mp4 -vf "select=eq(n\,15)" -frames:v 1 frame.png`; open frame.png.
- Pass: filter overlay visible at extracted frame; play in Google Photos → audio plays in sync with video.

**Step 10 — VID-04 auto-stop at 60s:**
- Action: tap Record → wait full 60s → recording auto-stops at 01:00.
- Pass: indicator disappears at 01:00; "Recording saved" Toast.

**Step 11 — VID-09 BackHandler discard flow:**
- Action: tap Record → at ~00:05 press device back button → AlertDialog "Recording in progress" appears → tap Discard.
- Pass: dialog dismissed; recording stopped; file `adb shell ls` shows pending file deleted; no Toast.

**Step 12 — VID-05 front-camera mirror:**
- Action: front cam record 5s → playback. Compare orientation to user's actual face.
- Pass: video shows mirrored view (matches preview orientation). Switch to back cam → record 5s → playback shows non-mirrored.

**Step 13 — MOD-03..06 Insect Filter sticker gestures:**
- Action: HomeScreen → Insect Filter → confirm sticker spawns at preview center → drag (1 finger), pinch (2 fingers), rotate (2 fingers twist).
- Pass: sticker translates with drag; scale clamps at min ~0.3x and max ~3x; rotation applies smoothly.

**Step 14 — MOD-07 sticker survives flip + orientation (and Phase 4 04-HUMAN-UAT bonus #1):**
- Action: place sticker top-left at scale 1.5x rotation 45° → flip front/back camera → confirm sticker preserved → orientation change to landscape (if app permits) → confirm preserved → flip back → flip lens with 2 faces in scene.
- Pass: sticker position/scale/rotation unchanged across all transitions. Bonus: 2-face scene does not crash; primary face receives full filter; secondary face graceful (Phase 4 MOD-02 #5).

**Step 15 — Insect Filter end-to-end record + Phase 4 04-HUMAN-UAT bonus #2 fps subjective 30s:**
- Action: in Insect Filter mode, tap Record, gesture sticker around for 30s, stop. Subjectively rate preview smoothness during this period.
- Pass: MP4 saved with sticker + audio in sync; preview did not visibly stutter for 30s. **Phase 4 deferred fps gate closure** — note any thermal indicator changes via `adb logcat -s ThermalMonitor:V`.

**Sign-off table:** 15 rows × {Step | Requirement | PASS/FAIL | Notes}. User fills.

**Post-PASS instructions:**
> When sign-off shows 15/15 PASS, run:
> 1. Re-invoke executor agent with task: "post-PASS Phase 5 close-out"
> 2. Executor flips `05-VALIDATION.md` frontmatter `nyquist_compliant: true`
> 3. Executor writes `05-07-SUMMARY.md`
> 4. Executor runs `gsd-tools roadmap update-plan-progress 05`
> 5. Executor updates `STATE.md` Current Position: Phase 5 complete

**Logcat filter cheatsheet section:**
```
adb logcat -s ThermalMonitor:V VideoRecorder:V FaceTracker:V OverlayEffect:V CameraVM:I
```
  </action>
  <acceptance_criteria>
    - File exists at `.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-HANDOFF.md`
    - File contains 15 numbered steps
    - Each step has explicit "Action:" + "Pass:" criteria
    - All 15 phase REQ-IDs (VID-01..10 + MOD-03..07) referenced at least once: `grep -c "VID-\|MOD-" 05-HANDOFF.md` ≥15
    - Phase 4 04-HUMAN-UAT bonus #1 (multi-face 2-person) referenced
    - Phase 4 04-HUMAN-UAT bonus #2 (fps subjective 30s) referenced
    - `adb pull` + `ffmpeg` commands present (VID-02 frame extract verification)
    - `adb shell ls /sdcard/DCIM/Bugzz/*.mp4` present (VID-06)
    - `adb logcat` filter command present
    - Sign-off table with 15 rows
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-HANDOFF.md"</automated>
  </verify>
  <done>05-HANDOFF.md device runbook complete and committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification checkpoint — Xiaomi 13T 15-step runbook sign-off</name>
  <files>(no files modified — manual device verification gate)</files>
  <action>User executes 05-HANDOFF.md steps 1-15 on Xiaomi 13T device. See &lt;what-built&gt; and &lt;how-to-verify&gt; below for full procedure. This task BLOCKS until user reports PASS or describes failure.</action>
  <verify><automated>(checkpoint:human-verify — manual gate; no automated verify)</automated></verify>
  <done>User replies "PASS — proceed to close-out" with logcat/screenshot evidence; OR planner spawns gap-closure plan on FAIL.</done>
  <what-built>
Phase 5 production code complete:
- ThermalMonitor + VideoRecorder + StickerState + StickerRenderer + InsectFilterViewModel + RecordingState
- CameraController production startRecording/stopRecording with MediaStore Video output
- CameraViewModel record lifecycle + isRecording guard + onDiscardRecording pending URI cleanup
- OverlayEffectBuilder cameraMode branch (FaceFilter vs InsectFilter)
- CameraScreen production Record button + indicator + AlertDialog + lock-during-record + RECORD_AUDIO lazy permission
- InsectFilterScreen 9-layer Box + detectTransformGestures + record UI mirror
- Nav graph rewired: Insect Filter mode live; stub deleted
- Clean debug APK assembled
- Unit suite GREEN (~30+ tests)
  </what-built>
  <how-to-verify>
1. `adb install -r app/build/outputs/apk/debug/app-debug.apk` on Xiaomi 13T
2. Open `.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-HANDOFF.md`
3. Execute steps 1-15 in order; record PASS/FAIL + screenshots/logcat as needed
4. For VID-02 frame-extract: `adb pull` MP4 → `ffmpeg -i video.mp4 -vf "select=eq(n\,15)" -frames:v 1 frame.png` → open frame.png and confirm overlay
5. For VID-08 thermal: enable `adb logcat -s ThermalMonitor:V` in a side terminal during the 60s recording test step

If ALL 15 steps PASS → reply with "PASS — proceed to close-out"
If any step FAILS → describe failure + share logcat snippet; planner spawns Phase 5 gap-closure plan
  </how-to-verify>
  <resume-signal>Type "PASS — proceed to close-out" with logcat/screenshot evidence, or describe failure (which step, what went wrong).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Post-PASS close-out — flip nyquist + write summary + update STATE/ROADMAP</name>
  <files>
    .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md
    .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-07-SUMMARY.md
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/04-filter-catalog-picker-face-filter-mode/04-08-SUMMARY.md (model summary structure)
    - .planning/STATE.md (current §Current Position section format)
    - .planning/ROADMAP.md (Phase 5 entry — bracket format `[ ]` → `[x]`; mark plans complete)
  </read_first>
  <action>
Only run AFTER user reports "PASS — proceed to close-out".

**Step A — Flip 05-VALIDATION.md nyquist flag:**
Change frontmatter `nyquist_compliant: false` → `nyquist_compliant: true` and `wave_0_complete: false` → `true`. Add Approval line: `**Approval:** approved 2026-MM-DD per Xiaomi 13T 15/15 sign-off`.

**Step B — Write 05-07-SUMMARY.md:**
Phase 5 closure summary covering all 7 plans. Mirror 04-08-SUMMARY structure:
- Phase scope recap (VID-01..10 + MOD-03..07; 15 REQs)
- Plans 01-07 quick recap (1-3 lines each)
- Device verification result (15/15 PASS or partial — list failures and gap-closure plan if any)
- Key Decisions during execution (any Rule 3 auto-fixes from each plan)
- Phase 3 + Phase 4 fix preservation grep results
- Performance observations from logcat (ThermalMonitor status changes during 60s record)
- Reference APK comparison (subjective; Phase 5 has no formal reference)
- Files added/modified count
- Next phase: Phase 6 UX Polish

**Step C — Update STATE.md:**
- Bump `progress.completed_phases: 5` and `completed_plans` count (add 7 — total ~34)
- Update `Current Focus:` to Phase 6 — UX Polish
- Add Phase 5 to "Key Decisions During Execution" §
- Update Phase Map: `Phase 5: ... [ complete ]`
- Update "Last agent" / "Last action" / "Stopped at" / "Next expected action"

**Step D — Update ROADMAP.md:**
- Phase 5 line: `- [x] **Phase 5: ...** ... (completed 2026-MM-DD)`
- Mark all 7 plans `- [x] 05-NN-PLAN.md` in Phase 5 plan list
- Update Progress table: `| 5. Video Recording + Audio + Insect Filter Mode | 7/7 | Complete | 2026-MM-DD |`

**Step E — Update REQUIREMENTS.md:**
Mark all 15 phase REQs as `[x]` complete in §Video Recording, §Dual Mode (MOD-03..07), and update the §Traceability table.

Use `gsd-tools` helpers where available:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress 05
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" requirements mark-complete VID-01 VID-02 ... MOD-07
```
  </action>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md` ≥1
    - File `05-07-SUMMARY.md` exists with all 7 plans recapped
    - `grep -c "Phase 5" .planning/STATE.md` ≥3 (Phase Map line + Decisions list + closure note)
    - `grep -c "Phase 5:" .planning/STATE.md` ≥1 AND `grep -c "complete" .planning/STATE.md` ≥1 (two simple substring matches verifying Phase 5 closure annotation; avoids brittle escaped-bracket regex per WARNING 11)
    - ROADMAP.md Phase 5 line marked `- [x]`
    - All 15 REQs marked `- [x]` in REQUIREMENTS.md
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-07-SUMMARY.md" &amp;&amp; grep -q "nyquist_compliant: true" ".planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md"</automated>
  </verify>
  <done>Phase 5 closed: 05-07-SUMMARY committed; nyquist flag true; STATE/ROADMAP/REQUIREMENTS updated; ready for Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → installable APK | Defensive: clean build + full unit suite before device install |
| Manual device verification | Human gate; 15-step explicit acceptance criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service (audio permission) | 05-HANDOFF Steps 3-5 | mitigate | Lazy permission flow tested |
| T-05-02 | Denial of Service (storage exhaustion) | 05-HANDOFF Step 8 | accept | Phase 7 PRF measures formally; Phase 5 ships and observes |
| T-05-03 | Tampering (concurrent recordings) | Plan 05-03 isRecording guard verified | mitigate | Inherited from prior plan |
| T-05-04 | Tampering (gesture during background) | 05-HANDOFF Step 13 | mitigate | Lifecycle-aware Compose state |
| T-05-05 | Information Disclosure (thermal listener leak) | Plan 05-02 process-lifetime singleton | accept | Verified architecturally |
| T-05-06 | Denial of Service (heavy sticker render) | Plan 05-02 lightweight Canvas ops | mitigate | Verified in Phase 5 unit + device |
| T-05-07 | Information Disclosure (orphan MediaStore) | 05-HANDOFF Step 11 | mitigate | Discard flow tests delete |
</threat_model>

<verification>
- Clean APK assembled
- Full unit suite GREEN
- 05-HANDOFF.md exists with 15-step runbook
- Phase 4 deferred items folded as bonus
- Device PASS gate satisfied by user (Task 3 checkpoint)
- Post-PASS: nyquist flag flipped + summary + STATE/ROADMAP updated
</verification>

<success_criteria>
- 5 ROADMAP Phase 5 success criteria all PASS via 05-HANDOFF runbook
- VID-01..10 + MOD-03..07 (15 REQs) marked complete
- Phase 4 04-HUMAN-UAT 2 deferred items closed
- T-05-01..07 dispositions verified
- Project ready for Phase 6 UX Polish
</success_criteria>

<output>
After Task 4 completes: Phase 5 closed.
- 05-07-SUMMARY.md written
- STATE.md + ROADMAP.md + REQUIREMENTS.md reflect completion
- User notified Phase 5 done; suggest `/gsd-discuss-phase 6` next
</output>
