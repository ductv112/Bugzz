---
phase: 05
plan: 06
type: execute
wave: 5
depends_on: [05-05]
files_modified:
  - app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt
  - app/src/main/java/com/bugzz/filter/camera/ui/home/InsectFilterStubScreen.kt
autonomous: true
requirements: []
threat_refs: []
must_haves:
  truths:
    - "BugzzApp nav graph routes CameraRoute(InsectFilter) to InsectFilterScreen (replaces stub)"
    - "InsectFilterStubScreen.kt deleted; no references remain in source tree"
    - "HomeScreen Insect Filter button enabled = true; navigates to InsectFilterScreen"
    - "CameraRoute(FaceFilter) unchanged — still resolves to CameraScreen"
  artifacts:
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt"
      provides: "Nav graph mapping CameraMode.InsectFilter → InsectFilterScreen"
      contains: "InsectFilterScreen"
    - path: "app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt"
      provides: "Production Insect Filter button (enabled=true)"
  key_links:
    - from: "BugzzApp NavGraph"
      to: "InsectFilterScreen composable"
      via: "composable<CameraRoute>"
      pattern: "InsectFilterScreen"
    - from: "HomeScreen Insect Filter button"
      to: "navController.navigate(CameraRoute(CameraMode.InsectFilter))"
      via: "onClick"
      pattern: "CameraMode.InsectFilter"
---

<objective>
Rewire navigation: BugzzApp routes `CameraRoute(InsectFilter)` to the new `InsectFilterScreen` (Plan 05-05). Delete `InsectFilterStubScreen.kt`. Enable HomeScreen's Insect Filter button (was Phase 4 placeholder `enabled = false`).

Purpose: Final UI integration — Phase 4 stub disappears, production Insect Filter mode goes live in nav graph.

Output: Stub deleted; routes wired; HomeScreen button live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-CONTEXT.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-UI-SPEC.md
@.planning/phases/04-filter-catalog-picker-face-filter-mode/04-07-SUMMARY.md
@app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
@app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt
@app/src/main/java/com/bugzz/filter/camera/ui/home/InsectFilterStubScreen.kt
@app/src/main/java/com/bugzz/filter/camera/ui/home/CameraMode.kt
@app/src/main/java/com/bugzz/filter/camera/ui/nav/Routes.kt

<interfaces>
<!-- Existing nav from Phase 4 -->
sealed interface Routes {
    data class CameraRoute(val mode: CameraMode) : Routes
    // ... others
}

enum class CameraMode { FaceFilter, InsectFilter }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewire BugzzApp nav graph + enable HomeScreen button + delete stub</name>
  <files>
    app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt
    app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt
    app/src/main/java/com/bugzz/filter/camera/ui/home/InsectFilterStubScreen.kt
  </files>
  <read_first>
    - app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt (Phase 4 04-07 wiring; CameraRoute branches by mode)
    - app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt (Phase 4 04-07; "Insect Filter" button currently `enabled = false` per 04-CONTEXT D-19)
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-UI-SPEC.md §Implementation Notes #9
  </read_first>
  <action>
**Step A — Update BugzzApp.kt nav route:**
Find the `composable<CameraRoute>` block (or equivalent type-safe nav entry). Phase 4 04-07 routes to `InsectFilterStubScreen` when `mode == CameraMode.InsectFilter`. Replace with `InsectFilterScreen(navController = navController)` — the new composable from Plan 05-05.

Pattern (concrete example based on standard Compose nav):
```kotlin
composable<CameraRoute> { entry ->
    val route: CameraRoute = entry.toRoute()
    when (route.mode) {
        CameraMode.FaceFilter -> CameraScreen(navController = navController)
        CameraMode.InsectFilter -> InsectFilterScreen(navController = navController)
    }
}
```
Add the necessary import: `import com.bugzz.filter.camera.ui.insect.InsectFilterScreen`. Remove the import for `InsectFilterStubScreen`.

**Step B — Update HomeScreen.kt:**
Find the `OutlinedButton("Insect Filter", enabled = false, ...)` (or equivalent) at the Insect Filter button site. Change `enabled = false` → `enabled = true`. Set onClick to:
```kotlin
onClick = { navController.navigate(CameraRoute(CameraMode.InsectFilter)) }
```
**Remove "Coming soon" subtitle** if present (UI-SPEC §Implementation Note 9).

**Step C — DELETE `InsectFilterStubScreen.kt`:**
Use file deletion. Since the file may have nav references in other paths, use grep first:
```bash
grep -r "InsectFilterStubScreen" app/src/main/java/
```
Should return matches only in BugzzApp.kt and the file itself. After updating BugzzApp.kt, delete the stub file:
- File: `app/src/main/java/com/bugzz/filter/camera/ui/home/InsectFilterStubScreen.kt`
- Delete via OS (the executor agent will handle).
  </action>
  <acceptance_criteria>
    - File `app/src/main/java/com/bugzz/filter/camera/ui/home/InsectFilterStubScreen.kt` does NOT exist (deleted)
    - `grep -r "InsectFilterStubScreen" app/src/main/java/com/bugzz/filter/camera/` returns ZERO matches
    - `grep -c "InsectFilterScreen" app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt` ≥1 (import + invocation)
    - `grep -c "CameraMode.InsectFilter" app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt` ≥1
    - `grep -c "CameraMode.FaceFilter" app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt` ≥1 (preserve FaceFilter route)
    - `grep -c "enabled = false" app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt` returns 0 OR limited to non-Insect-Filter elements (manual check)
    - `grep -c "navigate(CameraRoute(CameraMode.InsectFilter)\|CameraMode.InsectFilter)" app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt` ≥1
    - `grep -c "Coming soon\|coming soon" app/src/main/java/com/bugzz/filter/camera/ui/home/HomeScreen.kt` returns 0 (UI-SPEC §Stub Removal)
    - `grep -c "CameraScreen" app/src/main/java/com/bugzz/filter/camera/ui/BugzzApp.kt` ≥1 (FaceFilter unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd "d:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew :app:assembleDebug :app:testDebugUnitTest</automated>
  </verify>
  <done>InsectFilterStubScreen deleted; nav routes Insect Filter mode to production InsectFilterScreen; HomeScreen button enabled; clean APK assembles + suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nav route → screen composable | Type-safe nav contract; mode discriminator validated at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | Nav rewire is mechanical | accept | Type-safe nav prevents runtime route mismatch |
</threat_model>

<verification>
- InsectFilterStubScreen.kt deleted
- BugzzApp routes both CameraModes correctly
- HomeScreen Insect Filter button live
- Clean APK + suite GREEN
</verification>

<success_criteria>
- Phase 4 stub removed cleanly
- Production InsectFilterScreen reachable from HomeScreen
- FaceFilter path unchanged (regression-safe)
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-06-SUMMARY.md` documenting:
- File deletion confirmation
- Nav graph diff
- HomeScreen button state change
</output>
