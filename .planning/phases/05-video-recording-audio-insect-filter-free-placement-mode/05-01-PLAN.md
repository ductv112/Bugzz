---
phase: 05
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/com/bugzz/filter/camera/recording/VideoRecorderTest.kt
  - app/src/test/java/com/bugzz/filter/camera/thermal/ThermalMonitorTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/insect/StickerStateTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/insect/InsectFilterViewModelTest.kt
  - app/src/test/java/com/bugzz/filter/camera/render/StickerRendererTest.kt
  - app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt
  - app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt
autonomous: true
requirements: []
threat_refs: []
must_haves:
  truths:
    - "5 new test files compile RED (all tests @Ignored or asserting MISSING)"
    - "2 existing test files extended with Phase 5 cases (still @Ignored)"
    - "./gradlew :app:testDebugUnitTest passes (existing 10 GREEN; new tests do not break compile)"
  artifacts:
    - path: "app/src/test/java/com/bugzz/filter/camera/recording/VideoRecorderTest.kt"
      provides: "Robolectric Recorder construction tests for VID-04, VID-05"
    - path: "app/src/test/java/com/bugzz/filter/camera/thermal/ThermalMonitorTest.kt"
      provides: "Pure-JVM ThermalStatus mapping + frame-skip tests for VID-08"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/insect/StickerStateTest.kt"
      provides: "Pure-JVM gesture math + scale/offset clamp for MOD-03..06"
    - path: "app/src/test/java/com/bugzz/filter/camera/ui/insect/InsectFilterViewModelTest.kt"
      provides: "Hilt VM gesture handler + DataStore restore + flip-survival for MOD-07"
    - path: "app/src/test/java/com/bugzz/filter/camera/render/StickerRendererTest.kt"
      provides: "Robolectric Canvas mock translate/scale/rotate order"
  key_links:
    - from: "VideoRecorderTest.kt"
      to: "VideoRecorder.kt (will land Plan 02)"
      via: "@Ignore + KDoc forward-pointer"
      pattern: "@Ignore.*Plan 05-02"
    - from: "InsectFilterViewModelTest.kt"
      to: "InsectFilterViewModel.kt (will land Plan 02)"
      via: "@Ignore + KDoc forward-pointer"
      pattern: "@Ignore.*Plan 05-02"
---

<objective>
Wave 0 Nyquist test scaffolds. Land 5 new RED test files + extend 2 existing test files with Phase 5 placeholder cases. All new test methods @Ignored with KDoc pointing to the plan that will un-Ignore them. Existing 10 unit tests remain GREEN.

Purpose: Tests defined BEFORE production code per Phase 2/3/4 Nyquist precedent (STATE #1). Every requirement VID-01..10 + MOD-03..07 has a test file landed at this wave so per-task verification commands are wirable from Wave 1 onward.

Output: 5 new test files + 2 extended test files; compile-clean; suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-CONTEXT.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-RESEARCH.md
@.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md
@app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt
@app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt
@app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogExpandedTest.kt

<interfaces>
<!-- Phase 4 test patterns to mirror -->
- JUnit 4.13.2 + Robolectric 4.13 + Mockito-Kotlin + Turbine
- Robolectric tests: `@RunWith(RobolectricTestRunner::class) @Config(sdk = [34])`
- Pure JVM tests: no annotation
- @HiltViewModel test pattern: Dispatchers.setMain(StandardTestDispatcher()) in @Before
- VM Channel collection pattern: async { events.first() } before action, then advanceUntilIdle(), then deferred.await() (Phase 3 STATE #25)
- Mockito Uri trick: mock<Uri>() to avoid NPE under plain JVM (Phase 3 STATE #24)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 5 new RED test files (all @Ignored with forward KDoc)</name>
  <files>
    app/src/test/java/com/bugzz/filter/camera/recording/VideoRecorderTest.kt
    app/src/test/java/com/bugzz/filter/camera/thermal/ThermalMonitorTest.kt
    app/src/test/java/com/bugzz/filter/camera/ui/insect/StickerStateTest.kt
    app/src/test/java/com/bugzz/filter/camera/ui/insect/InsectFilterViewModelTest.kt
    app/src/test/java/com/bugzz/filter/camera/render/StickerRendererTest.kt
  </files>
  <read_first>
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-RESEARCH.md §Validation Architecture (per-REQ test map)
    - app/src/test/java/com/bugzz/filter/camera/filter/FilterCatalogExpandedTest.kt (Phase 4 pure-JVM pattern)
    - app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt (Robolectric pattern; 04-CONFIG sdk=34)
  </read_first>
  <action>
Create five RED test files. Every test method body is a single line `Assert.fail("Plan 05-02 / 05-03 / 05-04 lands SUT")` AND every test method is annotated `@Ignore("Plan 05-XX lands SUT")`. KDoc on the class points to the plan that un-Ignores it. Imports compile cleanly without referencing Phase 5 production classes (use TODO comments instead).

**File 1: VideoRecorderTest.kt** (Robolectric — Recorder construction):
```kotlin
package com.bugzz.filter.camera.recording

import org.junit.Ignore
import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner
import org.robolectric.annotation.Config

/**
 * RED scaffold for VID-04 (durationLimit) + VID-05 (mirror mode wired) +
 * audioEnabled toggle behavior. SUT lands in Plan 05-03.
 */
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [34])
class VideoRecorderTest {
    @Ignore("Plan 05-03 lands VideoRecorder SUT")
    @Test fun durationLimit_setTo60Seconds() { kotlin.test.fail("Plan 05-03 lands SUT") }

    @Ignore("Plan 05-03 lands VideoRecorder SUT")
    @Test fun startRecording_audioEnabledTrue_callsWithAudioEnabled() { kotlin.test.fail("Plan 05-03 lands SUT") }

    @Ignore("Plan 05-03 lands VideoRecorder SUT")
    @Test fun startRecording_audioEnabledFalse_omitsWithAudioEnabled() { kotlin.test.fail("Plan 05-03 lands SUT") }

    @Ignore("Plan 05-03 lands VideoRecorder SUT")
    @Test fun stopRecording_invokesRecordingStop() { kotlin.test.fail("Plan 05-03 lands SUT") }

    @Ignore("Plan 05-03 lands VideoRecorder SUT")
    @Test fun finalize_pendingDiscardFlag_deletesPendingUri() { kotlin.test.fail("Plan 05-03 lands SUT") }
}
```

**File 2: ThermalMonitorTest.kt** (pure JVM — ThermalStatus mapping + frame-skip):
```kotlin
package com.bugzz.filter.camera.thermal

import org.junit.Ignore
import org.junit.Test

/**
 * RED scaffold for VID-08. SUT lands in Plan 05-02.
 * Mock ThermalStatus values; verify ordinal ordering supports >= comparison;
 * verify frame-skip logic skips every 2nd frame when status >= Moderate.
 */
class ThermalMonitorTest {
    @Ignore("Plan 05-02 lands ThermalMonitor SUT")
    @Test fun mapStatus_powerManagerNone_returnsNone() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands ThermalMonitor SUT")
    @Test fun mapStatus_powerManagerModerate_returnsModerate() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands ThermalMonitor SUT")
    @Test fun thermalStatus_compareTo_supportsGreaterEqual() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands ThermalMonitor SUT")
    @Test fun status_initialValue_isNone() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands ThermalMonitor SUT")
    @Test fun frameSkip_belowModerate_doesNotSkip() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands ThermalMonitor SUT")
    @Test fun frameSkip_atModerate_skipsEverySecondFrame() { kotlin.test.fail("Plan 05-02 lands SUT") }
}
```

**File 3: StickerStateTest.kt** (pure JVM — gesture math):
```kotlin
package com.bugzz.filter.camera.ui.insect

import org.junit.Ignore
import org.junit.Test

/**
 * RED scaffold for MOD-03..06. SUT (StickerState data class + clamp helpers) lands in Plan 05-02.
 */
class StickerStateTest {
    @Ignore("Plan 05-02 lands StickerState SUT")
    @Test fun initialPosition_isCenter() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerState SUT")
    @Test fun drag_updatesOffsetByPanAmount() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerState SUT")
    @Test fun pinch_updatesScale_clampedToMax3() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerState SUT")
    @Test fun pinch_updatesScale_clampedToMin0_3() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerState SUT")
    @Test fun rotate_updatesRotation_modulo360() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerState SUT")
    @Test fun offset_clampedTo50PercentOverflow() { kotlin.test.fail("Plan 05-02 lands SUT") }
}
```

**File 4: InsectFilterViewModelTest.kt** (pure JVM with Dispatchers.setMain):
```kotlin
package com.bugzz.filter.camera.ui.insect

import org.junit.Ignore
import org.junit.Test

/**
 * RED scaffold for MOD-04..07. SUT (InsectFilterViewModel) lands in Plan 05-02.
 * Pattern: Dispatchers.setMain(StandardTestDispatcher()), mock CameraController +
 * AssetLoader + FilterPrefsRepository + StickerRenderer; assert state mutations
 * via uiState.first() pattern after advanceUntilIdle().
 */
class InsectFilterViewModelTest {
    @Ignore("Plan 05-02 lands InsectFilterViewModel SUT")
    @Test fun onStickerGesture_updatesStickerState() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands InsectFilterViewModel SUT")
    @Test fun onFilterSelected_savesToDataStore() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands InsectFilterViewModel SUT")
    @Test fun bind_restoresLastUsedFilterFromDataStore() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands InsectFilterViewModel SUT")
    @Test fun stickerState_survivesFlipLens() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands InsectFilterViewModel SUT")
    @Test fun bind_doesNotAttachFaceDetector() { kotlin.test.fail("Plan 05-02 lands SUT") }
}
```

**File 5: StickerRendererTest.kt** (Robolectric — Canvas mock):
```kotlin
package com.bugzz.filter.camera.render

import org.junit.Ignore
import org.junit.Test
import org.junit.runner.RunWith
import org.robolectric.RobolectricTestRunner
import org.robolectric.annotation.Config

/**
 * RED scaffold for sticker draw transform contract. SUT lands in Plan 05-02.
 * Verifies canvas.save() → translate → rotate → scale → drawBitmap → restore
 * exact call order via Mockito InOrder verifier on a mock Canvas.
 */
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [34])
class StickerRendererTest {
    @Ignore("Plan 05-02 lands StickerRenderer SUT")
    @Test fun setStickerState_mutatesInternalState() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerRenderer SUT")
    @Test fun setActiveFilter_loadsFromAssetLoader() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerRenderer SUT")
    @Test fun onDraw_appliesTranslateScaleRotateInOrder() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerRenderer SUT")
    @Test fun onDraw_emptyFrames_returnsEarly() { kotlin.test.fail("Plan 05-02 lands SUT") }

    @Ignore("Plan 05-02 lands StickerRenderer SUT")
    @Test fun onDraw_drawsBitmapCenteredAtOrigin() { kotlin.test.fail("Plan 05-02 lands SUT") }
}
```

Use `kotlin.test.fail` (already on classpath via existing test file imports). All 5 files compile-clean against current main classpath because no Phase 5 SUT class is referenced — only @Ignore + fail() in body.
  </action>
  <acceptance_criteria>
    - 5 new test files exist at exact paths listed in `<files>`
    - Each file has @RunWith + @Config (when Robolectric) or no class annotation (pure JVM)
    - Every @Test method has matching @Ignore("Plan 05-XX lands SUT") annotation
    - `grep -r "@Ignore" app/src/test/java/com/bugzz/filter/camera/recording/ app/src/test/java/com/bugzz/filter/camera/thermal/ app/src/test/java/com/bugzz/filter/camera/ui/insect/ app/src/test/java/com/bugzz/filter/camera/render/StickerRendererTest.kt` returns ≥27 matches (5+6+6+5+5)
    - No Phase 5 production classes referenced (zero imports from `com.bugzz.filter.camera.recording.VideoRecorder`, `.thermal.ThermalMonitor`, `.ui.insect.*`, `.render.StickerRenderer`)
  </acceptance_criteria>
  <verify>
    <automated>cd "d:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>5 new test files committed; `./gradlew :app:testDebugUnitTest` GREEN (existing 10 + new ignored = no failures); ready for Plan 02/03 SUT landing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend CameraControllerTest + CameraViewModelTest with Phase 5 placeholder cases</name>
  <files>
    app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt
    app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt
  </files>
  <read_first>
    - app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt (current Phase 2/3 tests; Robolectric)
    - app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt (Phase 3/4 patterns; Dispatchers.setMain; mock<Uri>() trick)
    - .planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-VALIDATION.md §Wave 0 Requirements §EXTEND blocks
  </read_first>
  <action>
**Append (do NOT replace existing tests)** new @Ignored test methods into both files.

**CameraControllerTest.kt — append at end of class (before closing brace):**
```kotlin
// ----- Phase 5 — Plan 05-03 will un-Ignore these -----

@Ignore("Plan 05-03 lands CameraController.startRecording / stopRecording production methods")
@Test fun bind_videoCaptureHasMirrorMode() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-03 lands CameraController.startRecording")
@Test fun startRecording_durationLimitSet() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-03 lands CameraController.startRecording audio toggle")
@Test fun startRecording_audioEnabledFlagToggle() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-03 lands CameraController.stopRecording")
@Test fun stopRecording_invokesRecordingStop() { kotlin.test.fail("Plan 05-03 lands SUT") }
```

**CameraViewModelTest.kt — append at end of class:**
```kotlin
// ----- Phase 5 — Plan 05-03 will un-Ignore these -----

@Ignore("Plan 05-03 lands onRecordTapped + RecordingState")
@Test fun onRecordTapped_idle_startsRecording_emitsActiveState() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-03 lands isRecording guard")
@Test fun onRecordTapped_alreadyActive_returnsEarly() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-03 lands onDiscardRecording")
@Test fun onDiscardRecording_stopsAndDeletesPendingUri() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-03 lands recording timer Status event handler")
@Test fun recordingState_statusEvent_incrementsElapsedMs() { kotlin.test.fail("Plan 05-03 lands SUT") }

@Ignore("Plan 05-04 lands lock-during-record UI signal")
@Test fun lockUI_duringRecording_pickerAlphaAndFlipDisabled() { kotlin.test.fail("Plan 05-04 lands SUT") }

@Ignore("Plan 05-03 lands lazy permission denial flow")
@Test fun onRecordTapped_audioPermissionDenied_emitsRationaleEvent() { kotlin.test.fail("Plan 05-03 lands SUT") }
```

Add the missing `@Ignore` import (`import org.junit.Ignore`) if not present in either file.
  </action>
  <acceptance_criteria>
    - `grep -c "Plan 05-03 lands SUT" app/src/test/java/com/bugzz/filter/camera/camera/CameraControllerTest.kt` ≥4
    - `grep -c "Plan 05-0[34] lands SUT" app/src/test/java/com/bugzz/filter/camera/ui/camera/CameraViewModelTest.kt` ≥6
    - Existing Phase 2/3/4 test methods unchanged (grep-assert: pre-existing test method names still present)
    - `import org.junit.Ignore` present in both files
  </acceptance_criteria>
  <verify>
    <automated>cd "d:/ClaudeProject/appmobile/Bugzz" &amp;&amp; JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" ./gradlew :app:testDebugUnitTest</automated>
  </verify>
  <done>2 test files extended with 4+6=10 new @Ignored Phase 5 cases; existing tests unchanged + still GREEN; suite compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test source ↔ main source | Test files reference no Phase 5 SUT classes — pre-implementation isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | Wave 0 test scaffolds only | accept | No production code, no runtime path; threat surface = zero |

(Wave 0 scaffolds carry no Phase 5 threat IDs. T-05-01..07 dispositions assigned in Plans 02-07.)
</threat_model>

<verification>
- 5 new test files compile and run (all tests @Ignored)
- 2 existing test files extended (existing Phase 2/3/4 tests still GREEN)
- `./gradlew :app:testDebugUnitTest` returns 0 exit code
- 10 existing GREEN tests continue passing
</verification>

<success_criteria>
- 7 test files (5 new + 2 extended) ready to be un-Ignored by Plans 02-04
- Per-task verification commands from 05-VALIDATION.md are now runnable (will fail until SUT lands; expected)
- Build is clean; no compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-video-recording-audio-insect-filter-free-placement-mode/05-01-SUMMARY.md` documenting:
- Files created (5)
- Files extended (2)
- Total @Ignored test method count
- Forward-pointer plan IDs (05-02, 05-03, 05-04)
</output>
